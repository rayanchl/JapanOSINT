# Source repair, round 8 — shard B (agent B), started 2026-09-20

Perpetual sweep-and-repair over `docs/batch30/shardB_ids.txt` (8,286 ids,
496 editable files listed in `docs/batch30/shardB_files.txt`). Method as in
round 7: a verdict is the re-sweep against the rebuilt binary
(`tools/audit_registry_emit.py --scheduled`), never an agent's own count.
One line per acted-on source in `source-repair-round8-B.tsv`.

## Progress

(updated per chunk)

## Fix patterns seen

(updated per chunk)

## Engine-shaped failures reported, not patched

(updated per chunk)

## Range reached

Not started yet.
