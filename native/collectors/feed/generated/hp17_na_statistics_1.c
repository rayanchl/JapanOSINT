/* Deep-record na_statistics sources (2), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "af-ihsn-na",
    .name = "IHSN Survey Catalog — Namibia",
    .collector = "na_statistics", .category = "statistics",
    .description = "IHSN index of Namibian surveys; idno, authoring_entity, year range. Detail by idno returns the full DDI study description.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"na\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://catalog.ihsn.org/index.php/api/catalog/search?ps=50&country=Namibia",
    .array_path = "result.rows",
    .detail_url = "https://catalog.ihsn.org/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-wbmicro-na",
    .name = "World Bank Microdata Library — Namibia datasets",
    .collector = "na_statistics", .category = "statistics",
    .description = "Namibia survey/census microdata studies (Namibia Statistics Agency has no live API; this is the working route). idno, doi, authoring_entity, access class, year range.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"na\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://microdata.worldbank.org/index.php/api/catalog/search?ps=50&country=Namibia",
    .array_path = "result.rows",
    .detail_url = "https://microdata.worldbank.org/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
