/* Deep-record mx_gazette sources (1), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "mx-dof-sidof-notas",
    .name = "Diario Oficial de la Federacion - SIDOF notas del dia (Mexico)",
    .collector = "mx_gazette", .category = "gazette",
    .description = "Undocumented JSON API behind Mexico's official gazette. One call for a date (DD-MM-YYYY) returns NotasMatutinas, NotasVespertinas and NotasExtraordinarias; each nota has codNota, section, date, diario id, issuing branch name (nombreCodOrgaUno), issuing secretariat (codOrgaDos), page range, order, status and flags for available HTML/DOC/PDF/image. DETAIL VERIFIED: /dof/sidof/notas/nota/5796377 returned a 33KB single Nota object with the full text and title - so every gazette entry can be expanded to its actual decree, appointment or sanction. Note: the tree already has www.dof.gob.mx scrapes but not this API host.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "gazette-record", .tags = "\"mx\",\"gazette\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "es",
    .url = "https://sidofqa.segob.gob.mx/dof/sidof/notas/14-08-2026",
    .array_path = "notasmatutinas",
    .detail_url = "https://sidofqa.segob.gob.mx/dof/sidof/notas/nota/{v}", .detail_key = "codNota",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
