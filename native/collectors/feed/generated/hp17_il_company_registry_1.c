/* Deep-record il_company-registry sources (2), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "il-ica-company-changes",
    .name = "Israel Registrar of Companies — changes and charges on corporations",
    .collector = "il_company-registry", .category = "company-registry",
    .description = "Per-corporation change events: corporation number, corporation name, request type (e.g. registration of a charge/pledge), status update date, charge id and request type code. The charge/security-interest trail behind an Israeli company.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "company-registry-record", .tags = "\"il\",\"company-registry\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "he",
    .url = "https://data.gov.il/api/3/action/datastore_search?resource_id=28780ab5-3ef1-44c7-8377-da82c0aa6781&limit=100",
    .array_path = "result.records",
    .detail_url = "https://data.gov.il/api/3/action/datastore_search?resource_id=28780ab5-3ef1-44c7-8377-da82c0aa6781&q={v}&limit=100", .detail_key = "מספר תאגיד",
    .interval = 86400, .free_tier = 1 },

  { .id = "il-moj-amutot-registered",
    .name = "Israel Registrar of Non-Profits — registered amutot",
    .collector = "il_company-registry", .category = "company-registry",
    .description = "Full NGO register: amuta number, registration date, Hebrew and English name, status and status-update date, activity classification and registered address. 24 columns per record.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "company-registry-record", .tags = "\"il\",\"company-registry\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "he",
    .url = "https://data.gov.il/api/3/action/datastore_search?resource_id=be5b7935-3922-45d4-9638-08871b17ec95&limit=100",
    .array_path = "result.records",
    .detail_url = "https://data.gov.il/api/3/action/datastore_search?resource_id=be5b7935-3922-45d4-9638-08871b17ec95&q={v}&limit=100", .detail_key = "מספר עמותה",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
