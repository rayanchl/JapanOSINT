/* Deep-record us_infrastructure sources (8), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "global-peeringdb-campus-list",
    .name = "PeeringDB campuses",
    .collector = "us_infrastructure", .category = "infrastructure",
    .description = "Multi-building datacentre campuses: id, org_id, org_name, name, aka, website, social_media, country/city/state/zipcode.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "infrastructure-record", .tags = "\"us\",\"infrastructure\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.peeringdb.com/api/campus?limit=3",
    .array_path = "data",
    .detail_url = "https://www.peeringdb.com/api/campus/{v}?depth=2", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "global-peeringdb-carrierfac",
    .name = "PeeringDB carrier facility presence",
    .collector = "us_infrastructure", .category = "infrastructure",
    .description = "Which datacentres a carrier fabric reaches: carrier_id, fac_id, facility name, created/updated. Carrier -> building hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "infrastructure-record", .tags = "\"us\",\"infrastructure\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.peeringdb.com/api/carrierfac?carrier_id=1",
    .array_path = "data",
    .detail_url = "https://www.peeringdb.com/api/fac/{v}?depth=2", .detail_key = "fac_id",
    .interval = 86400, .free_tier = 1 },

  { .id = "global-peeringdb-ixfac-by-ix",
    .name = "PeeringDB IX-to-facility links",
    .collector = "us_infrastructure", .category = "infrastructure",
    .description = "Which datacentres an exchange has switches in: ix_id, fac_id, facility name, city, country. IX -> physical building hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "infrastructure-record", .tags = "\"us\",\"infrastructure\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.peeringdb.com/api/ixfac?ix_id=26&limit=5",
    .array_path = "data",
    .detail_url = "https://www.peeringdb.com/api/fac/{v}?depth=2", .detail_key = "fac_id",
    .interval = 86400, .free_tier = 1 },

  { .id = "global-peeringdb-ixlan-by-ix",
    .name = "PeeringDB IX LAN",
    .collector = "us_infrastructure", .category = "infrastructure",
    .description = "The LAN under an exchange: mtu, dot1q_support, route-server ASN, arp_sponge, and the ixf_ixp_member_list_url (the exchange's own machine-readable member list, a further hop off-site).  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "infrastructure-record", .tags = "\"us\",\"infrastructure\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.peeringdb.com/api/ixlan?ix_id=26",
    .array_path = "data",
    .detail_url = "https://www.peeringdb.com/api/ixpfx?ixlan_id={v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "global-peeringdb-netfac-by-fac",
    .name = "PeeringDB facility presence (netfac)",
    .collector = "us_infrastructure", .category = "infrastructure",
    .description = "Which ASNs are physically present in a given datacentre: net_id, fac_id, local_asn, facility name/city/country. Facility -> tenant network hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "infrastructure-record", .tags = "\"us\",\"infrastructure\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.peeringdb.com/api/netfac?fac_id=1&limit=5",
    .array_path = "data",
    .detail_url = "https://www.peeringdb.com/api/net/{v}?depth=2", .detail_key = "net_id",
    .interval = 86400, .free_tier = 1 },

  { .id = "global-peeringdb-netixlan-by-ix",
    .name = "PeeringDB IX port memberships (netixlan)",
    .collector = "us_infrastructure", .category = "infrastructure",
    .description = "Per-port membership rows joining a network to an exchange LAN: net_id, ix_id, ixlan_id, asn, ipaddr4, ipaddr6, port speed (Mbit), is_rs_peer, bfd_support, operational flag. The IX -> member-ASN -> peering IP hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "infrastructure-record", .tags = "\"us\",\"infrastructure\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.peeringdb.com/api/netixlan?ix_id=26&limit=5",
    .array_path = "data",
    .detail_url = "https://www.peeringdb.com/api/net/{v}?depth=2", .detail_key = "net_id",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-arin-rdap-autnum",
    .name = "ARIN RDAP autnum",
    .collector = "us_infrastructure", .category = "infrastructure",
    .description = "ASN RDAP object: start/endAutnum, name, status, events, and nested entities carrying abuse/tech/admin POC handles with vCard (org, address, phone, email) and links back to whois.arin.net.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "infrastructure-record", .tags = "\"us\",\"infrastructure\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://rdap.arin.net/registry/autnum/15169",
    .array_path = "notices",
    .detail_url = "https://rdap.arin.net/registry/entity/{v}", .detail_key = "handle",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-arin-rdap-entity",
    .name = "ARIN RDAP entity (org) with autnums + networks",
    .collector = "us_infrastructure", .category = "infrastructure",
    .description = "RDAP entity for an org handle: full autnums[] array (each with handle, start/endAutnum, name, events for registration and last-changed) and networks[], plus nested entities with vCard contact blocks. 95 KB for GOGL.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "infrastructure-record", .tags = "\"us\",\"infrastructure\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://rdap.arin.net/registry/entity/GOGL",
    .array_path = "networks",
    .detail_url = "https://rdap.arin.net/registry/autnum/{v}", .detail_key = "endAutnum",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
