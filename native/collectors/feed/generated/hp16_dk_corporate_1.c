/* Deep-record dk_corporate sources (1), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "dk-erst-offentliggoerelser",
    .name = "Denmark ERST — filed annual reports Elasticsearch (6,422,525 filings)",
    .collector = "dk_corporate", .category = "corporate",
    .description = "Open, unauthenticated Elasticsearch endpoint over every annual report filed in Denmark: 6,422,525 hits total, 20 for one CVR. Each _source has cvrNummer, sagsNummer, regnskab.regnskabsperiode.startDato/slutDato, sidstOpdateret and dokumenter[] with dokumentType (AARSRAPPORT), dokumentMimeType and dokumentUrl pointing at the actual PDF and machine-readable XBRL. Supports both q= Lucene syntax and full POST DSL. NOTE: HTTP only, no TLS.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"dk\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "da",
    .url = "http://distribution.virk.dk/offentliggoerelser/_search?size=3&q=cvrNummer:28866984",
    .array_path = "hits.hits",
    .detail_url = "http://regnskaber.virk.dk/{v}", .detail_key = "dokumentUrl",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
