/* Deep-record us_environment sources (4), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "us-epa-envirofacts-br-reporting",
    .name = "EPA RCRA Biennial Report — hazardous waste generation",
    .collector = "us_environment", .category = "environment",
    .description = "Biennial hazardous-waste report rows: handler_id (EPA Handler ID), activity_location (state), source_type (B = biennial), sequence number, waste description, generation quantity, management method and receiving facility. The tonnage behind a RCRA generator.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "environment-record", .tags = "\"us\",\"environment\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://data.epa.gov/efservice/br_reporting/rows/0:0/JSON",
    .detail_url = "https://data.epa.gov/efservice/br_gm_waste_code/handler_id/{v}/rows/0:1000/JSON", .detail_key = "handler_id",
    .interval = 10800, .free_tier = 1 },

  { .id = "us-epa-envirofacts-pcs-permit-facility",
    .name = "EPA PCS — NPDES permitted discharger master",
    .collector = "us_environment", .category = "environment",
    .description = "Permit Compliance System discharger record: npdes (permit number, the join key), name_1 and name_2 (facility and process description), major_discharge_indicator, region, city_code/city_name, county_code/county_name, state, SIC, permit dates. Pivots to that permit's inspections and enforcement actions.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "environment-record", .tags = "\"us\",\"environment\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://data.epa.gov/efservice/pcs_permit_facility/rows/0:0/JSON",
    .detail_url = "https://data.epa.gov/efservice/pcs_inspection/npdes/{v}/rows/0:1000/JSON", .detail_key = "npdes",
    .interval = 10800, .free_tier = 1 },

  { .id = "us-epa-envirofacts-tri-facility",
    .name = "EPA Envirofacts — TRI facility master",
    .collector = "us_environment", .category = "environment",
    .description = "Toxics Release Inventory reporting facilities. Per row: tri_facility_id, facility_name, street_address, city_name, county_name, state_county_fips_code, state_abbr, zip_code, EPA region, fac_closed_ind, and the full mailing-address block (mail_name/street/city). Filter by any column: /efservice/tri_facility/<column>/<value>/rows/0:N/JSON. Joins straight into tri_reporting_form.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "environment-record", .tags = "\"us\",\"environment\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://data.epa.gov/efservice/tri_facility/state_abbr/TX/rows/0:2/JSON",
    .detail_url = "https://data.epa.gov/efservice/tri_facility/tri_facility_id/{v}/tri_reporting_form/rows/0:100/JSON", .detail_key = "tri_facility_id",
    .interval = 10800, .free_tier = 1 },

  { .id = "us-sdwis-water-system",
    .name = "EPA SDWIS — public water system master",
    .collector = "us_environment", .category = "environment",
    .description = "Every regulated public water system. pwsid (the key for the whole SDWIS family), pws_name, primacy_agency_code, epa_region, season_begin/end_date, pws_activity_code, pws_deactivation_date, pws_type_code (CWS/TNCWS/NTNCWS), gw_sw_code (groundwater/surface), dbpr and lt2 schedule categories, cds_id, population served. Pivots directly to that system's drinking-water violations.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "environment-record", .tags = "\"us\",\"environment\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://data.epa.gov/efservice/water_system/state_code/RI/rows/0:1/JSON",
    .detail_url = "https://data.epa.gov/efservice/violation/pwsid/{v}/rows/0:1000/JSON", .detail_key = "pwsid",
    .interval = 10800, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
