/* Deep-record int_environment sources (1), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "gcf-projects",
    .name = "Green Climate Fund — approved projects",
    .collector = "int_environment", .category = "environment",
    .description = "All 372 GCF projects: ProjectName, approved reference (FP001), board meeting, theme, sector, lifetime CO2, risk category, direct and indirect beneficiaries, total GCF funding, co-financing, total value, project URL, plus nested Countries, accredited Entities, Disbursements, Funding tranches and ResultAreas. Detail hop verified with ProjectsID 13020.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "environment-record", .tags = "\"int\",\"environment\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.gcfund.org/v1/projects",
    .detail_url = "https://api.gcfund.org/v1/projects/{v}", .detail_key = "ProjectsID",
    .interval = 10800, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
