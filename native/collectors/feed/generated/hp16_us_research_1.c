/* Deep-record us_research sources (12), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "crossref-work-detail",
    .name = "Crossref work record (detail)",
    .collector = "us_research", .category = "research",
    .description = "Full deposited metadata for one DOI: publisher, reference-count, license terms with delay-in-days and TDM urls, funder array (name + Crossref Funder DOI + award numbers), authors with ORCID and affiliations, container title, ISSNs, update-to (retraction/correction pointers) and assertion blocks. The DOI->funding+licence hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.crossref.org/works/10.1038/nature12373",
    .array_path = "message.reference",
    .detail_url = "https://api.crossref.org/works/{v}", .detail_key = "DOI",
    .interval = 86400, .free_tier = 1 },

  { .id = "openalex-institution-associated",
    .name = "OpenAlex institution -> associated institutions / roles",
    .collector = "us_research", .category = "research",
    .description = "Trimmed institution record exposing the org-graph edges: associated_institutions (each with id, ROR, country, type and relationship=parent/child/related) plus the institution's roles as funder/publisher/institution. Turns one university into its hospital, campus and institute network.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openalex.org/institutions/I27837315?select=id,display_name,associated_institutions,roles,repositories",
    .array_path = "associated_institutions",
    .detail_url = "https://api.openalex.org/institutions/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "openalex-institution-by-ror",
    .name = "OpenAlex institution by ROR id",
    .collector = "us_research", .category = "research",
    .description = "Resolve a bare ROR identifier to the full OpenAlex institution record (works counts, lineage, geo, associated institutions). The ROR->OpenAlex join key.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openalex.org/institutions?filter=ror:00cvxb145",
    .array_path = "results",
    .detail_url = "https://api.openalex.org/institutions/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "openalex-institution-detail",
    .name = "OpenAlex institution record (detail)",
    .collector = "us_research", .category = "research",
    .description = "One institution: ROR id, country, type, homepage, acronyms and alternative names, lineage (parent institutions), geo coordinates, works/cited-by counts, associated_institutions and repositories.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openalex.org/institutions/I27837315",
    .array_path = "topics",
    .detail_url = "https://api.openalex.org/institutions/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "openalex-work-by-doi",
    .name = "OpenAlex work by DOI (id pivot)",
    .collector = "us_research", .category = "research",
    .description = "Same full work record but keyed by DOI instead of OpenAlex id - lets you enter the OpenAlex graph from any Crossref/DataCite DOI you already hold and come back out with authors, institutions, funders and the referenced_works edge list.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openalex.org/works/doi:10.1038/nature12373",
    .array_path = "mesh",
    .detail_url = "https://api.openalex.org/works/doi:{v}", .detail_key = "doi",
    .interval = 86400, .free_tier = 1 },

  { .id = "openalex-works-by-author",
    .name = "OpenAlex works by author id",
    .collector = "us_research", .category = "research",
    .description = "Every work attributed to one OpenAlex author id (meta.count=68 for the probe), each row a full work object with co-authors, their institutions and countries, funders and citation counts. The author->publications hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openalex.org/works?filter=authorships.author.id:A5023888391&per_page=25",
    .array_path = "results",
    .detail_url = "https://api.openalex.org/works/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "openalex-works-by-ror",
    .name = "OpenAlex works by institution ROR",
    .collector = "us_research", .category = "research",
    .description = "All works with at least one author affiliated to a given ROR org (meta.count=499,568 for U. Washington), full work objects with co-author institutions and countries. Institution->output hop keyed on ROR rather than OpenAlex id.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openalex.org/works?filter=authorships.institutions.ror:https://ror.org/00cvxb145&per_page=10",
    .array_path = "results",
    .detail_url = "https://api.openalex.org/works/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "ror-org-detail-v1",
    .name = "ROR organisation record (v1 detail)",
    .collector = "us_research", .category = "research",
    .description = "Authoritative research-org record: established year, all external ids (Crossref Fundref ids, GRID, ISNI, Wikidata), names in multiple scripts, relationships to parent/child/related orgs, geonames location with lat/lng and country subdivision, website links, admin/schema versions.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.ror.org/organizations/https://ror.org/02kpeqv85",
    .array_path = "relationships",
    .detail_url = "https://api.ror.org/organizations/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "ror-org-detail-v2",
    .name = "ROR organisation record (v2 detail, bare id)",
    .collector = "us_research", .category = "research",
    .description = "Same record via the v2 path taking a bare ROR id (no url prefix) - the form you get from an OpenAlex or DataCite affiliation field. Includes the fundref id array which joins straight into Crossref funder queries.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.ror.org/v2/organizations/02kpeqv85",
    .array_path = "relationships",
    .detail_url = "https://api.ror.org/v2/organizations/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "ror-search-v2",
    .name = "ROR v2 organisation search",
    .collector = "us_research", .category = "research",
    .description = "Free-text org search returning 122 hits as full v2 records (external ids, locations, links, relationships) - the name->ROR-id resolver that then feeds OpenAlex, Crossref and DataCite affiliation filters.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.ror.org/v2/organizations?query=kyoto",
    .array_path = "items",
    .detail_url = "https://api.ror.org/v2/organizations/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "semanticscholar-paper-by-doi-graph",
    .name = "Semantic Scholar paper with references+citations expanded",
    .collector = "us_research", .category = "research",
    .description = "175KB in one request: the paper plus its authors with S2 authorIds AND the titles of every reference and every citing paper inline via the nested fields syntax. Three graph hops for one request. Keyless but rate-limited - the sibling /paper/search returned HTTP 429 during this probe, so expect intermittent throttling (S2_API_KEY raises limits).  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.semanticscholar.org/graph/v1/paper/DOI:10.7717/peerj.4375?fields=title,authors,references.title,citations.title",
    .array_path = "citations",
    .detail_url = "https://api.semanticscholar.org/graph/v1/paper/DOI:{v}?fields=title,abstract,authors,externalIds,references.title,references.externalIds,citations.title,citations.externalIds", .detail_key = "doi",
    .interval = 86400, .free_tier = 1 },

  { .id = "unpaywall-doi",
    .name = "Unpaywall OA location for a DOI",
    .collector = "us_research", .category = "research",
    .description = "For any Crossref DOI: best_oa_location and first_oa_location with host_type (publisher vs repository), licence, OA date, landing page and PDF url, plus the full oa_locations array listing every repository copy found. Requires an email query param, not a key.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"us\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.unpaywall.org/v2/10.7717/peerj.4375?email=rayanchouialkessal@gmail.com",
    .array_path = "z_authors",
    .detail_url = "https://api.unpaywall.org/v2/{v}?email=rayanchouialkessal@gmail.com", .detail_key = "doi",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
