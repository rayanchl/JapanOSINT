/* Deep-record ug_aid sources (2), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "af-dportal-act-ug",
    .name = "d-portal (IATI) — aid activities in Uganda",
    .collector = "ug_aid", .category = "aid",
    .description = "IATI aid activities with Uganda as recipient, with reporting organisation, funder reference, description, status and multi-currency commitment/spend. Detail hop by aid.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "aid-record", .tags = "\"ug\",\"aid\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://d-portal.org/q?from=act&country_code=UG&limit=100&form=json",
    .array_path = "rows",
    .detail_url = "https://d-portal.org/q?from=act&aid={v}&form=json", .detail_key = "aid",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-dportal-trans-ug",
    .name = "d-portal (IATI) — aid transactions in Uganda",
    .collector = "ug_aid", .category = "aid",
    .description = "Uganda activity-plus-transaction rows with trans_ref and trans_description; detail by aid returns transaction values, currencies, flow/finance/sector codes.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "aid-record", .tags = "\"ug\",\"aid\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://d-portal.org/q?from=act,trans&trans_country_code=UG&limit=100&form=json",
    .array_path = "rows",
    .detail_url = "https://d-portal.org/q?from=trans&aid={v}&form=json", .detail_key = "aid",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
