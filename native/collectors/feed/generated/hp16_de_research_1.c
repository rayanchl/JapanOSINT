/* Deep-record de_research sources (9), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "datacite-dois-by-client",
    .name = "DataCite DOIs deposited by one repository",
    .collector = "de_research", .category = "research",
    .description = "Everything a named repository has minted, full DataCite records with creators, affiliations and ORCIDs. Repository->holdings hop; combined with provider->clients it enumerates a whole national consortium's data output.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"de\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.datacite.org/dois?client-id=gesis.gesis&page[size]=5",
    .array_path = "data",
    .detail_url = "https://api.datacite.org/dois/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "datacite-dois-by-orcid",
    .name = "DataCite DOIs by creator ORCID",
    .collector = "de_research", .category = "research",
    .description = "Datasets and software authored by a specific ORCID, full records with affiliation. Researcher->data-output hop that Crossref cannot answer.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"de\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.datacite.org/dois?query=creators.nameIdentifiers.nameIdentifier:*0000-0001-6187-6610*&page[size]=5",
    .array_path = "data",
    .detail_url = "https://api.datacite.org/dois/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "datacite-dois-by-provider",
    .name = "DataCite DOIs under one provider organisation",
    .collector = "de_research", .category = "research",
    .description = "All DOIs across every repository belonging to one provider org, full records including contributors and affiliations. Institution-level research-output sweep.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"de\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.datacite.org/dois?provider-id=acjp&page[size]=5",
    .array_path = "data",
    .detail_url = "https://api.datacite.org/dois/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "datacite-dois-related-identifier",
    .name = "DataCite DOIs related to a given DOI",
    .collector = "de_research", .category = "research",
    .description = "Finds datasets, software and theses that declare a relationship to a target DOI (IsSupplementTo, Cites, IsDerivedFrom ...). The data-behind-the-paper hop, reversed.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"de\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.datacite.org/dois?query=relatedIdentifiers.relatedIdentifier:10.7717/peerj.4375&page[size]=5",
    .array_path = "data",
    .detail_url = "https://api.datacite.org/dois/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "datacite-dois-title-query",
    .name = "DataCite DOI search with affiliation+publisher expansion",
    .collector = "de_research", .category = "research",
    .description = "Fielded Elasticsearch query over DataCite with affiliation=true and publisher=true, which expands the creator affiliation objects and publisher entity inline instead of returning bare strings.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"de\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.datacite.org/dois?query=titles.title:graphene&page[size]=5&affiliation=true&publisher=true",
    .array_path = "data",
    .detail_url = "https://api.datacite.org/dois/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "datacite-prefixes",
    .name = "DataCite DOI prefix registry",
    .collector = "de_research", .category = "research",
    .description = "Every DataCite DOI prefix with creation date and relationships to its clients and providers - attribution of an arbitrary DOI prefix to the organisation behind it.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"de\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.datacite.org/prefixes?page[size]=5",
    .array_path = "meta.years",
    .detail_url = "https://api.datacite.org/prefixes/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "datacite-repositories",
    .name = "DataCite repositories directory",
    .collector = "de_research", .category = "research",
    .description = "Global directory of data repositories with symbol, re3data/OpenDOAR ids, client type, languages, certificates, domains, ISSNs, url and repository software. Registry of who is holding research data worldwide.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"de\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.datacite.org/repositories?page[size]=5",
    .array_path = "meta.years",
    .detail_url = "https://api.datacite.org/clients/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "wikidata-backlinks",
    .name = "Wikidata backlinks (what references this item)",
    .collector = "de_research", .category = "research",
    .description = "Every Wikidata item whose statements point at a target item, with continuation token. Reverse-edge traversal - e.g. all people whose employer/alma mater is a given university - without writing SPARQL.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"de\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.wikidata.org/w/api.php?action=query&list=backlinks&bltitle=Q336264&format=json&bllimit=10",
    .array_path = "query.backlinks",
    .detail_url = "https://www.wikidata.org/wiki/Special:EntityData/{v}.json", .detail_key = "title",
    .interval = 86400, .free_tier = 1 },

  { .id = "wikidata-wbgetclaims",
    .name = "Wikidata claims only (statement-level)",
    .collector = "de_research", .category = "research",
    .description = "Just the statements for an entity keyed by property (P17 country, P373 commons category, P571 inception, P1454 legal form, identifiers to ROR/GRID/ISNI/VIAF), each with snak hash, datatype, rank and statement GUID. The identifier-crosswalk workhorse.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"de\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.wikidata.org/w/api.php?action=wbgetclaims&entity=Q336264&format=json",
    .array_path = "claims.p1352",
    .detail_url = "https://www.wikidata.org/w/api.php?action=wbgetclaims&entity={v}&format=json", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
