/* Deep-record us_health sources (8), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "uk-govuk-search-drug-safety-update",
    .name = "GOV.UK Search API - MHRA Drug Safety Update",
    .collector = "us_health", .category = "health",
    .description = "MHRA monthly drug safety bulletins - product-level warnings, restrictions and label changes (e.g. botulinum toxin type A iatrogenic botulism warning). Title, link, summary, first_published_at; Content API detail hop returns the full article with the affected products and the regulatory action taken. NOTE: country should read GB.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"us\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.gov.uk/api/search.json?filter_format=drug_safety_update&count=3&fields=title,link,description,first_published_at",
    .array_path = "results",
    .detail_url = "https://www.gov.uk/api/content{v}", .detail_key = "link",
    .interval = 43200, .free_tier = 1 },

  { .id = "uk-govuk-search-medical-safety-alerts",
    .name = "GOV.UK Search API - MHRA medical device / drug safety alerts",
    .collector = "us_health", .category = "health",
    .description = "MHRA device safety information and national patient safety alerts, e.g. devices supplied without valid UKCA/CE conformity marking ordered removed from use. Per row: title with alert reference (DSI/2026/007), alert_type, issued_date, medical specialism. Content API detail hop returns the full alert naming the manufacturer, affected models and required action. NOTE: country should read GB.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"us\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.gov.uk/api/search.json?filter_format=medical_safety_alert&count=3&fields=title,link,description,alert_type,issued_date,medical_specialism",
    .array_path = "results",
    .detail_url = "https://www.gov.uk/api/content{v}", .detail_key = "link",
    .interval = 43200, .free_tier = 1 },

  { .id = "us-cms-data-catalog",
    .name = "CMS Open Data — DCAT catalogue",
    .collector = "us_health", .category = "health",
    .description = "3 MB Project Open Data catalogue for data.cms.gov. Per dataset: title, description, accrualPeriodicity, contactPoint, describedBy (data dictionary URL), and distribution[] where the entry carrying description:'latest' holds the STABLE accessURL https://data.cms.gov/data-api/v1/dataset/{uuid}/data. This is the authoritative way to resolve the provider-enrollment, ownership, change-of-ownership and revalidation dataset uuids used below.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"us\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://data.cms.gov/data.json",
    .array_path = "dataset",
    .detail_url = "https://data.cms.gov/data-api/v1/dataset/{v}/data?size=100", .detail_key = "accessURL",
    .interval = 43200, .free_tier = 1 },

  { .id = "us-cms-open-payments-catalog",
    .name = "CMS Open Payments — dataset catalogue",
    .collector = "us_health", .category = "health",
    .description = "127 KB DKAN catalogue of all 65 Open Payments datasets (general, research and ownership payment detail per reporting year 2013-2025, plus profile files). Each record's distribution[0].identifier is the id used by /api/1/datastore/query/{id}. The tree carries only the bare /datastore/query/ path with no dataset id — this resolves them.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"us\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://openpaymentsdata.cms.gov/api/1/metastore/schemas/dataset/items?show-reference-ids=false",
    .detail_url = "https://openpaymentsdata.cms.gov/api/1/datastore/query/{v}?limit=100", .detail_key = "distribution.identifier",
    .interval = 43200, .free_tier = 1 },

  { .id = "us-cms-provider-data-catalog",
    .name = "CMS Care Compare — dataset catalogue (DKAN metastore)",
    .collector = "us_health", .category = "health",
    .description = "550 KB DCAT catalogue of every Care Compare dataset with identifier (the 4x4 used by /datastore/query/{id}/0), title, description, theme, keyword[], modified/released/nextUpdateDate, publisher, contactPoint and distribution downloadURL. This is how you discover the nursing-home, hospital, hospice, home-health and dialysis enforcement tables without hardcoding ids.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"us\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://data.cms.gov/provider-data/api/1/metastore/schemas/dataset/items?show-reference-ids=false",
    .detail_url = "https://data.cms.gov/provider-data/api/1/metastore/schemas/dataset/items/{v}?show-reference-ids=false", .detail_key = "identifier",
    .interval = 43200, .free_tier = 1 },

  { .id = "us-cms-provider-enrollment",
    .name = "CMS — Medicare Fee-For-Service public provider enrollment",
    .collector = "us_health", .category = "health",
    .description = "2,978,925 rows (confirmed via the /data/stats endpoint). Per provider: NPI, MULTIPLE_NPI_FLAG, PECOS_ASCT_CNTL_ID (the PECOS associate id that links individuals to organisations), ENRLMT_ID, PROVIDER_TYPE_CD and PROVIDER_TYPE_DESC, STATE_CD, FIRST_NAME/MDL_NAME/LAST_NAME, ORG_NAME. Server-side filtering works: ?filter[NPI]=... returns the single record.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"us\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://data.cms.gov/data-api/v1/dataset/2457ea29-fc82-48b0-86ec-3b0755de7515/data?size=2",
    .detail_url = "https://data.cms.gov/data-api/v1/dataset/2457ea29-fc82-48b0-86ec-3b0755de7515/data?size=100&filter[NPI]={v}", .detail_key = "NPI",
    .interval = 43200, .free_tier = 1 },

  { .id = "us-cpsc-recall-by-date-range",
    .name = "CPSC SaferProducts — recalls in a date window",
    .collector = "us_health", .category = "health",
    .description = "902 KB covering H1-2026 recalls with the full nested envelope per recall (Products, Hazards, Injuries, Remedies, Manufacturers, Importers, Retailers, Distributors, ManufacturerCountries, ProductUPCs). The bounded-window form for incremental crawling; RecallDateEnd is the parameter the tree's existing open-ended call omits.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"us\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.saferproducts.gov/RestWebServices/Recall?format=json&RecallDateStart=2026-01-01&RecallDateEnd=2026-06-30",
    .detail_url = "https://www.saferproducts.gov/RestWebServices/Recall?format=json&RecallID={v}", .detail_key = "RecallID",
    .interval = 43200, .free_tier = 1 },

  { .id = "us-cpsc-recall-by-product-name",
    .name = "CPSC SaferProducts — recall search by product name",
    .collector = "us_health", .category = "health",
    .description = "85 recalls matching 'helmet', each a complete recall record (not a stub) with Products/Hazards/Injuries/Manufacturers/Importers/Retailers arrays. Free-text product pivot the tree lacks — the tree only carries the RecallDateStart form.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"us\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.saferproducts.gov/RestWebServices/Recall?format=json&ProductName=helmet",
    .detail_url = "https://www.saferproducts.gov/RestWebServices/Recall?format=json&RecallID={v}", .detail_key = "RecallID",
    .interval = 43200, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
