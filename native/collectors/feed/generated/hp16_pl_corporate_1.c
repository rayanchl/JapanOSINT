/* Deep-record pl_corporate sources (2), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "pl-mf-whitelist-bank-account",
    .name = "Poland VAT White List — reverse lookup by bank account",
    .collector = "pl_corporate", .category = "corporate",
    .description = "Reverse pivot: give a 26-digit Polish bank account number and get every taxpayer that has declared it — name, NIP, REGON, KRS, statusVat, addresses, representatives, partners. An account-number-to-company resolver, which is rare and very high value for tracing payments.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"pl\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "pl",
    .url = "https://wl-api.mf.gov.pl/api/search/bank-account/17103015080000000503131100?date=2026-08-14",
    .array_path = "result.subjects",
    .detail_url = "https://wl-api.mf.gov.pl/api/search/nip/{v}?date=2026-08-14", .detail_key = "nip",
    .interval = 86400, .free_tier = 1 },

  { .id = "pl-mf-whitelist-nips-batch",
    .name = "Poland VAT White List — batch NIP lookup",
    .collector = "pl_corporate", .category = "corporate",
    .description = "Batch form of the white list: comma-separated NIPs in one request, response grouped as entries[].identifier + entries[].subjects[] with the same full payload (name, statusVat, regon, krs, addresses, representatives, partners, accountNumbers). Lets you resolve a whole list of Polish counterparties in a single request.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"pl\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "pl",
    .url = "https://wl-api.mf.gov.pl/api/search/nips/5260250995,5250008198?date=2026-08-14",
    .array_path = "result.entries",
    .detail_url = "https://wl-api.mf.gov.pl/api/search/nip/{v}?date=2026-08-14", .detail_key = "identifier",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
