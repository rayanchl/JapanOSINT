/* Deep-record it_research sources (2), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "opencitations-meta-author",
    .name = "OpenCitations Meta works by author ORCID",
    .collector = "it_research", .category = "research",
    .description = "All works Meta attributes to one ORCID, each with co-authors (with their own ORCIDs and omids), venue, date and type. A third independent answer to 'what did this person publish', good for cross-checking ORCID and OpenAlex.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"it\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .mode = HP_CSV,
    .url = "https://api.opencitations.net/meta/v1/author/orcid:0000-0001-6187-6610",
    .detail_url = "https://api.opencitations.net/meta/v1/author/orcid:{v}", .detail_key = "orcid",
    .interval = 86400, .free_tier = 1 },

  { .id = "opencitations-meta-doi",
    .name = "OpenCitations Meta bibliographic record",
    .collector = "it_research", .category = "research",
    .description = "Merged bibliographic record showing ALL identifiers that resolve to the same work (multiple DOIs incl. the preprint, PMID, OMID), authors each with their own omid/orcid, venue with ISSN and OpenAlex source id, type, volume/issue/pages. Deduplicates works across identifier systems.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"it\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .mode = HP_CSV,
    .url = "https://api.opencitations.net/meta/v1/metadata/doi:10.7717/peerj.4375",
    .detail_url = "https://api.opencitations.net/meta/v1/metadata/doi:{v}", .detail_key = "doi",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
