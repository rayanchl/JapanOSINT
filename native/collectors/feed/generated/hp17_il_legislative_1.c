/* Deep-record il_legislative sources (6), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "il-knesset-agenda",
    .name = "Knesset — agenda motions (KNS_Agenda)",
    .collector = "il_legislative", .category = "legislative",
    .description = "Motions for the agenda: motion name, classification, Knesset number, urgency subtype, status, InitiatorPersonID and MinisterPersonID (pivot to KNS_Person), CommitteeID, government recommendation and postponement reason.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "legislative-record", .tags = "\"il\",\"legislative\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "he",
    .url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_Agenda?$top=100&$format=json",
    .array_path = "value",
    .detail_url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_Agenda({v})?$format=json", .detail_key = "AgendaID",
    .interval = 86400, .free_tier = 1 },

  { .id = "il-knesset-bill-name",
    .name = "Knesset — bill name history (KNS_BillName)",
    .collector = "il_legislative", .category = "legislative",
    .description = "Every historical title a bill carried, with the reading stage the title belonged to (NameHistoryTypeDesc). Lets you track a bill through renames; BillID pivots to the bill record.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "legislative-record", .tags = "\"il\",\"legislative\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "he",
    .url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_BillName?$top=100&$format=json",
    .array_path = "value",
    .detail_url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_Bill({v})?$format=json", .detail_key = "BillID",
    .interval = 86400, .free_tier = 1 },

  { .id = "il-knesset-cmt-session-item",
    .name = "Knesset — committee agenda items (KNS_CmtSessionItem)",
    .collector = "il_legislative", .category = "legislative",
    .description = "The individual items discussed inside a committee sitting: item name, ordinal, status, item type, and the CommitteeSessionID / ItemID that link back to the session and to the underlying bill or query.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "legislative-record", .tags = "\"il\",\"legislative\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "he",
    .url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_CmtSessionItem?$top=100&$format=json",
    .array_path = "value",
    .detail_url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_CommitteeSession({v})?$format=json", .detail_key = "CommitteeSessionID",
    .interval = 86400, .free_tier = 1 },

  { .id = "il-knesset-committee-session",
    .name = "Knesset — committee sessions (KNS_CommitteeSession)",
    .collector = "il_legislative", .category = "legislative",
    .description = "Committee sitting records: room/location, committee id, open-vs-closed type, status, start and finish datetimes, plus SessionUrl and BroadcastUrl.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "legislative-record", .tags = "\"il\",\"legislative\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "he",
    .url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_CommitteeSession?$top=100&$format=json",
    .array_path = "value",
    .detail_url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_CmtSessionItem?$filter=CommitteeSessionID%20eq%20{v}&$format=json", .detail_key = "CommitteeSessionID",
    .interval = 86400, .free_tier = 1 },

  { .id = "il-knesset-faction",
    .name = "Knesset — parliamentary factions (KNS_Faction)",
    .collector = "il_legislative", .category = "legislative",
    .description = "Every faction across all Knessets: name, Knesset number, start and finish dates, IsCurrent flag. Resolves faction_id seen on vote records.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "legislative-record", .tags = "\"il\",\"legislative\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "he",
    .url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_Faction?$top=100&$format=json",
    .array_path = "value",
    .detail_url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_Faction({v})?$format=json", .detail_key = "FactionID",
    .interval = 86400, .free_tier = 1 },

  { .id = "il-knesset-query",
    .name = "Knesset — parliamentary questions (KNS_Query)",
    .collector = "il_legislative", .category = "legislative",
    .description = "Parliamentary questions to ministers: subject, urgency type, status, asking PersonID, answering GovMinistryID, submit date and minister reply date. Direct MK-to-ministry accountability trail.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "legislative-record", .tags = "\"il\",\"legislative\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "he",
    .url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_Query?$top=100&$format=json",
    .array_path = "value",
    .detail_url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_DocumentQuery?$filter=QueryID%20eq%20{v}&$format=json", .detail_key = "QueryID",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
