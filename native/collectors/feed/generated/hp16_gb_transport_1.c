/* Deep-record gb_transport sources (5), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "uk-tfl-line-route-all",
    .name = "TfL all lines with route sections",
    .collector = "gb_transport", .category = "transport",
    .description = "Every line in the network (82 KB) with id, name, modeName, created/modified, lineStatuses and routeSections giving each direction's name, originationName/destinationName, originator and destination Naptan codes and validFrom/validTo. Full network graph in one request.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"gb\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.tfl.gov.uk/Line/Route?serviceTypes=Regular",
    .detail_url = "https://api.tfl.gov.uk/Line/{v}/StopPoints", .detail_key = "id",
    .interval = 3600, .free_tier = 1 },

  { .id = "uk-tfl-line-route-sequence",
    .name = "TfL line route sequence with geometry",
    .collector = "gb_transport", .category = "transport",
    .description = "Ordered station sequence for a line and direction plus the lineStrings polyline geometry, orderedLineRoutes and per-station stationId/icsId/topMostParentId/zone/lat/lon. Physical route trace, not just a stop list.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"gb\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.tfl.gov.uk/Line/victoria/Route/Sequence/inbound",
    .array_path = "stations",
    .detail_url = "https://api.tfl.gov.uk/StopPoint/{v}", .detail_key = "stationId",
    .interval = 3600, .free_tier = 1 },

  { .id = "uk-tfl-line-stoppoints",
    .name = "TfL line -> all stop points",
    .collector = "gb_transport", .category = "transport",
    .description = "Every stop served by a line: naptanId, stationNaptan, hubNaptanCode, icsCode, stopType, modes, the lines and lineGroups served, lineModeGroups, lat/lon and additionalProperties. Line -> station roster hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"gb\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.tfl.gov.uk/Line/victoria/StopPoints",
    .detail_url = "https://api.tfl.gov.uk/StopPoint/{v}", .detail_key = "naptanId",
    .interval = 3600, .free_tier = 1 },

  { .id = "uk-tfl-stoppoint-detail",
    .name = "TfL stop point detail",
    .collector = "gb_transport", .category = "transport",
    .description = "One station/interchange: naptanId, hubNaptanCode, stopType, all modes, every line calling there, lineGroup and lineModeGroups, children stops (platforms, entrances, bus stands) and additionalProperties (zone, WiFi, toilets, accessibility).  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"gb\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.tfl.gov.uk/StopPoint/940GZZLUVIC",
    .array_path = "additionalproperties",
    .detail_url = "https://api.tfl.gov.uk/StopPoint/{v}/Arrivals", .detail_key = "naptanId",
    .interval = 3600, .free_tier = 1 },

  { .id = "uk-tfl-stoppoint-search",
    .name = "TfL stop point name search",
    .collector = "gb_transport", .category = "transport",
    .description = "Name -> stop ids with icsId, modes, fare zone, topMostParentId, lat/lon and total match count. The entry point that makes every other TfL id-based hop reachable from a plain place name.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"gb\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.tfl.gov.uk/StopPoint/Search/Victoria",
    .array_path = "matches",
    .detail_url = "https://api.tfl.gov.uk/StopPoint/{v}", .detail_key = "id",
    .interval = 3600, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
