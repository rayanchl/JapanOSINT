/* Deep-record za_statistics sources (3), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "af-dhs-data-za",
    .name = "DHS Program — South Africa indicator data",
    .collector = "za_statistics", .category = "statistics",
    .description = "South Africa DHS estimates by survey and characteristic: Indicator, Value, SurveyId, SurveyYear, CharacteristicCategory/Label. Detail by IndicatorId returns definition, denominator and measurement type.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"za\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.dhsprogram.com/rest/dhs/data?countryIds=ZA&f=json&perpage=100",
    .array_path = "data",
    .detail_url = "https://api.dhsprogram.com/rest/dhs/indicators/{v}?f=json", .detail_key = "IndicatorId",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-ihsn-za",
    .name = "IHSN Survey Catalog — South Africa",
    .collector = "za_statistics", .category = "statistics",
    .description = "IHSN index of South African surveys with idno, authoring_entity, year range, access class; detail by idno returns the full DDI record.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"za\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://catalog.ihsn.org/index.php/api/catalog/search?ps=50&country=South%20Africa",
    .array_path = "result.rows",
    .detail_url = "https://catalog.ihsn.org/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-wbmicro-za",
    .name = "World Bank Microdata Library — South Africa datasets",
    .collector = "za_statistics", .category = "statistics",
    .description = "South Africa survey/census microdata studies with idno, doi, authoring_entity and access class; detail by idno returns producers, contacts and sampling.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"za\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://microdata.worldbank.org/index.php/api/catalog/search?ps=50&country=South%20Africa",
    .array_path = "result.rows",
    .detail_url = "https://microdata.worldbank.org/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
