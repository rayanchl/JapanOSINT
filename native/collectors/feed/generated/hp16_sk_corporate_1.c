/* Deep-record sk_corporate sources (3), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "sk-rpo-by-ico",
    .name = "Slovakia RPO — lookup by IČO",
    .collector = "sk_corporate", .category = "corporate",
    .description = "Same RPO record set but pivoted on the 8-digit IČO instead of a name, so a company number from any other Slovak source resolves straight to the full name history (e.g. SLOVTEL GSM → GLOBTEL GSM → Globtel → Orange Slovensko), address history, legal form and statutory bodies. The direct entity-number entry point into the Slovak register.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"sk\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "sk",
    .url = "https://api.statistics.sk/rpo/v1/search?identifier=35697270",
    .array_path = "results",
    .detail_url = "https://api.statistics.sk/rpo/v1/entity/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "sk-rpo-search",
    .name = "Slovakia RPO — Register of Legal Entities (name search)",
    .collector = "sk_corporate", .category = "corporate",
    .description = "Keyless JSON search over the Slovak Register právnických osôb. Every hit is already a rich record: internal id, identifiers (ICO with validFrom), full name history with validFrom/validTo, address history, legalForms, establishment/termination dates, sourceRegister (Obchodný register / Živnostenský register), legalStatuses (e.g. 'Zaniknutá spoločnosť'), and empty-or-populated statutoryBodies and stakeholders arrays. The `id` is the key to the entity detail hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"sk\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "sk",
    .url = "https://api.statistics.sk/rpo/v1/search?fullName=Slovensk&limit=5",
    .array_path = "results",
    .detail_url = "https://api.statistics.sk/rpo/v1/entity/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "sk-rpvs-pvs-expand-partner",
    .name = "Slovakia RPVS - public-sector partners with parent entity expanded",
    .collector = "sk_corporate", .category = "corporate",
    .description = "Slovak Register of Public Sector Partners (mandatory beneficial-ownership register for anyone taking public money). This $expand form returns each registered partner - person name/surname/date of birth or company ObchodneMeno + ICO, legal-person flag, validity from/to - together with its parent Partner record. The tree has the bare list; the expand form and the id detail hop below are the deep path.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"sk\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "sk",
    .url = "https://rpvs.gov.sk/opendatav2/PartneriVerejnehoSektora?$expand=Partner",
    .array_path = "value",
    .detail_url = "https://rpvs.gov.sk/opendatav2/Partneri({v})?$expand=PartneriVerejnehoSektora,KonecniUzivateliaVyhod,OpravneneOsoby,VerejniFunkcionari", .detail_key = "Id",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
