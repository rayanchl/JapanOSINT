/* Deep-record de_transport sources (2), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "de-vbb-transport-rest-locations",
    .name = "VBB (Berlin-Brandenburg) location search",
    .collector = "de_transport", .category = "transport",
    .description = "Stop name -> stop id with lat/lon, products served (suburban/subway/tram/bus/ferry/express/regional booleans), IFOPT id and stationDHID (the German national stop identifier). Entry hop for the departures API.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"de\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "de",
    .url = "https://v6.vbb.transport.rest/locations?query=Alexanderplatz&results=3",
    .detail_url = "https://v6.bvg.transport.rest/stops/{v}/departures?results=10", .detail_key = "id",
    .interval = 3600, .free_tier = 1 },

  { .id = "eu-transitous-map-stops",
    .name = "Transitous stops in a bounding box",
    .collector = "de_transport", .category = "transport",
    .description = "All transit stops inside a bbox with stopId, name, lat/lon, level, timezone, free-text description of the physical stop, vertexType and served modes. Geographic entry point into the aggregated feed set.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"de\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.transitous.org/api/v1/map/stops?min=52.5,13.4&max=52.52,13.42",
    .detail_url = "https://api.transitous.org/api/v1/stoptimes?stopId={v}&n=10", .detail_key = "stopId",
    .interval = 3600, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
