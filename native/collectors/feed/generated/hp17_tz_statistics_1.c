/* Deep-record tz_statistics sources (4), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "af-dhs-data-tz",
    .name = "DHS Program — Tanzania indicator data",
    .collector = "tz_statistics", .category = "statistics",
    .description = "Tanzania DHS estimates by survey and characteristic with Indicator, Value, SurveyId and IndicatorId. Detail by IndicatorId returns indicator metadata.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"tz\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.dhsprogram.com/rest/dhs/data?countryIds=TZ&f=json&perpage=100",
    .array_path = "data",
    .detail_url = "https://api.dhsprogram.com/rest/dhs/indicators/{v}?f=json", .detail_key = "IndicatorId",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-ihsn-tz",
    .name = "IHSN Survey Catalog — Tanzania",
    .collector = "tz_statistics", .category = "statistics",
    .description = "IHSN index of Tanzanian surveys with idno, authoring_entity and year range; detail by idno returns the full DDI study description.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"tz\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://catalog.ihsn.org/index.php/api/catalog/search?ps=50&country=Tanzania",
    .array_path = "result.rows",
    .detail_url = "https://catalog.ihsn.org/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-nada-tz-nbs-microdata",
    .name = "Tanzania NBS — microdata catalogue",
    .collector = "tz_statistics", .category = "statistics",
    .description = "62 Tanzanian statistical datasets including the 2023/24 Statistical Business Register Census (an enterprise register). Row: id, type (survey/document), idno, title, subtitle, nation, authoring_entity, year_start/end, varcount, views, downloads, url. Detail by idno gives the full DDI record.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"tz\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://microdata.nbs.go.tz/index.php/api/catalog/search?ps=100",
    .array_path = "result.rows",
    .detail_url = "https://microdata.nbs.go.tz/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-wbmicro-tz",
    .name = "World Bank Microdata Library — Tanzania datasets",
    .collector = "tz_statistics", .category = "statistics",
    .description = "Tanzania survey/census microdata studies with idno, doi, authoring_entity and access class; detail hop by idno returns full DDI metadata.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"tz\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://microdata.worldbank.org/index.php/api/catalog/search?ps=50&country=Tanzania",
    .array_path = "result.rows",
    .detail_url = "https://microdata.worldbank.org/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
