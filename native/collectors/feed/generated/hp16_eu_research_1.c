/* Deep-record eu_research sources (10), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "cordis-search-projects-query",
    .name = "CORDIS project search with keyword clause",
    .collector = "eu_research", .category = "research",
    .description = "1,488 EU-funded projects matching a term. Each result: reference/grant number, id, acronym, the programme array with code/id/rcn/title (H2020 pillar and topic), dates and teaser. The reference number is the key into cordis.europa.eu/project/id/{ref}.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"eu\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://cordis.europa.eu/api/search/results?q=contenttype%3D%27project%27%20AND%20%27hydrogen%27&format=json&p=1&num=10",
    .array_path = "payload.results",
    .detail_url = "https://cordis.europa.eu/project/id/{v}", .detail_key = "reference",
    .page_param = "p",   /* the URL pins this cursor at its first value */
    .interval = 86400, .free_tier = 1 },

  { .id = "cordis-search-results-query",
    .name = "CORDIS project RESULTS (outcomes) search",
    .collector = "eu_research", .category = "research",
    .description = "7,691 reported project outcomes, each carrying relatedProjectAcronym, relatedProjectReference and relatedProjectRcn plus the outcome teaser - the deliverables-behind-a-grant hop, and it links back to the parent project id.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"eu\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://cordis.europa.eu/api/search/results?q=contenttype%3D%27result%27%20AND%20%27hydrogen%27&format=json&p=1&num=5",
    .array_path = "payload.results",
    .detail_url = "https://cordis.europa.eu/project/id/{v}", .detail_key = "relatedProjectReference",
    .page_param = "p",   /* the URL pins this cursor at its first value */
    .interval = 86400, .free_tier = 1 },

  { .id = "eu-cordis-projects-search",
    .name = "CORDIS (EU) — research project search",
    .collector = "eu_research", .category = "research",
    .description = "58,915 EU-funded research projects. Rows give grant agreement reference/id, acronym, title, framework programme with code and rcn, start/end dates, coordinating country, objective teaser and last-update date. The tree had the cordis host but not this search API. Supports arbitrary field queries (e.g. AND /project/rcn=193159) so it doubles as a lookup.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"eu\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://cordis.europa.eu/api/search/results?q=contenttype%3D%27project%27&p=1&num=5&format=json",
    .array_path = "payload.results",
    .detail_url = "https://cordis.europa.eu/project/id/{v}?format=json", .detail_key = "id",
    .page_param = "p",   /* the URL pins this cursor at its first value */
    .interval = 86400, .free_tier = 1 },

  { .id = "openaire-datasources-search",
    .name = "OpenAIRE data sources search",
    .collector = "eu_research", .category = "research",
    .description = "Repositories and aggregators OpenAIRE harvests: official/English name, type scheme, OpenAIRE compatibility level, website, logo, original ids. Provenance registry - tells you which repository a record actually came from.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"eu\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openaire.eu/graph/v1/dataSources?search=Zenodo&pageSize=5",
    .array_path = "results",
    .detail_url = "https://api.openaire.eu/graph/v1/dataSources?search={v}&pageSize=50", .detail_key = "officialName",
    .interval = 86400, .free_tier = 1 },

  { .id = "openaire-organizations-search",
    .name = "OpenAIRE organizations search",
    .collector = "eu_research", .category = "research",
    .description = "118 org records for a name query: legal name, legal short name, website, country, OpenAIRE org id, pids and originalIds. The id used to attribute research products and projects to an institution.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"eu\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openaire.eu/graph/v1/organizations?search=Kyoto&pageSize=5",
    .array_path = "results",
    .detail_url = "https://api.openaire.eu/graph/v1/organizations?search={v}&pageSize=50", .detail_key = "legalName",
    .interval = 86400, .free_tier = 1 },

  { .id = "openaire-products-by-project",
    .name = "OpenAIRE research products BY project id",
    .collector = "eu_research", .category = "research",
    .description = "25 outputs produced under one funded project, full product records with authors and ORCIDs. The grant->deliverables hop, and the reason the project id above matters. Verified with a real project id pulled from the projects search.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"eu\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openaire.eu/graph/v1/researchProducts?relProjectId=corda__h2020::6c228bea4ee8a4ef22cd1d4fa079b63a&pageSize=5",
    .array_path = "results",
    .detail_url = "https://api.openaire.eu/graph/v1/researchProducts?relProjectId={v}&pageSize=100", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "openaire-project-by-id",
    .name = "OpenAIRE project by id (detail)",
    .collector = "eu_research", .category = "research",
    .description = "One project resolved from its OpenAIRE id: grant code, acronym, title, dates, call id, open-access mandates, subjects and funding stream. The stable id that the research-products-by-project hop consumes.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"eu\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openaire.eu/graph/v1/projects?id=corda__h2020::6c228bea4ee8a4ef22cd1d4fa079b63a",
    .array_path = "results",
    .detail_url = "https://api.openaire.eu/graph/v1/projects?id={v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "openaire-projects-search",
    .name = "OpenAIRE Graph funded projects search",
    .collector = "eu_research", .category = "research",
    .description = "5,202 funded projects: OpenAIRE project id, national/EU grant code, acronym, title, start/end dates, call identifier, keywords, and the fundings array (funder short name, jurisdiction, funding stream). Covers national funders (Czech TA, DFG, ANR...) as well as EU.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"eu\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openaire.eu/graph/v1/projects?search=hydrogen&pageSize=10",
    .array_path = "results",
    .detail_url = "https://api.openaire.eu/graph/v1/projects?id={v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "openaire-research-product-by-pid",
    .name = "OpenAIRE research product by DOI/PID (detail)",
    .collector = "eu_research", .category = "research",
    .description = "Resolves any persistent identifier to OpenAIRE's deduplicated record: full author list each with ORCID and its provenance (orcid vs orcid_pending), instances across every repository holding it, licence, and links to funding projects.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"eu\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openaire.eu/graph/v1/researchProducts?pid=10.7717/peerj.4375",
    .array_path = "results",
    .detail_url = "https://api.openaire.eu/graph/v1/researchProducts?pid={v}", .detail_key = "doi",
    .interval = 86400, .free_tier = 1 },

  { .id = "openaire-research-products",
    .name = "OpenAIRE Graph research products search",
    .collector = "eu_research", .category = "research",
    .description = "1.93M hits; each result is a merged research product with authors and their ORCIDs, type, language, descriptions, subjects with provenance, pids, publication venue, and the OpenAIRE id used for project/organisation joins.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"eu\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openaire.eu/graph/v1/researchProducts?search=climate&pageSize=10",
    .array_path = "results",
    .detail_url = "https://api.openaire.eu/graph/v1/researchProducts?pid={v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
