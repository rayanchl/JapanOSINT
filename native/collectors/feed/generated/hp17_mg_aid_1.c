/* Deep-record mg_aid sources (2), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "dportal-iati-mg",
    .name = "d-portal IATI activities - Madagascar",
    .collector = "mg_aid", .category = "aid",
    .description = "Aid activities in Madagascar: reporting organisation and ref, funder, title, description, commitment/spend and dates; transaction detail per activity.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "aid-record", .tags = "\"mg\",\"aid\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://d-portal.org/q?country_code=MG&limit=50&form=json",
    .array_path = "rows",
    .detail_url = "https://d-portal.org/q?from=act%2Ctrans&aid={v}&form=json&limit=100", .detail_key = "aid",
    .interval = 86400, .free_tier = 1 },

  { .id = "undp-projects-mg",
    .name = "UNDP Open Data projects - Madagascar",
    .collector = "mg_aid", .category = "aid",
    .description = "UNDP Madagascar projects with award id, title, outputs, geolocated activity sites and purchase orders.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "aid-record", .tags = "\"mg\",\"aid\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "fr",
    .url = "https://api.open.undp.org/api/units/MDG.json",
    .array_path = "projects",
    .detail_url = "https://api.open.undp.org/api/projects/{v}.json", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
