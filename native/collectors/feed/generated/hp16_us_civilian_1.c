/* Deep-record us_civilian sources (11), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "global-nominatim-structured-search",
    .name = "Nominatim structured address search",
    .collector = "us_civilian", .category = "civilian",
    .description = "Field-by-field address query (street/city/county/state/postalcode/country) returning place_id, osm_type+osm_id, lat/lon, category/type, addresstype, parsed address components with ISO3166-2, extratags (height, building:levels, architect, operator, old descriptions) and namedetails in every language.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://nominatim.openstreetmap.org/search?street=1600+Amphitheatre+Parkway&city=Mountain+View&country=USA&format=jsonv2&addressdetails=1&extratags=1&namedetails=1&limit=5",
    .detail_url = "https://nominatim.openstreetmap.org/details.php?osmtype=W&osmid={v}&format=json&addressdetails=1&hierarchy=1&linkedplaces=1&keywords=1", .detail_key = "osm_id",
    .interval = 21600, .free_tier = 1 },

  { .id = "global-osm-way-full",
    .name = "OpenStreetMap way with all member nodes",
    .collector = "us_civilian", .category = "civilian",
    .description = "A way plus every node it references, each with lat/lon, timestamp, version, changeset id, editing user name and uid. Turns one feature id into its full geometry and its complete edit provenance.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openstreetmap.org/api/0.6/way/23733659/full.json",
    .array_path = "elements",
    .detail_url = "https://api.openstreetmap.org/api/0.6/changeset/{v}.json?include_discussion=true", .detail_key = "changeset",
    .interval = 21600, .free_tier = 1 },

  { .id = "littlesis-entity-connections",
    .name = "LittleSis entity connections (neighbour entities, hydrated)",
    .collector = "us_civilian", .category = "civilian",
    .description = "The entities on the other end of every relationship, returned as FULL entity records (name, blurb, biography, types, aliases) rather than ids - so one request gives the whole first-degree network around a company or person already hydrated.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://littlesis.org/api/entities/1/connections",
    .array_path = "data",
    .detail_url = "https://littlesis.org/api/entities/{v}/connections", .detail_key = "id",
    .interval = 21600, .free_tier = 1 },

  { .id = "littlesis-entity-extensions",
    .name = "LittleSis entity type extensions",
    .collector = "us_civilian", .category = "civilian",
    .description = "The typed extension records attached to an entity (Org -> Business -> PublicCompany), each with definition_id and display name. Tells a parser which additional typed field sets exist for that entity.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://littlesis.org/api/entities/1/extensions",
    .array_path = "data",
    .detail_url = "https://littlesis.org/api/entities/{v}/extensions", .detail_key = "id",
    .interval = 21600, .free_tier = 1 },

  { .id = "littlesis-entity-lists",
    .name = "LittleSis lists containing an entity",
    .collector = "us_civilian", .category = "civilian",
    .description = "Every curated list an entity belongs to (Fortune 1000, donor lists, board networks) with list id, description, entity_count and ranking flag. Reverse membership lookup - tells you what cohort researchers have already placed this entity in.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://littlesis.org/api/entities/1/lists",
    .array_path = "data",
    .detail_url = "https://littlesis.org/api/entities/{v}/lists", .detail_key = "id",
    .interval = 21600, .free_tier = 1 },

  { .id = "us-austin-apd-crime-arcgis",
    .name = "Austin Police Dept reported crimes (city ArcGIS FeatureServer, non-Socrata)",
    .collector = "us_civilian", .category = "civilian",
    .description = "CRIME_INCIDENT_POINTS_ID, RIN (report incident number), OCCURRENCE_DATE, OCCURRENCE_TIME, OCCURRENCE_WEEK_DAY, ADDRESS_BLOCK, offence fields and point geometry. Full ArcGIS query grammar (where=, time filters, geometry envelope, returnIdsOnly, orderByFields) so it can be paged exhaustively. Detail hop verified with objectIds=4388.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://maps.austintexas.gov/arcgis/rest/services/CrimeViewer_new/APD_Reported_Crimes_new/FeatureServer/0/query?where=1%3D1&outFields=*&resultRecordCount=2&f=json",
    .array_path = "fields",
    .detail_url = "https://maps.austintexas.gov/arcgis/rest/services/CrimeViewer_new/APD_Reported_Crimes_new/FeatureServer/0/query?objectIds={v}&outFields=*&f=json", .detail_key = "OBJECTID",
    .interval = 21600, .free_tier = 1 },

  { .id = "us-brookline-311-open311-request-detail",
    .name = "Brookline MA 311 - single service request",
    .collector = "us_civilian", .category = "civilian",
    .description = "Single-case detail with full status_notes and geocoded address.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://spot.brooklinema.gov/open311/v2/requests/SR-138499.json",
    .detail_url = "https://spot.brooklinema.gov/open311/v2/requests/{v}.json", .detail_key = "service_request_id",
    .interval = 21600, .free_tier = 1 },

  { .id = "us-brookline-311-open311-requests",
    .name = "Brookline MA 311 (SPOT) - Open311 GeoReport v2 requests",
    .collector = "us_civilian", .category = "civilian",
    .description = "service_request_id (SR-nnnnnn), status, status_notes, service_name, service_code, requested_datetime, updated_datetime, address, lat/long, token. Municipal complaint stream for a Boston-area town, keyless.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://spot.brooklinema.gov/open311/v2/requests.json",
    .detail_url = "https://spot.brooklinema.gov/open311/v2/requests/{v}.json", .detail_key = "service_request_id",
    .interval = 21600, .free_tier = 1 },

  { .id = "us-sf-311-open311-request-detail",
    .name = "San Francisco 311 - single service request",
    .collector = "us_civilian", .category = "civilian",
    .description = "Full record for one case id including the complete free-text description, resolution status_notes, media_url and coordinates.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://mobile311.sfgov.org/open311/v2/requests/101004634285.json",
    .detail_url = "https://mobile311.sfgov.org/open311/v2/requests/{v}.json", .detail_key = "service_request_id",
    .interval = 21600, .free_tier = 1 },

  { .id = "us-sf-311-open311-requests",
    .name = "San Francisco 311 - Open311 GeoReport v2 requests",
    .collector = "us_civilian", .category = "civilian",
    .description = "Live 311 case feed: service_request_id, status, status_notes, service_name, service_code, description (free text, frequently contains licence plates, vehicle descriptions and named complaints), requested_datetime, updated_datetime, address, lat/long, media_url. Supports &service_code=, &start_date=, &end_date=, &status= filters. One of the only surviving Open311 GeoReport servers in the US.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://mobile311.sfgov.org/open311/v2/requests.json?page_size=5",
    .detail_url = "https://mobile311.sfgov.org/open311/v2/requests/{v}.json", .detail_key = "service_request_id",
    .interval = 21600, .free_tier = 1 },

  { .id = "us-sf-311-open311-services",
    .name = "San Francisco 311 - service catalogue",
    .collector = "us_civilian", .category = "civilian",
    .description = "service_code, service_name, metadata flag, type, group for each request category (MTA:Parking Enforcement, RPD:General, PW:BSM:Damage Property...). The service_code values are the filter keys for the requests feed.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "civilian-record", .tags = "\"us\",\"civilian\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://mobile311.sfgov.org/open311/v2/services.json",
    .detail_url = "https://mobile311.sfgov.org/open311/v2/requests.json?service_code={v}", .detail_key = "service_code",
    .interval = 21600, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
