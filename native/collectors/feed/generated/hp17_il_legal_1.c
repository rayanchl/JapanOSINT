/* Deep-record il_legal sources (2), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "il-knesset-israel-law",
    .name = "Knesset — Israeli laws (KNS_IsraelLaw)",
    .collector = "il_legal", .category = "legal",
    .description = "Consolidated statute register: law name, IsBasicLaw, IsBudgetLaw, first and latest publication dates, validity status and validity start/finish dates with notes.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "legal-record", .tags = "\"il\",\"legal\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "he",
    .url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_IsraelLaw?$top=100&$format=json",
    .array_path = "value",
    .detail_url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_IsraelLawName?$filter=IsraelLawID%20eq%20{v}&$format=json", .detail_key = "IsraelLawID",
    .interval = 43200, .free_tier = 1 },

  { .id = "il-knesset-law",
    .name = "Knesset — enacted laws and amendments (KNS_Law)",
    .collector = "il_legal", .category = "legal",
    .description = "Each enactment/amendment as published: name, type and subtype (incl. Mandate-era British statutes), Knesset number, publication date, publication series, gazette number and page number.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "legal-record", .tags = "\"il\",\"legal\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "he",
    .url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_Law?$top=100&$format=json",
    .array_path = "value",
    .detail_url = "https://knesset.gov.il/Odata/ParliamentInfo.svc/KNS_Law({v})?$format=json", .detail_key = "LawID",
    .interval = 43200, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
