/* Deep-record eu_corporate sources (1), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "esma-solr-upreg",
    .name = "ESMA registers — raw Solr, upreg core (101,959 docs)",
    .collector = "eu_corporate", .category = "corporate",
    .description = "The raw Solr index behind the ESMA register UI, far deeper than the searchRegister HTML endpoint already in the tree. Parent docs are entities (ae_entityName, ae_entityTypeCode MIF/CI/…, ae_lei, ae_headOfficeLei, ae_officeType Branch/Head office, ae_competentAuthority, ae_lastUpdate); child docs (type_s:child, _root_) are the individual authorised activities/services (ac_serviceName, ac_authorisationNotificationDateStr, ac_status). Supports full Solr syntax, e.g. q=ae_entityName:*Deutsche* → 46 hits.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"eu\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://registers.esma.europa.eu/solr/esma_registers_upreg/select?q=*:*&wt=json&rows=3",
    .array_path = "response.docs",
    .detail_url = "https://registers.esma.europa.eu/publication/details?core=esma_registers_upreg&docId={v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
