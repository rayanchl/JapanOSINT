/* Deep-record jp_research sources (10), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "global-ooni-measurements-jp",
    .name = "OONI measurements list (country filtered)",
    .collector = "jp_research", .category = "research",
    .description = "Measurement rows with anomaly/confirmed/failure flags, input URL, probe_asn, probe_cc, report_id, blocking scores (general/global/country/isp/local) and, critically, measurement_url — the raw_measurement link used as the detail hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"jp\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.ooni.io/api/v1/measurements?limit=3&probe_cc=JP",
    .array_path = "results",
    .detail_url = "https://api.ooni.io/api/v1/raw_measurement?measurement_uid={v}", .detail_key = "measurement_uid",
    .interval = 86400, .free_tier = 1 },

  { .id = "jp-ihr-networks",
    .name = "IIJ Internet Health Report network lookup",
    .collector = "jp_research", .category = "research",
    .description = "Resolves an AS number to IHR's canonical network name plus flags for which IHR datasets exist for it (hegemony, delay_forwarding, disco). The index hop before the heavier IHR calls.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"jp\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://ihr.iijlab.net/ihr/api/networks/?number=2497",
    .array_path = "results",
    .detail_url = "https://ihr.iijlab.net/ihr/api/hegemony/prefixes/?originasn={v}&af=4", .detail_key = "number",
    .interval = 86400, .free_tier = 1 },

  { .id = "jp-ndl-authority-person",
    .name = "Web NDL Authorities - person/corporate name authority record (JSON-LD)",
    .collector = "jp_research", .category = "research",
    .description = "Authority record for a named person or organisation: uri, primaryTopic with foaf:Person/foaf:Organization type, prefLabel literalForm plus transcription (kana reading), altLabel variant names and pseudonyms, birth/death dates, related authorities (owl:sameAs to VIAF/LCNAME), source citations and created/modified timestamps. The entity-resolution layer for Japanese personal and corporate names in library data.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"jp\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "ja",
    .url = "https://id.ndl.go.jp/auth/ndlna/00270232.json",
    .array_path = "altlabel",
    .detail_url = "https://id.ndl.go.jp/auth/ndlna/{v}.json", .detail_key = "ndlna_id",
    .interval = 86400, .free_tier = 1 },

  { .id = "jp-ndl-authority-subject",
    .name = "Web NDL Authorities - subject heading (NDLSH) record",
    .collector = "jp_research", .category = "research",
    .description = "NDL subject heading as SKOS Concept: uri, prefLabel literalForm with kana transcription, altLabel synonyms, broader/narrower/related concept links, created (back to 1980) and modified dates. Lets you expand or normalise a topic before running an NDL Search query.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"jp\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "ja",
    .url = "https://id.ndl.go.jp/auth/ndlsh/00560222.json",
    .array_path = "narrower",
    .detail_url = "https://id.ndl.go.jp/auth/ndlsh/{v}.json", .detail_key = "ndlsh_id",
    .interval = 86400, .free_tier = 1 },

  { .id = "jp-ndl-dl-iiif-manifest",
    .name = "NDL Digital Collections - IIIF Presentation manifest",
    .collector = "jp_research", .category = "research",
    .description = "IIIF 2.x manifest for a digitised NDL item: label, metadata array (Persistent ID info:ndljp/pid/NNNN, Title, Creator, Publisher, Date, call number, rights), attribution, license, and the sequence of canvases with per-page image service URLs. Turns any NDL pid seen in a search result into page-level scanned imagery.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"jp\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "ja",
    .url = "https://dl.ndl.go.jp/api/iiif/1032509/manifest.json",
    .array_path = "structures",
    .detail_url = "https://dl.ndl.go.jp/api/iiif/{v}/manifest.json", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "jp-ndl-lab-dl-book-search",
    .name = "NDL Lab Next Digital Library - full-text book search",
    .collector = "jp_research", .category = "research",
    .description = "Full-text OCR search across NDL Digital Collections public-domain books. 5,426 hits for one keyword; each row carries id (NDL pid), version, title, volume, responsibility statement, publisher, published/publishyear, NDC class, bibId, callNo, leftopen flag, autoTOCFlag and an index array of OCR'd table-of-contents entries with .jp2 page anchors. Also returns an isClassic facet.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"jp\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "ja",
    .url = "https://lab.ndl.go.jp/dl/api/book/search?keyword=%E9%98%B2%E7%81%BD",
    .array_path = "list",
    .detail_url = "https://lab.ndl.go.jp/dl/api/book/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "openalex-authors-by-institution",
    .name = "OpenAlex authors at an institution",
    .collector = "jp_research", .category = "research",
    .description = "Researchers whose last known affiliation is a given institution (meta.count=45,762 at Kyoto University), each with ORCID, name variants, h-index and full affiliation history. The institution->people roster hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"jp\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openalex.org/authors?filter=last_known_institutions.id:I22299242&per_page=10",
    .array_path = "results",
    .detail_url = "https://api.openalex.org/authors/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "openalex-funders-country-jp",
    .name = "OpenAlex funders by country (JP)",
    .collector = "jp_research", .category = "research",
    .description = "1,140 Japanese funding bodies (JSPS/KAKENHI first), each with alternate names in Japanese and English, awards_count, works_count and cross-ids. Entry point for tracing Japanese public research money.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"jp\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openalex.org/funders?filter=country_code:jp&per_page=25",
    .array_path = "results",
    .detail_url = "https://api.openalex.org/funders/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "openalex-institutions-lineage",
    .name = "OpenAlex institutions under a parent (lineage)",
    .collector = "jp_research", .category = "research",
    .description = "All 19 institutions whose lineage contains Kyoto University - the sub-institutes, hospitals and centres rolled up under one parent org, each a full institution record with ROR and geo.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"jp\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.openalex.org/institutions?filter=lineage:I22299242&per_page=25",
    .array_path = "results",
    .detail_url = "https://api.openalex.org/institutions/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "ror-filter-types-country",
    .name = "ROR organisations by type + country",
    .collector = "jp_research", .category = "research",
    .description = "1,546 Japanese education-type organisations, full records with fundref/GRID/ISNI/Wikidata cross-ids and geonames. Enumerates a country's higher-education sector as a joinable id list.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"jp\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.ror.org/organizations?filter=types:Education,country.country_code:JP",
    .array_path = "items",
    .detail_url = "https://api.ror.org/v2/organizations/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
