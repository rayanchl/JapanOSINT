/* Deep-record ng_statistics sources (3), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "af-dhs-data-ng",
    .name = "DHS Program — Nigeria indicator data",
    .collector = "ng_statistics", .category = "statistics",
    .description = "Every published DHS/MIS/SPA estimate for Nigeria, broken down by survey and by characteristic. Row: DataId, Indicator (the label), Value, Precision, DHS_CountryCode, CountryName, SurveyYear, SurveyId, IndicatorId, IndicatorType, CharacteristicCategory, CharacteristicLabel, ByVariableLabel, RegionId. Detail by IndicatorId returns the indicator definition, denominator, measurement type and level hierarchy.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"ng\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.dhsprogram.com/rest/dhs/data?countryIds=NG&f=json&perpage=100",
    .array_path = "data",
    .detail_url = "https://api.dhsprogram.com/rest/dhs/indicators/{v}?f=json", .detail_key = "IndicatorId",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-ihsn-ng",
    .name = "IHSN Survey Catalog — Nigeria",
    .collector = "ng_statistics", .category = "statistics",
    .description = "International Household Survey Network index of Nigerian surveys, including holdings not in the national archive. Row: idno, title, nation, authoring_entity, year_start/end, form_model, varcount, views, downloads, rank, type, url. Detail by idno returns full DDI with authoring entities, producers and study topics.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"ng\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://catalog.ihsn.org/index.php/api/catalog/search?ps=50&country=Nigeria",
    .array_path = "result.rows",
    .detail_url = "https://catalog.ihsn.org/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-wbmicro-ng",
    .name = "World Bank Microdata Library — Nigeria datasets",
    .collector = "ng_statistics", .category = "statistics",
    .description = "Nigeria survey/census microdata studies. Row: id, idno, doi, type, title, nation, authoring_entity, form_model, year_start/end, repositoryid, varcount, views, downloads, url. Detail by idno yields full DDI: producers, contact persons with email, nation list, sampling and access policy.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"ng\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://microdata.worldbank.org/index.php/api/catalog/search?ps=50&country=Nigeria",
    .array_path = "result.rows",
    .detail_url = "https://microdata.worldbank.org/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
