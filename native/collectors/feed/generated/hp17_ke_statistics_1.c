/* Deep-record ke_statistics sources (4), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "af-dhs-data-ke",
    .name = "DHS Program — Kenya indicator data",
    .collector = "ke_statistics", .category = "statistics",
    .description = "Kenya DHS estimates by survey and characteristic: DataId, Indicator, Value, SurveyId, SurveyYear, IndicatorId, CharacteristicCategory/Label, ByVariableLabel. Detail by IndicatorId returns the indicator definition and denominator.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"ke\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.dhsprogram.com/rest/dhs/data?countryIds=KE&f=json&perpage=100",
    .array_path = "data",
    .detail_url = "https://api.dhsprogram.com/rest/dhs/indicators/{v}?f=json", .detail_key = "IndicatorId",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-ihsn-ke",
    .name = "IHSN Survey Catalog — Kenya",
    .collector = "ke_statistics", .category = "statistics",
    .description = "IHSN index of Kenyan surveys with idno, authoring_entity, year range and access class; detail hop by idno returns full DDI including topics and producers.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"ke\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://catalog.ihsn.org/index.php/api/catalog/search?ps=50&country=Kenya",
    .array_path = "result.rows",
    .detail_url = "https://catalog.ihsn.org/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-nada-ke-knbs-microdata",
    .name = "Kenya KNBS — National Data Archive (microdata catalogue)",
    .collector = "ke_statistics", .category = "statistics",
    .description = "129 KNBS surveys (KDHS 2022, FinAccess, Time Use, census products). Row: id, idno (DDI-KEN-KNBS-…), title, nation, authoring_entity, form_model (access class), year_start/end, total_views, total_downloads, created/changed, url. Detail by idno returns full DDI metadata including producers, version statement and authoring entities with affiliation.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"ke\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://statistics.knbs.or.ke/nada/index.php/api/catalog/search?ps=100",
    .array_path = "result.rows",
    .detail_url = "https://statistics.knbs.or.ke/nada/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

  { .id = "af-wbmicro-ke",
    .name = "World Bank Microdata Library — Kenya datasets",
    .collector = "ke_statistics", .category = "statistics",
    .description = "Kenya survey/census microdata studies with idno, doi, authoring_entity, access class and year range; detail hop by idno returns the full DDI including producer and contact records.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "statistics-record", .tags = "\"ke\",\"statistics\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://microdata.worldbank.org/index.php/api/catalog/search?ps=50&country=Kenya",
    .array_path = "result.rows",
    .detail_url = "https://microdata.worldbank.org/index.php/api/catalog/{v}", .detail_key = "idno",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
