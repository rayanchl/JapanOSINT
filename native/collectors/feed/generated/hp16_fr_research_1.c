/* Deep-record fr_research sources (3), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "theses-fr-in-progress",
    .name = "theses.fr theses filtered by status",
    .collector = "fr_research", .category = "research",
    .description = "563,354 records reachable with the filtres= syntax (status, institution, year). Demonstrates the filter grammar needed to sweep the corpus rather than keyword-search it.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"fr\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "fr",
    .url = "https://theses.fr/api/v1/theses/recherche/?q=*&nombre=10&filtres=%5Bstatus%3DenCours%5D",
    .array_path = "theses",
    .detail_url = "https://theses.fr/api/v1/theses/these/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "theses-fr-personne-search",
    .name = "theses.fr person search (FR academics)",
    .collector = "fr_research", .category = "research",
    .description = "568 people with IdRef PPN, has_idref flag, a roles map counting how often they served as author/supervisor/examiner/reporter, and the NNT list of every thesis they touched. Academic-influence network for France.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"fr\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "fr",
    .url = "https://theses.fr/api/v1/personnes/recherche/?q=dupont&nombre=10",
    .array_path = "personnes",
    .detail_url = "https://theses.fr/api/v1/personnes/personne/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "theses-fr-search",
    .name = "theses.fr doctoral thesis search (FR)",
    .collector = "fr_research", .category = "research",
    .description = "1,057 French PhD theses: NNT id, title in FR and EN, awarding institution with its PPN, defence date, first-enrolment date, and the people - auteurs, directeurs, rapporteurs, jury - each with their IdRef PPN. Person-level academic-lineage data not present anywhere in the tree.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "research-record", .tags = "\"fr\",\"research\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "fr",
    .url = "https://theses.fr/api/v1/theses/recherche/?q=graphene&nombre=10",
    .array_path = "theses",
    .detail_url = "https://theses.fr/api/v1/theses/these/{v}", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
