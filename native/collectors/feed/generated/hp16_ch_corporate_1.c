/* Deep-record ch_corporate sources (9), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "gleif-lei-direct-child-relationships",
    .name = "GLEIF direct-child RELATIONSHIP records",
    .collector = "ch_corporate", .category = "corporate",
    .description = "All 330 direct-subsidiary relationship edges for a parent LEI as relationship objects (start/end node, type, status, validity and accounting periods) rather than plain child records - the dated corporate-structure graph beneath a group.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"ch\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.gleif.org/api/v1/lei-records/7LTWFZYICNSX8D621K86/direct-child-relationships?page[size]=15",
    .array_path = "data",
    .detail_url = "https://api.gleif.org/api/v1/lei-records/{v}/direct-child-relationships?page[size]=50", .detail_key = "lei",
    .interval = 86400, .free_tier = 1 },

  { .id = "gleif-lei-field-modifications",
    .name = "GLEIF LEI field modification history",
    .collector = "ch_corporate", .category = "corporate",
    .description = "164 dated change events for one LEI: which field changed, from what value to what value, when. Detects quiet renames, address moves, status flips and legal-form changes - the audit trail behind a company record.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"ch\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.gleif.org/api/v1/lei-records/7LTWFZYICNSX8D621K86/field-modifications?page[size]=20",
    .array_path = "data",
    .detail_url = "https://api.gleif.org/api/v1/lei-records/{v}/field-modifications?page[size]=100", .detail_key = "lei",
    .interval = 86400, .free_tier = 1 },

  { .id = "gleif-lei-filter-entity-names",
    .name = "GLEIF search across ALL entity names (incl. former/other)",
    .collector = "ch_corporate", .category = "corporate",
    .description = "Searches legal name plus otherNames and transliteratedOtherNames (291 hits) - catches entities that have been renamed or that trade under a different local-script name.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"ch\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.gleif.org/api/v1/lei-records?filter[entity.names]=Toyota&page[size]=10",
    .array_path = "data",
    .detail_url = "https://api.gleif.org/api/v1/lei-records/{v}", .detail_key = "lei",
    .interval = 86400, .free_tier = 1 },

  { .id = "gleif-lei-filter-fulltext",
    .name = "GLEIF full-text LEI search",
    .collector = "ch_corporate", .category = "corporate",
    .description = "Full-text search across the whole LEI record (names, addresses, other names) - 408 hits for Toyota, each a complete record. Broader recall than the exact legalName filter the tree already uses.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"ch\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.gleif.org/api/v1/lei-records?filter[fulltext]=Toyota&page[size]=10",
    .array_path = "data",
    .detail_url = "https://api.gleif.org/api/v1/lei-records/{v}", .detail_key = "lei",
    .interval = 86400, .free_tier = 1 },

  { .id = "gleif-lei-filter-isin",
    .name = "GLEIF LEI lookup by ISIN (security -> issuer)",
    .collector = "ch_corporate", .category = "corporate",
    .description = "Resolves a traded security's ISIN to the issuing legal entity's full LEI record (US0378331005 -> Apple Inc., with former names, addresses, legal form and registration status). Security->issuer->corporate-group entry point.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"ch\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.gleif.org/api/v1/lei-records?filter[isin]=US0378331005",
    .array_path = "data",
    .detail_url = "https://api.gleif.org/api/v1/lei-records/{v}/direct-children", .detail_key = "lei",
    .interval = 86400, .free_tier = 1 },

  { .id = "gleif-lei-filter-lapsed",
    .name = "GLEIF LEIs with LAPSED registration",
    .collector = "ch_corporate", .category = "corporate",
    .description = "1,191,703 entities that stopped renewing their LEI - full records. A lapsed LEI on an entity still doing regulated business is a due-diligence signal, and the set is a ready-made watchlist.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"ch\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.gleif.org/api/v1/lei-records?filter[registration.status]=LAPSED&page[size]=10",
    .array_path = "data",
    .detail_url = "https://api.gleif.org/api/v1/lei-records/{v}", .detail_key = "lei",
    .interval = 86400, .free_tier = 1 },

  { .id = "gleif-lei-filter-nonconforming",
    .name = "GLEIF non-conforming LEI records",
    .collector = "ch_corporate", .category = "corporate",
    .description = "1,300,990 LEI records flagged NON_CONFORMING by GLEIF's data-quality checks (missing parent reporting, stale renewals). Full records; a systematic list of entities whose corporate-structure disclosure is incomplete.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"ch\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.gleif.org/api/v1/lei-records?filter[conformityFlag]=NON_CONFORMING&page[size]=10",
    .array_path = "data",
    .detail_url = "https://api.gleif.org/api/v1/lei-records/{v}/direct-parent-relationship", .detail_key = "lei",
    .interval = 86400, .free_tier = 1 },

  { .id = "gleif-lei-filter-owns",
    .name = "GLEIF reverse ownership lookup (filter[owns])",
    .collector = "ch_corporate", .category = "corporate",
    .description = "Given a LEI, returns the LEI records of entities that OWN it (2 owners for the probe) as full records. Reverse of the parent hop and the cheapest way to walk upward through an opaque holding chain.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"ch\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.gleif.org/api/v1/lei-records?filter[owns]=5493001KJTIIGC8Y1R12&page[size]=10",
    .array_path = "data",
    .detail_url = "https://api.gleif.org/api/v1/lei-records/{v}", .detail_key = "lei",
    .interval = 86400, .free_tier = 1 },

  { .id = "gleif-lei-ultimate-children",
    .name = "GLEIF ultimate children of a LEI",
    .collector = "ch_corporate", .category = "corporate",
    .description = "416 entities that name this LEI as their ULTIMATE consolidating parent (vs 330 direct) - full LEI records with legal name, addresses, legal form and registration status. The whole group, not just tier one.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "corporate-record", .tags = "\"ch\",\"corporate\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.gleif.org/api/v1/lei-records/7LTWFZYICNSX8D621K86/ultimate-children?page[size]=15",
    .array_path = "data",
    .detail_url = "https://api.gleif.org/api/v1/lei-records/{v}/ultimate-children?page[size]=50", .detail_key = "lei",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
