/* Deep-record us_transport sources (7), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "global-gbfs-systems-catalog",
    .name = "MobilityData GBFS systems catalogue",
    .collector = "us_transport", .category = "transport",
    .description = "The canonical registry of every public bike/scooter share system worldwide: Country Code, Name, Location, System ID, operator URL, Auto-Discovery URL (the gbfs.json to crawl), supported GBFS versions and authentication requirements. 38 KB — this is the seed list that makes exhaustive GBFS collection possible, and it is keyless unlike api.mobilitydatabase.org.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"us\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .mode = HP_CSV,
    .url = "https://raw.githubusercontent.com/MobilityData/gbfs/master/systems.csv",
    .detail_url = "{v}", .detail_key = "Auto-Discovery URL",
    .interval = 3600, .free_tier = 1 },

  { .id = "uk-govuk-search-maib-reports",
    .name = "GOV.UK Search API - Marine Accident Investigation Branch reports",
    .collector = "us_transport", .category = "transport",
    .description = "MAIB reports naming the vessel and its registration (e.g. fishing vessel Silver Cloud II WK 80), date_of_occurrence and vessel type. Content API detail hop returns the full investigation report with owner/operator, causal analysis and recommendations. NOTE: country should read GB.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"us\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.gov.uk/api/search.json?filter_format=maib_report&count=3&fields=title,link,description,date_of_occurrence,vessel_type",
    .array_path = "results",
    .detail_url = "https://www.gov.uk/api/content{v}", .detail_key = "link",
    .interval = 3600, .free_tier = 1 },

  { .id = "uk-govuk-search-raib-reports",
    .name = "GOV.UK Search API - Rail Accident Investigation Branch reports",
    .collector = "us_transport", .category = "transport",
    .description = "RAIB investigation reports: report number, incident description, date_of_occurrence, railway type. Content API detail hop returns the full report page with the PDF, findings, causal factors and safety recommendations addressed to named operators and infrastructure managers. NOTE: country should read GB.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"us\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.gov.uk/api/search.json?filter_format=raib_report&count=3&fields=title,link,description,date_of_occurrence,railway_type",
    .array_path = "results",
    .detail_url = "https://www.gov.uk/api/content{v}", .detail_key = "link",
    .interval = 3600, .free_tier = 1 },

  { .id = "us-fmcsa-carrier-census",
    .name = "FMCSA — motor carrier census (SAFER replacement)",
    .collector = "us_transport", .category = "transport",
    .description = "Keyless substitute for the 403-blocked QCMobile/SAFER APIs. Per carrier: dot_number, status_code, add_date, mcs150_date, mcs150_mileage and year, dun_bradstreet_no, carrier_operation (interstate/intrastate), business_org_id, phy_omc_region, safety_inv_terr, legal and DBA name, physical and mailing address, phone, power units, driver count, hazmat flags, cargo-carried flags, and the safety rating with its rating date. Verified server-side filter ?dot_number=162845.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"us\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://data.transportation.gov/resource/az4n-8mr2.json?$limit=2",
    .detail_url = "https://data.transportation.gov/resource/az4n-8mr2.json?$limit=50&dot_number={v}", .detail_key = "dot_number",
    .interval = 3600, .free_tier = 1 },

  { .id = "us-fmcsa-crash-file",
    .name = "FMCSA — motor carrier crash file",
    .collector = "us_transport", .category = "transport",
    .description = "CARRIER → ITS CRASHES, the exact detail hop required. Per crash: crash_id, dot_number, report_state, report_number, report_date and time, report_seq_no, ci_status_code, final_status_date, location, state, county, fatalities, injuries, tow_away flag, vehicle configuration, hazmat released flag, weather and light conditions, road access control, trafficway description, and citation issued. Verified filter ?dot_number=728630.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"us\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://data.transportation.gov/resource/aayw-vxb3.json?$limit=2",
    .detail_url = "https://data.transportation.gov/resource/aayw-vxb3.json?$limit=500&dot_number={v}", .detail_key = "dot_number",
    .interval = 3600, .free_tier = 1 },

  { .id = "us-fmcsa-vehicle-inspection-file",
    .name = "FMCSA — roadside vehicle inspection file",
    .collector = "us_transport", .category = "transport",
    .description = "CARRIER → ITS ROADSIDE INSPECTIONS. Per inspection: inspection_id, dot_number, report_state, report_number, insp_date, insp_start_time/insp_end_time, region, ci_status_code, location_desc, inspection level, driver and vehicle out-of-service counts and OOS totals, hazmat inspection flag, unsafe-driving and fatigued-driving violation counts, and the vehicle/licence-plate identifiers. Verified filter ?dot_number=728630.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"us\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://data.transportation.gov/resource/fx4q-ay7w.json?$limit=2",
    .detail_url = "https://data.transportation.gov/resource/fx4q-ay7w.json?$limit=500&dot_number={v}", .detail_key = "dot_number",
    .interval = 3600, .free_tier = 1 },

  { .id = "us-nhtsa-vpic-manufacturers",
    .name = "NHTSA vPIC — all registered vehicle manufacturers",
    .collector = "us_transport", .category = "transport",
    .description = "100 manufacturers per page. Per row: Mfr_ID, Mfr_Name, Mfr_CommonName, Country, VehicleTypes[] with IsPrimary flag. The enumeration list whose Mfr_Name feeds the manufacturer-detail hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "transport-record", .tags = "\"us\",\"transport\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://vpic.nhtsa.dot.gov/api/vehicles/GetAllManufacturers?format=json&page=1",
    .array_path = "results",
    .detail_url = "https://vpic.nhtsa.dot.gov/api/vehicles/GetManufacturerDetails/{v}?format=json", .detail_key = "Mfr_Name",
    .page_param = "page",   /* the URL pins this cursor at its first value */
    .interval = 3600, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
