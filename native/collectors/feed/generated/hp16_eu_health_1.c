/* Deep-record eu_health sources (5), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "europepmc-citations",
    .name = "Europe PMC citations of an article",
    .collector = "eu_health", .category = "health",
    .description = "246 citing records for one MED id, each with id, source (MED/PPR/PMC), citationType, title, authorString, journal, year, page and citedByCount - i.e. the citing works already hydrated, including preprints. Forward-citation hop the tree lacked.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"eu\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.ebi.ac.uk/europepmc/webservices/rest/MED/29456894/citations?format=json&pageSize=25",
    .array_path = "citationlist.citation",
    .detail_url = "https://www.ebi.ac.uk/europepmc/webservices/rest/MED/{v}/citations?format=json&pageSize=100", .detail_key = "pmid",
    .interval = 43200, .free_tier = 1 },

  { .id = "europepmc-core-record",
    .name = "Europe PMC core record (full metadata)",
    .collector = "eu_health", .category = "health",
    .description = "resultType=core returns the deep record: abstract, full author list with first/last names and affiliations, grants list (agency, grant id, acronym), MeSH terms, chemicals, full text ids, licence, and comment/correction links. Much richer than the lite search rows already in the tree.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"eu\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.ebi.ac.uk/europepmc/webservices/rest/search?query=EXT_ID:29456894&resultType=core&format=json",
    .array_path = "resultlist.result",
    .detail_url = "https://www.ebi.ac.uk/europepmc/webservices/rest/search?query=EXT_ID:{v}&resultType=core&format=json", .detail_key = "pmid",
    .interval = 43200, .free_tier = 1 },

  { .id = "europepmc-references",
    .name = "Europe PMC reference list of an article",
    .collector = "eu_health", .category = "health",
    .description = "The parsed and matched reference list: each reference with its own id/source, title, authors, journal, year, volume, pages, citedOrder and a match flag showing whether Europe PMC resolved it to a real record.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"eu\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.ebi.ac.uk/europepmc/webservices/rest/MED/29456894/references?format=json&pageSize=25",
    .array_path = "referencelist.reference",
    .detail_url = "https://www.ebi.ac.uk/europepmc/webservices/rest/MED/{v}/references?format=json&pageSize=100", .detail_key = "pmid",
    .interval = 43200, .free_tier = 1 },

  { .id = "europepmc-search-by-grant-agency",
    .name = "Europe PMC works by funding agency",
    .collector = "eu_health", .category = "health",
    .description = "215,412 papers acknowledging one named funder, cursor-pageable via nextCursorMark. Funder->literature sweep independent of Crossref's deposited funder metadata.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"eu\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.ebi.ac.uk/europepmc/webservices/rest/search?query=GRANT_AGENCY:%22Wellcome%20Trust%22&format=json&pageSize=10",
    .array_path = "resultlist.result",
    .detail_url = "https://www.ebi.ac.uk/europepmc/webservices/rest/search?query=EXT_ID:{v}&resultType=core&format=json", .detail_key = "pmid",
    .interval = 43200, .free_tier = 1 },

  { .id = "europepmc-search-by-grant-id",
    .name = "Europe PMC works by grant ID",
    .collector = "eu_health", .category = "health",
    .description = "108 publications acknowledging one specific grant reference. Grant->outputs hop: takes a grant number from UKRI/GtR, ORCID fundings or a CV and returns the papers it produced.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "health-record", .tags = "\"eu\",\"health\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://www.ebi.ac.uk/europepmc/webservices/rest/search?query=%28GRANT_ID%3A%22MR%2FN013468%2F1%22%29&format=json",
    .array_path = "resultlist.result",
    .detail_url = "https://www.ebi.ac.uk/europepmc/webservices/rest/search?query=%28GRANT_ID%3A%22{v}%22%29&resultType=core&format=json", .detail_key = "grantId",
    .interval = 43200, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
