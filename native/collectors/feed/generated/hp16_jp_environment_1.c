/* Deep-record jp_environment sources (2), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "jp-safecast-bgeigie-imports",
    .name = "Safecast bGeigie import batches",
    .collector = "jp_environment", .category = "environment",
    .description = "Each uploaded survey drive: import id, user_id, source.url (the raw .LOG file on S3), md5sum, status and status_details, measurements_count, lines_count, approved flag, created/updated, name, description, credits, height, orientation and the cities covered — often in Japanese. Not present in the tree.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "environment-record", .tags = "\"jp\",\"environment\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.safecast.org/bgeigie_imports.json?limit=3",
    .detail_url = "https://api.safecast.org/bgeigie_imports/{v}.json", .detail_key = "id",
    .interval = 10800, .free_tier = 1 },

  { .id = "jp-safecast-measurements",
    .name = "Safecast radiation measurements",
    .collector = "jp_environment", .category = "environment",
    .description = "Volunteer radiation readings: id, user_id, value, unit (cpm), device_id, devicetype_id, sensor_id, station_id, channel_id, captured_at, height, latitude, longitude, and measurement_import_id linking back to the upload batch.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "environment-record", .tags = "\"jp\",\"environment\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.safecast.org/measurements.json?limit=3",
    .detail_url = "https://api.safecast.org/measurements/{v}.json", .detail_key = "id",
    .interval = 10800, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
