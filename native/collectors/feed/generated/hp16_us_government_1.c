/* Deep-record us_government sources (12), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "us-ma-ocpf-filer",
    .name = "Massachusetts OCPF - filer (candidate/committee) profile",
    .collector = "us_government", .category = "government",
    .description = "cpfId, candidate first/last name, committeeName, accountType (candidate/PAC/ballot-question/party flags), officeSought, district, treasurer, depository bank id and name, address, isActive/isClosed, treasurerTrainingCompletionDate. Entry point for a person -> committee -> reports -> donors chain.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.ocpf.us/filer/15021",
    .array_path = "tags",
    .detail_url = "https://api.ocpf.us/filer/{v}", .detail_key = "cpfId",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-ma-ocpf-iepacs",
    .name = "Massachusetts OCPF - independent-expenditure PAC reports",
    .collector = "us_government", .category = "government",
    .description = "Per IE-PAC filing: reportId, cpfId, committeeName, treasurer first/last name, street address, city/state, reporting period, filing date, amendment lineage. The dark-money layer of MA politics.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.ocpf.us/miscreports/iepacs/reports/2026",
    .detail_url = "https://api.ocpf.us/report/{v}", .detail_key = "reportId",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-ma-ocpf-municipalities",
    .name = "Massachusetts OCPF - all 351 municipalities with their elected filers",
    .collector = "us_government", .category = "government",
    .description = "Per town: code, city, county, pop2020, localReportsUrl, and electedFilers[] with cpfId, filerName, candidateFullAddress (home address), districtNameHeld, officeNameHeld, partyAffiliation. A complete officeholder roster for the state keyed to campaign-finance ids.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.ocpf.us/municipalities",
    .detail_url = "https://api.ocpf.us/filer/{v}", .detail_key = "cpfId",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-ma-ocpf-report-detail",
    .name = "Massachusetts OCPF - itemised report (donors and spending)",
    .collector = "us_government", .category = "government",
    .description = "Full contents of one filing: receipts[] with each contributor's name, address, occupation, employer, principalOfficer, amount, date, tenderTypeDescription (Credit Card/Check), relatedCpfId; plus merchantProviderFees, refundedContributions, expenditures, bankName, officeDistrictSought and reporting totals. This is the deepest hop in MA campaign finance.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.ocpf.us/report/1038377",
    .array_path = "receipts",
    .detail_url = "https://api.ocpf.us/report/{v}", .detail_key = "reportId",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-ma-ocpf-report-list",
    .name = "Massachusetts OCPF - all reports for one filer",
    .collector = "us_government", .category = "government",
    .description = "Every report a committee has filed of a given base type, each already containing its receipts/expenditure arrays, bankName, reportingPeriod, depositSequence and totals. Combined with /reports/baseReportTypes/{cpfId} this enumerates a filer's complete filing history.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.ocpf.us/reports/reportList/15021?baseReportTypeId=2",
    .array_path = "items",
    .detail_url = "https://api.ocpf.us/reports/reportList/{v}?baseReportTypeId=2", .detail_key = "cpfId",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-miamidade-arcgis-services",
    .name = "Miami-Dade County FL ArcGIS service directory",
    .collector = "us_government", .category = "government",
    .description = "21 folders including 311, LandManagement (zoning/platting records), EnerGov (permits), MDFRInspections (fire inspections), MDPD, RER (regulatory & economic resources), CAD911, BusinessTracker. Verified drill-down into /LandManagement.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://gisweb.miamidade.gov/arcgis/rest/services?f=json",
    .array_path = "services",
    .detail_url = "https://gisweb.miamidade.gov/arcgis/rest/services/{v}?f=json", .detail_key = "folders",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-phl-carto-311",
    .name = "Philadelphia 311 service requests",
    .collector = "us_government", .category = "government",
    .description = "service_request_id, status, status_notes, service_name, service_code, requested_datetime, updated_datetime, expected_datetime, agency_responsible, address, zipcode, lat/lon, media_url. Verified detail lookup by service_request_id=10007982.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://phl.carto.com/api/v2/sql?q=SELECT%20*%20FROM%20public_cases_fc%20LIMIT%205",
    .array_path = "rows",
    .detail_url = "https://phl.carto.com/api/v2/sql?q=SELECT%20*%20FROM%20public_cases_fc%20WHERE%20service_request_id={v}", .detail_key = "service_request_id",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-phl-carto-li-permits",
    .name = "Philadelphia L&I building permits",
    .collector = "us_government", .category = "government",
    .description = "permitnumber, permittype, permit_type_name, typeofwork, descriptionofwork, status, permitissuedate, address, unit, zip, opa_account_num, ownername, organization, contractorname/contractortype, censustract. Pivots straight into the violations and case tables by opa_account_num.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://phl.carto.com/api/v2/sql?q=SELECT%20*%20FROM%20li_permits%20LIMIT%205",
    .array_path = "rows",
    .detail_url = "https://phl.carto.com/api/v2/sql?q=SELECT%20*%20FROM%20violations%20WHERE%20opa_account_num=%27{v}%27%20LIMIT%20200", .detail_key = "opa_account_num",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-phl-carto-opa-properties",
    .name = "Philadelphia OPA Property Assessments (Carto SQL API)",
    .collector = "us_government", .category = "government",
    .description = "Full assessor record per parcel: parcel_number (OPA account), location, owner_1/owner_2, mailing_address_1/2 + mailing city/state/zip (the out-of-state LLC address), market_value, sale_date, sale_price, book_and_page, building_code_description, category_code_description, year_built, total_livable_area, zoning, census_tract, council_district, lat/lng. Arbitrary SQL SELECT/WHERE/JOIN is accepted against the table.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://phl.carto.com/api/v2/sql?q=SELECT%20*%20FROM%20opa_properties_public%20LIMIT%205",
    .array_path = "rows",
    .detail_url = "https://phl.carto.com/api/v2/sql?q=SELECT%20*%20FROM%20assessments%20WHERE%20parcel_number=%27{v}%27", .detail_key = "parcel_number",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-usaspending-agency-federal-account",
    .name = "USAspending agency federal-account breakdown",
    .collector = "us_government", .category = "government",
    .description = "157 federal accounts for USDA FY2024, each with code, name, obligated_amount, gross_outlay_amount and children[] listing the treasury account symbols beneath it. The appropriations map of an agency.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.usaspending.gov/api/v2/agency/012/federal_account/?fiscal_year=2024&limit=3",
    .array_path = "results",
    .detail_url = "https://api.usaspending.gov/api/v2/federal_accounts/{v}/", .detail_key = "code",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-usaspending-agency-overview",
    .name = "USAspending agency profile",
    .collector = "us_government", .category = "government",
    .description = "Detail hop off a toptier agency code: fiscal_year, toptier_code, name, abbreviation, agency_id, mission statement, website, congressional_justification_url, subtier_agency_count, and def_codes[] listing every disaster/emergency funding statute the agency draws on (code, public_law, title, govinfo URL, disaster type).  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.usaspending.gov/api/v2/agency/012/",
    .array_path = "def_codes",
    .detail_url = "https://api.usaspending.gov/api/v2/agency/{v}/sub_agency/?fiscal_year=2024", .detail_key = "toptier_code",
    .interval = 86400, .free_tier = 1 },

  { .id = "us-usaspending-reporting-agencies-overview",
    .name = "USAspending agency data-quality overview",
    .collector = "us_government", .category = "government",
    .description = "111 agencies with agency_name, abbreviation, toptier_code, agency_id, current_total_budget_authority_amount, recent_publication_date, discrepancy_count, obligation_difference, unlinked contract/assistance award counts and assurance statement URL. This is the metadata that tells you how trustworthy an agency's reported spending is.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "government-record", .tags = "\"us\",\"government\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://api.usaspending.gov/api/v2/reporting/agencies/overview/?fiscal_year=2024&fiscal_period=9&limit=3",
    .array_path = "results",
    .detail_url = "https://api.usaspending.gov/api/v2/reporting/agencies/{v}/differences/?fiscal_year=2024&fiscal_period=9", .detail_key = "toptier_code",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
