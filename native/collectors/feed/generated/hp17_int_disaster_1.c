/* Deep-record int_disaster sources (3), part 1 of 1.
 *
 * Batch 17 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch17.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "ifrc-go-country",
    .name = "IFRC GO — national societies directory",
    .collector = "int_disaster", .category = "disaster",
    .description = "281 countries/territories with the Red Cross/Red Crescent national society name, society URL, IFRC directory URL, FDRS code, postal address, phone, emails, website, INFORM risk score, bbox and centroid. Detail hop (id 14) adds contacts and NSI income/expenditure/branch counts.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "disaster-record", .tags = "\"int\",\"disaster\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://goadmin.ifrc.org/api/v2/country/?limit=300&format=json",
    .array_path = "results",
    .detail_url = "https://goadmin.ifrc.org/api/v2/country/{v}/?format=json", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "ifrc-go-field-report",
    .name = "IFRC GO — field reports (operational situation reports)",
    .collector = "int_disaster", .category = "disaster",
    .description = "Per-emergency field report: title, casualty/affected counts (num_dead, num_injured, num_missing, num_displaced, gov_ and other_ variants), epidemic case counts, DREF/appeal amounts requested, ERU deployment flags by unit type, national-society contacts, countries/districts, actions taken. The detail hop with a real id (18533) returns the same record plus full contact and action narrative.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "disaster-record", .tags = "\"int\",\"disaster\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://goadmin.ifrc.org/api/v2/field-report/?limit=200&format=json",
    .array_path = "results",
    .detail_url = "https://goadmin.ifrc.org/api/v2/field-report/{v}/?format=json", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

  { .id = "ifrc-go-project",
    .name = "IFRC GO — 3W national-society projects",
    .collector = "int_disaster", .category = "disaster",
    .description = "Who-does-what-where: project name, reporting national society, beneficiary country and districts, sector, programme/operation type, budget_amount, actual_expenditure, target vs reached beneficiaries split by sex, start/end dates. Detail hop verified with id 2510.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "disaster-record", .tags = "\"int\",\"disaster\",\"batch17\",\"detail-hop\",\"deep-record\"", .lang = "en",
    .url = "https://goadmin.ifrc.org/api/v2/project/?limit=200&format=json",
    .array_path = "results",
    .detail_url = "https://goadmin.ifrc.org/api/v2/project/{v}/?format=json", .detail_key = "id",
    .interval = 86400, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
