/* Deep-record ch_legal sources (1), part 1 of 1.
 *
 * Batch 16 verified each of these endpoints live AND separately probed the
 * per-record detail endpoint below it with a real id. The list half shipped as
 * a VJSON collector and the proven detail half sat unused in
 * docs/candidate-sources-batch16.detail-hops.tsv, which is a rule-2 violation
 * (docs/SOURCE_EXHAUSTIVENESS.md): the route to the record behind each hit was
 * known and not walked. These rows walk it.
 *
 * Generated by collectors/gen_detail_hop_upgrade.py — regenerate rather than
 * hand-editing. */
#include "lib/hpengine.h"

static const hp_source T[] = {
  { .id = "ch-entscheidsuche-elasticsearch",
    .name = "entscheidsuche.ch - all Swiss court decisions (Elasticsearch proxy)",
    .collector = "ch_legal", .category = "legal",
    .description = "Open Elasticsearch proxy over 53 shards covering federal and all cantonal Swiss courts; 10,000+ hits on a single keyword. Each hit's _source gives: date, court hierarchy array (canton/court/collection), multilingual abstract (de/fr/it), multilingual title, reference/keywords array, source id, AND an attachment object containing content_type, language, the decision PDF content_url and - critically - the ENTIRE extracted PDF text inline. One request yields both the index row and the full decision body; the content_url is the verified PDF detail hop.  A per-record detail endpoint was verified for this source; see docs/verified-sources-batch16.md. This collector fetches the list endpoint only.  Second hop: the record behind each list hit is fetched from the row's detail endpoint and merged in under detail.*, so the row returns the record and not just the search result. Records past the per-run detail budget ($JO_HP_DETAIL_MAX) are stamped _detail_pending rather than shipped as though nothing was behind them.",
    .record_type = "legal-record", .tags = "\"ch\",\"legal\",\"batch16\",\"detail-hop\",\"deep-record\"", .lang = "de",
    .url = "https://entscheidsuche.ch/_search.php?q=Kartell",
    .array_path = "hits.hits",
    .detail_url = "{v}", .detail_key = "attachment.content_url",
    .interval = 43200, .free_tier = 1 },

};
HP_REGISTER_TABLE(T)
