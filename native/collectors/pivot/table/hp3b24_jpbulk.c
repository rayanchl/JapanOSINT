/* collectors/pivot/table/hp3b24_jpbulk.c -- batch 24: jpbulk
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch24.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch24.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch24.jpbulk.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3B24_JPBULK[] = {
  { .id = "JPOD_DATAGOJP_INDEX", .name = "data.go.jp — complete dataset index",
    .name_ja = "データカタログサイト 全データセット一覧",
    .category = "government", .portal = "https://www.data.go.jp",
    .record_type = "dataset-index",
    .tags = "\"japan\",\"opendata\",\"datagojp\",\"catalogue\",\"index\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.data.go.jp/data/api/3/action/package_list",
    .array_path = "result",
    .id_keys = "value",
    .interval = 86400,
    .title_keys = "value",
    .description = "Every dataset identifier held by the Japanese national open-data "
      "portal — 18,147 of them, returned whole. This is the ONLY "
      "endpoint on that portal that hands back its full holdings: "
      "package_search ignores rows, start and every filter alike and "
      "returns the same ten records whatever you ask it, so the "
      "per-organisation and free-text forms were rejected outright. The "
      "index is what the portal will honestly give, and it is a "
      "complete inventory of what the state publishes." },

  { .id = "JSTAGE_CORPUS_BULK", .name = "J-STAGE — article corpus sweep",
    .name_ja = "J-STAGE 論文コーパス",
    .category = "research", .portal = "https://www.jstage.jst.go.jp",
    .record_type = "journal-article",
    .tags = "\"japan\",\"jstage\",\"research\",\"journal\",\"corpus\"",
    .mode = HP_XML, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.jstage.jst.go.jp/searchapi/do?service=3&text=%E6%97%A5%E6%9C%AC&count=100",
    .array_path = "entry",
    .date_keys = "published,updated",
    .id_keys = "id,doi",
    .interval = 86400,
    .max_items = 20000,
    .page_param = "start",
    .page_size = 100,
    .page_start = 1,
    .title_keys = "article_title,title",
    .description = "The J-STAGE article corpus — the electronic editions of roughly "
      "three thousand Japanese scholarly societies, most publishing in "
      "Japanese only and absent from every international index. Each "
      "record carries every author with their affiliation, the society, "
      "the volume and the DOI. Registered as a BULK sweep rather than a "
      "search because the search is not real: service=3 accepts text= "
      "and article= and ignores both, returning 20 of 20 entries for a "
      "term that cannot exist. totalResults is 2,637,915 and start= was "
      "verified to advance to at least 10,001, so this row is "
      "deliberately bounded by max_items and emits a "
      "collector-truncation-notice stating how much of the corpus it "
      "took — bounded and in-band rather than silently partial." },
};

HP_REGISTER_TABLE(HP3B24_JPBULK)
