/* collectors/pivot/table/hp3b19_intlorg.c -- batch 19: intlorg
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch19.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch19.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch19.intlorg.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3B19_INTLORG[] = {
  { .id = "UNESCO_UIS_VERSIONS", .name = "UNESCO UIS — data release versions",
    .name_ja = "UNESCO統計研究所 データ版",
    .category = "research", .portal = "https://uis.unesco.org",
    .record_type = "statistical-release",
    .tags = "\"statistics\",\"unesco\",\"education\",\"un\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.uis.unesco.org/api/public/versions",
    .interval = 86400,
    .description = "Every published version of the UNESCO Institute for Statistics "
      "database with its publication date, description and the themes "
      "it covers. UIS is the authority for cross-country education, "
      "science spending and R&D personnel figures, and the version "
      "record is what makes a citation to those figures reproducible" },

  { .id = "UNHCR_COUNTRY_LIST", .name = "UNHCR — country and territory reference list",
    .name_ja = "UNHCR 国・地域参照表",
    .category = "government", .portal = "https://www.unhcr.org",
    .record_type = "displacement-reference",
    .tags = "\"refugee\",\"displacement\",\"unhcr\",\"un\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.unhcr.org/population/v1/countries/",
    .array_path = "items",
    .interval = 86400,
    .page_param = "page",
    .page_start = 1,
    .description = "The controlled list of countries and territories UNHCR reports "
      "displacement against, with ISO codes, names and the regional "
      "bureau responsible. The join key for every UNHCR population "
      "series, and the reference that resolves contested territory "
      "naming consistently" },

  { .id = "WORLDBANK_COUNTRY_REGISTER", .name = "World Bank — country and aggregate register",
    .name_ja = "世界銀行 国・集計区分一覧",
    .category = "research", .portal = "https://data.worldbank.org",
    .record_type = "country-reference",
    .tags = "\"worldbank\",\"reference\",\"country\",\"statistics\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.worldbank.org/v2/country?format=json&per_page=300",
    .array_path = "1",
    .id_keys = "id",
    .interval = 604800,
    .page_param = "page",
    .title_keys = "name",
    .description = "Every economy and regional aggregate the World Bank reports "
      "against, with region, income group, lending category and capital "
      "city coordinates. This is the code list every World Bank "
      "indicator series is keyed to, so it is what makes those series "
      "joinable to anything else." },

  { .id = "WORLDBANK_SOURCE_REGISTER", .name = "World Bank — statistical source register",
    .name_ja = "世界銀行 統計ソース一覧",
    .category = "research", .portal = "https://data.worldbank.org",
    .record_type = "statistical-source",
    .tags = "\"worldbank\",\"reference\",\"statistics\",\"metadata\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.worldbank.org/v2/sources?format=json&per_page=200",
    .array_path = "1",
    .id_keys = "id",
    .interval = 604800,
    .title_keys = "name",
    .description = "The databases behind the World Bank API — World Development "
      "Indicators, IDS, Doing Business and the rest — with their update "
      "cadence and coverage. Says which collection a figure came from "
      "rather than treating \"the World Bank\" as one source." },

  { .id = "WORLDBANK_INDICATOR_CATALOGUE", .name = "World Bank — indicator catalogue",
    .name_ja = "世界銀行 指標カタログ",
    .category = "research", .portal = "https://data.worldbank.org",
    .record_type = "statistical-indicator",
    .tags = "\"worldbank\",\"reference\",\"indicator\",\"statistics\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.worldbank.org/v2/indicator?format=json&per_page=500",
    .array_path = "1",
    .id_keys = "id",
    .interval = 604800,
    .page_param = "page",
    .title_keys = "name",
    .description = "Roughly 25,000 indicator definitions with source note, "
      "organisation and topic. Enumerating it is the difference between "
      "querying indicators you already know the code for and "
      "discovering what exists." },

  { .id = "WHO_GHO_DIMENSIONS", .name = "WHO Global Health Observatory — dimension register",
    .name_ja = "WHO GHO 次元一覧",
    .category = "research", .portal = "https://www.who.int/data/gho",
    .record_type = "health-dimension",
    .tags = "\"who\",\"health\",\"dimension\",\"un\",\"metadata\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ghoapi.azureedge.net/api/DIMENSION",
    .array_path = "value",
    .id_keys = "Code",
    .interval = 604800,
    .title_keys = "Title",
    .description = "The disaggregation dimensions GHO facts are broken down by — "
      "country, year, sex, age group, residence. Without them a GHO "
      "fact is a number with no idea what population it describes." },

  { .id = "IEA_TES_BY_SOURCE", .name = "IEA — total energy supply by source",
    .name_ja = "IEA 一次エネルギー供給",
    .category = "energy", .portal = "https://www.iea.org/data-and-statistics",
    .record_type = "energy-statistic",
    .tags = "\"iea\",\"energy\",\"supply\",\"statistics\",\"intlorg\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.iea.org/stats/indicator/TESbySource",
    .id_keys = "flowLabel",
    .interval = 604800,
    .title_keys = "country",
    .description = "Total energy supply broken down by source for every country and "
      "year the IEA publishes. The IEA series is the one national "
      "energy balances are reconciled against, and it is served here "
      "without a key." },
};

HP_REGISTER_TABLE(HP3B19_INTLORG)
