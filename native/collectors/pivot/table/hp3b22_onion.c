/* collectors/pivot/table/hp3b22_onion.c -- batch 22: onion
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch22.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch22.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch22.onion.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3B22_ONION[] = {
  { .id = "TOR_METRICS_NETWORKSIZE", .name = "Tor Metrics — network size (relays and bridges)",
    .name_ja = "Tor メトリクス ネットワーク規模",
    .category = "infrastructure", .portal = "https://metrics.torproject.org/networksize.html",
    .record_type = "relay-statistic",
    .tags = "\"tor\",\"relay\",\"network\",\"metrics\",\"anonymity\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://metrics.torproject.org/networksize.csv",
    .csv_comment = "#",
    .interval = 86400,
    .title_keys = "date",
    .description = "The daily count of running Tor relays and bridges since 2007, "
      "one row per date. It is the headline capacity measure of the "
      "anonymity network and the denominator every other relay "
      "statistic is read against, so a sudden dip flags a large "
      "relay-family removal or an outage before any incident report "
      "does" },

  { .id = "TOR_METRICS_PLATFORMS", .name = "Tor Metrics — relays by operating system",
    .name_ja = "Tor メトリクス プラットフォーム別",
    .category = "infrastructure", .portal = "https://metrics.torproject.org/platforms.html",
    .record_type = "relay-statistic",
    .tags = "\"tor\",\"relay\",\"platform\",\"os\",\"metrics\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://metrics.torproject.org/platforms.csv",
    .csv_comment = "#",
    .interval = 86400,
    .title_keys = "date",
    .description = "Daily relay counts split across BSD, Linux, macOS, Windows and "
      "other, one wide row per date. The operating-system mix is a "
      "durable fingerprint of the relay operator population and moves "
      "when a large hosting provider or a single operator family joins "
      "or leaves" },

  { .id = "TOR_METRICS_BANDWIDTH", .name = "Tor Metrics — advertised and consumed bandwidth",
    .name_ja = "Tor メトリクス 帯域幅",
    .category = "infrastructure", .portal = "https://metrics.torproject.org/bandwidth.html",
    .record_type = "relay-statistic",
    .tags = "\"tor\",\"relay\",\"bandwidth\",\"capacity\",\"metrics\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://metrics.torproject.org/bandwidth.csv",
    .csv_comment = "#",
    .interval = 86400,
    .title_keys = "date",
    .description = "The advertised bandwidth capacity of the network against the "
      "bandwidth actually consumed, per day. The gap between the two is "
      "the network's spare headroom, and a closing gap is the leading "
      "indicator of congestion that degrades every user's circuit" },

  { .id = "TOR_METRICS_DIRBYTES", .name = "Tor Metrics — directory traffic",
    .name_ja = "Tor メトリクス ディレクトリ通信量",
    .category = "infrastructure", .portal = "https://metrics.torproject.org/dirbytes.html",
    .record_type = "relay-statistic",
    .tags = "\"tor\",\"directory\",\"bandwidth\",\"metrics\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://metrics.torproject.org/dirbytes.csv",
    .csv_comment = "#",
    .interval = 86400,
    .title_keys = "date",
    .description = "The bytes read and written serving directory information, broken "
      "out for ordinary relays and for the directory authorities, per "
      "day. Directory traffic is the control-plane cost of the network "
      "and a spike on the authority lines points at a client-side bug "
      "or a descriptor-flooding attempt" },

  { .id = "TOR_METRICS_HIDSERV_ONIONS_SEEN", .name = "Tor Metrics — unique onion addresses seen",
    .name_ja = "Tor メトリクス オニオンアドレス数",
    .category = "infrastructure", .portal = "https://metrics.torproject.org/hidserv-dir-onions-seen.html",
    .record_type = "onion-statistic",
    .tags = "\"tor\",\"onion\",\"hidden-service\",\"hsdir\",\"metrics\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://metrics.torproject.org/hidserv-dir-onions-seen.csv",
    .csv_comment = "#",
    .interval = 86400,
    .title_keys = "date",
    .description = "The estimated number of distinct onion-service addresses the "
      "directory relays observed per day, with the fraction of HSDirs "
      "reporting. It is the only public population count of the "
      "onion-service ecosystem, so its trend is the ground truth behind "
      "any claim that hidden services are growing or shrinking" },

  { .id = "TOR_METRICS_HIDSERV_RELAYED_CELLS", .name = "Tor Metrics — onion-service rendezvous traffic",
    .name_ja = "Tor メトリクス オニオン中継トラフィック",
    .category = "infrastructure", .portal = "https://metrics.torproject.org/hidserv-rend-relayed-cells.html",
    .record_type = "onion-statistic",
    .tags = "\"tor\",\"onion\",\"hidden-service\",\"traffic\",\"metrics\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://metrics.torproject.org/hidserv-rend-relayed-cells.csv",
    .csv_comment = "#",
    .interval = 86400,
    .title_keys = "date",
    .description = "The estimated volume of cells relayed at onion-service "
      "rendezvous points per day, with the reporting fraction. Read "
      "against the address count it separates a network of many small "
      "onion sites from one dominated by a few high-traffic services, "
      "which is the difference between a marketplace surge and "
      "background growth" },

  { .id = "TOR_METRICS_WEBSTATS_TB", .name = "Tor Metrics — Tor Browser downloads and updates",
    .name_ja = "Tor メトリクス ブラウザDL",
    .category = "infrastructure", .portal = "https://metrics.torproject.org/webstats-tb.html",
    .record_type = "usage-statistic",
    .tags = "\"tor\",\"browser\",\"download\",\"adoption\",\"metrics\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://metrics.torproject.org/webstats-tb.csv",
    .csv_comment = "#",
    .interval = 86400,
    .title_keys = "date",
    .description = "The daily count of initial Tor Browser downloads, signature "
      "downloads and update pings from the web server logs. Initial "
      "downloads track new-user acquisition while update pings track "
      "the installed base, and the two together are the closest public "
      "proxy for the size of the Tor user population" },

  { .id = "TOR_METRICS_RELAYS_IPV6", .name = "Tor Metrics — IPv6 relay counts",
    .name_ja = "Tor メトリクス IPv6リレー",
    .category = "infrastructure", .portal = "https://metrics.torproject.org/relays-ipv6.html",
    .record_type = "relay-statistic",
    .tags = "\"tor\",\"relay\",\"ipv6\",\"reachability\",\"metrics\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://metrics.torproject.org/relays-ipv6.csv",
    .csv_comment = "#",
    .interval = 86400,
    .title_keys = "date",
    .description = "The daily count of relays that announce an IPv6 address, that "
      "are reachable over it, and that will exit over it, one wide row "
      "per date. IPv6 exit capacity is a known scarce resource on the "
      "network, and this series is how you see whether it is keeping "
      "pace with demand" },

  { .id = "TOR_METRICS_BRIDGES_IPV6", .name = "Tor Metrics — IPv6 bridge counts",
    .name_ja = "Tor メトリクス IPv6ブリッジ",
    .category = "infrastructure", .portal = "https://metrics.torproject.org/bridges-ipv6.html",
    .record_type = "relay-statistic",
    .tags = "\"tor\",\"bridge\",\"ipv6\",\"reachability\",\"metrics\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://metrics.torproject.org/bridges-ipv6.csv",
    .csv_comment = "#",
    .interval = 86400,
    .title_keys = "date",
    .description = "The daily count of bridges in total and of those announcing an "
      "IPv6 address. IPv6-capable bridges matter where a censor has "
      "better control over IPv4 than IPv6 routing, so this count is a "
      "quiet input into whether circumvention over IPv6 is even an "
      "option in a given network" },

  { .id = "TOR_COLLECTOR_INDEX", .name = "Tor CollecTor — archive index",
    .name_ja = "Tor CollecTor アーカイブ索引",
    .category = "infrastructure", .portal = "https://collector.torproject.org",
    .record_type = "archive-index",
    .tags = "\"tor\",\"collector\",\"archive\",\"descriptor\",\"metrics\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://collector.torproject.org/index/index.json",
    .array_path = "directories",
    .interval = 86400,
    .title_keys = "path",
    .description = "The machine-readable index of the CollecTor archive, naming the "
      "recent and archive trees that hold every consensus, relay "
      "descriptor, bridge descriptor and onion-service statistic the "
      "network has ever published. It is the entry point to the raw "
      "provenance behind every aggregate on Tor Metrics" },

  { .id = "RANSOMLOOK_STATS", .name = "RansomLook — ecosystem statistics",
    .name_ja = "RansomLook 統計",
    .category = "threat", .portal = "https://www.ransomlook.io",
    .record_type = "leaksite-statistic",
    .tags = "\"darkweb\",\"ransomware\",\"leaksite\",\"metrics\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.ransomlook.io/api/stats",
    .interval = 21600,
    .description = "A single snapshot of the tracked extortion ecosystem: the number "
      "of groups, their total and currently-reachable onion locations, "
      "the split of leak, file-share, chat and admin infrastructure, "
      "the market count and the running post totals. Sampled over time "
      "it is a compact health-of-the-ecosystem series that needs no "
      "per-record parsing" },
};

HP_REGISTER_TABLE(HP3B22_ONION)
