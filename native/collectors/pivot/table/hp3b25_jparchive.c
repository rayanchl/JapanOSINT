/* collectors/pivot/table/hp3b25_jparchive.c -- batch 25: jparchive
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch25.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch25.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch25.jparchive.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3B25_JPARCHIVE[] = {
  { .id = "JP25_JPARCH_WEKO_AICHI_PU", .name = "愛知県立大学 institutional repository (JAIRO Cloud)",
    .name_ja = "愛知県立大学学術リポジトリ",
    .category = "research", .portal = "https://aichi-pu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://aichi-pu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 愛知県立大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 3024 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_AIR", .name = "秋田大学 institutional repository (JAIRO Cloud)",
    .name_ja = "秋田大学学術情報リポジトリ",
    .category = "research", .portal = "https://air.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://air.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 秋田大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 3465 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_AIST", .name = "産業技術総合研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "産総研リポジトリ",
    .category = "research", .portal = "https://aist.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://aist.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 産業技術総合研究所, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 62 and an impossible entity returned total "
      "0, so the filter is honoured. Pagination follows links.next. The "
      "OAI-PMH ListRecords on the same host answers in 35-70 s, beyond "
      "the engine's 20 s budget, which is why the JSON API is "
      "registered instead." },

  { .id = "JP25_JPARCH_WEKO_BARREL", .name = "小樽商科大学 institutional repository (JAIRO Cloud)",
    .name_ja = "小樽商科大学学術成果コレクション",
    .category = "research", .portal = "https://barrel.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://barrel.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 小樽商科大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 2513 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_CHIKYU", .name = "人間文化研究機構総合地球環境学研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "総合地球環境学研究所学術情報リポジトリ",
    .category = "research", .portal = "https://chikyu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://chikyu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "人間文化研究機構総合地球環境学研究所, served by the WEKO3 (Invenio) records API "
      "that every JAIRO Cloud tenant exposes. Records are theses, "
      "bulletin articles, reports and datasets with title, every "
      "creator with affiliation, publisher, ISSN, handle/DOI and the "
      "file URL under metadata; hits.total for 研究 was 3031 and an "
      "impossible entity returned total 0, so the filter is honoured. "
      "Pagination follows links.next. The OAI-PMH ListRecords on the "
      "same host answers in 35-70 s, beyond the engine's 20 s budget, "
      "which is why the JSON API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_DOKKYO", .name = "獨協大学 institutional repository (JAIRO Cloud)",
    .name_ja = "獨協大学学術リポジトリ",
    .category = "research", .portal = "https://dokkyo.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://dokkyo.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 獨協大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1277 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_DWCLA", .name = "同志社女子大学 institutional repository (JAIRO Cloud)",
    .name_ja = "同志社女子大学学術リポジトリ",
    .category = "research", .portal = "https://dwcla.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://dwcla.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 同志社女子大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1397 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_FRA", .name = "水産研究・教育機構 institutional repository (JAIRO Cloud)",
    .name_ja = "水産研究・教育機構機関リポジトリ",
    .category = "research", .portal = "https://fra.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://fra.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 水産研究・教育機構, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 11663 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_GLIM_RE", .name = "学習院大学 institutional repository (JAIRO Cloud)",
    .name_ja = "学習院大学学術成果リポジトリ",
    .category = "research", .portal = "https://glim-re.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://glim-re.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 学習院大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 3298 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_HIROSHIMA_CU", .name = "広島市立大学 institutional repository (JAIRO Cloud)",
    .name_ja = "広島市立大学学術リポジトリ",
    .category = "research", .portal = "https://hiroshima-cu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://hiroshima-cu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 広島市立大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1204 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_HOKKYODAI", .name = "北海道教育大学 institutional repository (JAIRO Cloud)",
    .name_ja = "北海道教育大学学術リポジトリ",
    .category = "research", .portal = "https://hokkyodai.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://hokkyodai.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 北海道教育大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 5049 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_ICU", .name = "国際基督教大学 institutional repository (JAIRO Cloud)",
    .name_ja = "国際基督教大学リポジトリ",
    .category = "research", .portal = "https://icu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://icu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 国際基督教大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 3819 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_IPSS", .name = "国立社会保障・人口問題研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "国立社会保障・人口問題研究所リポジトリ",
    .category = "research", .portal = "https://ipss.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ipss.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "国立社会保障・人口問題研究所, served by the WEKO3 (Invenio) records API that "
      "every JAIRO Cloud tenant exposes. Records are theses, bulletin "
      "articles, reports and datasets with title, every creator with "
      "affiliation, publisher, ISSN, handle/DOI and the file URL under "
      "metadata; hits.total for 研究 was 972 and an impossible entity "
      "returned total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_JAEA", .name = "日本原子力研究開発機構 institutional repository (JAIRO Cloud)",
    .name_ja = "研究開発成果検索・閲覧システム（JOPSS）",
    .category = "research", .portal = "https://jaea.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://jaea.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "日本原子力研究開発機構, served by the WEKO3 (Invenio) records API that "
      "every JAIRO Cloud tenant exposes. Records are theses, bulletin "
      "articles, reports and datasets with title, every creator with "
      "affiliation, publisher, ISSN, handle/DOI and the file URL under "
      "metadata; hits.total for 研究 was 13 and an impossible entity "
      "returned total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_JAXA", .name = "宇宙航空研究開発機構 institutional repository (JAIRO Cloud)",
    .name_ja = "宇宙航空研究開発機構リポジトリ",
    .category = "research", .portal = "https://jaxa.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://jaxa.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "宇宙航空研究開発機構, served by the WEKO3 (Invenio) records API that every "
      "JAIRO Cloud tenant exposes. Records are theses, bulletin "
      "articles, reports and datasets with title, every creator with "
      "affiliation, publisher, ISSN, handle/DOI and the file URL under "
      "metadata; hits.total for 研究 was 46543 and an impossible entity "
      "returned total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_JIIA", .name = "公益財団法人日本国際問題研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "日本国際問題研究所リポジトリ",
    .category = "research", .portal = "https://jiia.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://jiia.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "公益財団法人日本国際問題研究所, served by the WEKO3 (Invenio) records API that "
      "every JAIRO Cloud tenant exposes. Records are theses, bulletin "
      "articles, reports and datasets with title, every creator with "
      "affiliation, publisher, ISSN, handle/DOI and the file URL under "
      "metadata; hits.total for 研究 was 1497 and an impossible entity "
      "returned total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_JIRCAS", .name = "国際農林水産業研究センター institutional repository (JAIRO Cloud)",
    .name_ja = "国際農研機関リポジトリ",
    .category = "research", .portal = "https://jircas.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://jircas.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "国際農林水産業研究センター, served by the WEKO3 (Invenio) records API that "
      "every JAIRO Cloud tenant exposes. Records are theses, bulletin "
      "articles, reports and datasets with title, every creator with "
      "affiliation, publisher, ISSN, handle/DOI and the file URL under "
      "metadata; hits.total for 研究 was 692 and an impossible entity "
      "returned total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_JST", .name = "国立研究開発法人科学技術振興機構 institutional repository (JAIRO Cloud)",
    .name_ja = "科学技術振興機構リポジトリ",
    .category = "research", .portal = "https://jst.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://jst.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "国立研究開発法人科学技術振興機構, served by the WEKO3 (Invenio) records API that "
      "every JAIRO Cloud tenant exposes. Records are theses, bulletin "
      "articles, reports and datasets with title, every creator with "
      "affiliation, publisher, ISSN, handle/DOI and the file URL under "
      "metadata; hits.total for 研究 was 212 and an impossible entity "
      "returned total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_JWU", .name = "日本女子大学 institutional repository (JAIRO Cloud)",
    .name_ja = "日本女子大学学術情報リポジトリ",
    .category = "research", .portal = "https://jwu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://jwu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 日本女子大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 2488 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_KITAKYU", .name = "北九州市立大学 institutional repository (JAIRO Cloud)",
    .name_ja = "北九州市立大学　学術リポジトリ　（ルクソール）",
    .category = "research", .portal = "https://kitakyu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://kitakyu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 北九州市立大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 669 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_KOCHI", .name = "高知大学 institutional repository (JAIRO Cloud)",
    .name_ja = "高知大学学術情報リポジトリ",
    .category = "research", .portal = "https://kochi.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://kochi.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 高知大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 5142 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_KPU", .name = "京都府立大学 institutional repository (JAIRO Cloud)",
    .name_ja = "京都府立大学学術機関リポジトリ",
    .category = "research", .portal = "https://kpu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://kpu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 京都府立大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1223 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_KSU", .name = "京都産業大学 institutional repository (JAIRO Cloud)",
    .name_ja = "京都産業大学学術リポジトリ",
    .category = "research", .portal = "https://ksu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ksu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 京都産業大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 4412 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_KUMADAI", .name = "熊本大学 institutional repository (JAIRO Cloud)",
    .name_ja = "熊本大学学術リポジトリ",
    .category = "research", .portal = "https://kumadai.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://kumadai.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 熊本大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 7202 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_MINPAKU", .name = "人間文化研究機構国立民族学博物館 institutional repository (JAIRO Cloud)",
    .name_ja = "国立民族学博物館学術情報リポジトリ（みんぱくリポジトリ）",
    .category = "research", .portal = "https://minpaku.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://minpaku.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "人間文化研究機構国立民族学博物館, served by the WEKO3 (Invenio) records API that "
      "every JAIRO Cloud tenant exposes. Records are theses, bulletin "
      "articles, reports and datasets with title, every creator with "
      "affiliation, publisher, ISSN, handle/DOI and the file URL under "
      "metadata; hits.total for 研究 was 2224 and an impossible entity "
      "returned total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NAGOYA", .name = "名古屋大学 institutional repository (JAIRO Cloud)",
    .name_ja = "名古屋大学学術機関リポジトリ",
    .category = "research", .portal = "https://nagoya.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://nagoya.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 名古屋大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 21198 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NARAHAKU", .name = "奈良国立博物館 institutional repository (JAIRO Cloud)",
    .name_ja = "奈良国立博物館リポジトリ",
    .category = "research", .portal = "https://narahaku.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://narahaku.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 奈良国立博物館, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 275 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NICHIBUN", .name = "人間文化研究機構国際日本文化研究センター institutional repository (JAIRO Cloud)",
    .name_ja = "国際日本文化研究センター学術リポジトリ",
    .category = "research", .portal = "https://nichibun.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://nichibun.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "人間文化研究機構国際日本文化研究センター, served by the WEKO3 (Invenio) records API "
      "that every JAIRO Cloud tenant exposes. Records are theses, "
      "bulletin articles, reports and datasets with title, every "
      "creator with affiliation, publisher, ISSN, handle/DOI and the "
      "file URL under metadata; hits.total for 研究 was 6005 and an "
      "impossible entity returned total 0, so the filter is honoured. "
      "Pagination follows links.next. The OAI-PMH ListRecords on the "
      "same host answers in 35-70 s, beyond the engine's 20 s budget, "
      "which is why the JSON API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NICT", .name = "情報通信研究機構 institutional repository (JAIRO Cloud)",
    .name_ja = "NICTリポジトリ",
    .category = "research", .portal = "https://nict.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://nict.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 情報通信研究機構, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 53 and an impossible entity returned total "
      "0, so the filter is honoured. Pagination follows links.next. The "
      "OAI-PMH ListRecords on the same host answers in 35-70 s, beyond "
      "the engine's 20 s budget, which is why the JSON API is "
      "registered instead." },

  { .id = "JP25_JPARCH_WEKO_NIER", .name = "国立教育政策研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "国立教育政策研究所研究成果アーカイブ",
    .category = "research", .portal = "https://nier.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://nier.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 国立教育政策研究所, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1867 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NIES", .name = "国立環境研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "国立環境研究所機関リポジトリ",
    .category = "research", .portal = "https://nies.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://nies.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 国立環境研究所, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 190 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NIPH", .name = "国立保健医療科学院 institutional repository (JAIRO Cloud)",
    .name_ja = "国立保健医療科学院リポジトリ",
    .category = "research", .portal = "https://niph.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://niph.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 国立保健医療科学院, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1313 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NIPR", .name = "国立極地研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "国立極地研究所学術情報リポジトリ",
    .category = "research", .portal = "https://nipr.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://nipr.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 国立極地研究所, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 5746 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NISE", .name = "国立特別支援教育総合研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "国立特別支援教育総合研究所リポジトリ",
    .category = "research", .portal = "https://nise.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://nise.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "国立特別支援教育総合研究所, served by the WEKO3 (Invenio) records API that "
      "every JAIRO Cloud tenant exposes. Records are theses, bulletin "
      "articles, reports and datasets with title, every creator with "
      "affiliation, publisher, ISSN, handle/DOI and the file URL under "
      "metadata; hits.total for 研究 was 1279 and an impossible entity "
      "returned total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NISTEP", .name = "文部科学省　科学技術・学術政策研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "科学技術・学術政策研究所 ライブラリ",
    .category = "research", .portal = "https://nistep.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://nistep.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "文部科学省　科学技術・学術政策研究所, served by the WEKO3 (Invenio) records API "
      "that every JAIRO Cloud tenant exposes. Records are theses, "
      "bulletin articles, reports and datasets with title, every "
      "creator with affiliation, publisher, ISSN, handle/DOI and the "
      "file URL under metadata; hits.total for 研究 was 2399 and an "
      "impossible entity returned total 0, so the filter is honoured. "
      "Pagination follows links.next. The OAI-PMH ListRecords on the "
      "same host answers in 35-70 s, beyond the engine's 20 s budget, "
      "which is why the JSON API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NWEC", .name = "男女共同参画機構 institutional repository (JAIRO Cloud)",
    .name_ja = "男女共同参画機構リポジトリ",
    .category = "research", .portal = "https://nwec.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://nwec.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 男女共同参画機構, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1589 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_OACIS", .name = "東京海洋大学 institutional repository (JAIRO Cloud)",
    .name_ja = "東京海洋大学学術機関リポジトリ",
    .category = "research", .portal = "https://oacis.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://oacis.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 東京海洋大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1665 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_OKA_PU", .name = "岡山県立大学 institutional repository (JAIRO Cloud)",
    .name_ja = "岡山県立大学学術情報リポジトリ",
    .category = "research", .portal = "https://oka-pu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://oka-pu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 岡山県立大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1492 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_OMNH", .name = "大阪市立自然史博物館 institutional repository (JAIRO Cloud)",
    .name_ja = "大阪市立自然史博物館学術リポジトリ",
    .category = "research", .portal = "https://omnh.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://omnh.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "大阪市立自然史博物館, served by the WEKO3 (Invenio) records API that every "
      "JAIRO Cloud tenant exposes. Records are theses, bulletin "
      "articles, reports and datasets with title, every creator with "
      "affiliation, publisher, ISSN, handle/DOI and the file URL under "
      "metadata; hits.total for 研究 was 840 and an impossible entity "
      "returned total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_OUR", .name = "大分大学 institutional repository (JAIRO Cloud)",
    .name_ja = "大分大学学術情報リポジトリ",
    .category = "research", .portal = "https://our.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://our.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 大分大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 2525 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_PU_HIROSHIMA", .name = "県立広島大学 institutional repository (JAIRO Cloud)",
    .name_ja = "県立広島大学学術情報リポジトリ",
    .category = "research", .portal = "https://pu-hiroshima.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://pu-hiroshima.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 県立広島大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 726 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_PU_KUMAMOTO", .name = "熊本県立大学 institutional repository (JAIRO Cloud)",
    .name_ja = "熊本県立大学リポジトリ",
    .category = "research", .portal = "https://pu-kumamoto.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://pu-kumamoto.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 熊本県立大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 766 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_REKIHAKU", .name = "人間文化研究機構国立歴史民俗博物館 institutional repository (JAIRO Cloud)",
    .name_ja = "国立歴史民俗博物館学術情報リポジトリ",
    .category = "research", .portal = "https://rekihaku.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://rekihaku.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "人間文化研究機構国立歴史民俗博物館, served by the WEKO3 (Invenio) records API "
      "that every JAIRO Cloud tenant exposes. Records are theses, "
      "bulletin articles, reports and datasets with title, every "
      "creator with affiliation, publisher, ISSN, handle/DOI and the "
      "file URL under metadata; hits.total for 研究 was 2612 and an "
      "impossible entity returned total 0, so the filter is honoured. "
      "Pagination follows links.next. The OAI-PMH ListRecords on the "
      "same host answers in 35-70 s, beyond the engine's 20 s budget, "
      "which is why the JSON API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_RIKEN", .name = "国立研究開発法人理化学研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "理研成果公開リポジトリ",
    .category = "research", .portal = "https://riken.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://riken.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "国立研究開発法人理化学研究所, served by the WEKO3 (Invenio) records API that "
      "every JAIRO Cloud tenant exposes. Records are theses, bulletin "
      "articles, reports and datasets with title, every creator with "
      "affiliation, publisher, ISSN, handle/DOI and the file URL under "
      "metadata; hits.total for 研究 was 13 and an impossible entity "
      "returned total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_RITSUMEI", .name = "立命館大学 institutional repository (JAIRO Cloud)",
    .name_ja = "立命館学術成果リポジトリ",
    .category = "research", .portal = "https://ritsumei.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ritsumei.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 立命館大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 10075 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_ROSE_IBADAI", .name = "茨城大学 institutional repository (JAIRO Cloud)",
    .name_ja = "茨城大学学術情報リポジトリ",
    .category = "research", .portal = "https://rose-ibadai.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://rose-ibadai.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 茨城大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 6156 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_SEINAN_GU", .name = "西南学院大学 institutional repository (JAIRO Cloud)",
    .name_ja = "西南学院大学　機関リポジトリ",
    .category = "research", .portal = "https://seinan-gu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://seinan-gu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 西南学院大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 2021 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_SOAR_IR", .name = "信州大学 institutional repository (JAIRO Cloud)",
    .name_ja = "信州大学機関リポジトリ",
    .category = "research", .portal = "https://soar-ir.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://soar-ir.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 信州大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 15847 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_SUCRA", .name = "埼玉大学 institutional repository (JAIRO Cloud)",
    .name_ja = "埼玉大学学術情報リポジトリ（SUCRA）",
    .category = "research", .portal = "https://sucra.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://sucra.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 埼玉大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 9972 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_SWU", .name = "昭和女子大学 institutional repository (JAIRO Cloud)",
    .name_ja = "昭和女子大学学術機関リポジトリ",
    .category = "research", .portal = "https://swu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://swu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 昭和女子大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 3827 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_TOBUNKEN", .name = "独立行政法人国立文化財機構東京文化財研究所 institutional repository (JAIRO Cloud)",
    .name_ja = "東京文化財研究所刊行物リポジトリ",
    .category = "research", .portal = "https://tobunken.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://tobunken.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of "
      "独立行政法人国立文化財機構東京文化財研究所, served by the WEKO3 (Invenio) records API "
      "that every JAIRO Cloud tenant exposes. Records are theses, "
      "bulletin articles, reports and datasets with title, every "
      "creator with affiliation, publisher, ISSN, handle/DOI and the "
      "file URL under metadata; hits.total for 研究 was 3357 and an "
      "impossible entity returned total 0, so the filter is honoured. "
      "Pagination follows links.next. The OAI-PMH ListRecords on the "
      "same host answers in 35-70 s, beyond the engine's 20 s budget, "
      "which is why the JSON API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_TOKYO_METRO_U", .name = "東京都立大学 institutional repository (JAIRO Cloud)",
    .name_ja = "みやこ鳥",
    .category = "research", .portal = "https://tokyo-metro-u.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://tokyo-metro-u.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 東京都立大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 5779 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_TUFS", .name = "東京外国語大学 institutional repository (JAIRO Cloud)",
    .name_ja = "東京外国語大学学術成果コレクション",
    .category = "research", .portal = "https://tufs.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://tufs.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 東京外国語大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 8177 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_TUS", .name = "東京理科大学 institutional repository (JAIRO Cloud)",
    .name_ja = "東京理科大学学術リポジトリ",
    .category = "research", .portal = "https://tus.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://tus.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 東京理科大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1143 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_TWCU", .name = "東京女子大学 institutional repository (JAIRO Cloud)",
    .name_ja = "東京女子大学学術情報リポジトリ",
    .category = "research", .portal = "https://twcu.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://twcu.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 東京女子大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1805 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_USHIMANE", .name = "島根県立大学 institutional repository (JAIRO Cloud)",
    .name_ja = "島根県立大学学術機関リポジトリ　USAGI",
    .category = "research", .portal = "https://ushimane.repo.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"jairo-cloud\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ushimane.repo.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "Full-text search over the institutional repository of 島根県立大学, "
      "served by the WEKO3 (Invenio) records API that every JAIRO Cloud "
      "tenant exposes. Records are theses, bulletin articles, reports "
      "and datasets with title, every creator with affiliation, "
      "publisher, ISSN, handle/DOI and the file URL under metadata; "
      "hits.total for 研究 was 1150 and an impossible entity returned "
      "total 0, so the filter is honoured. Pagination follows "
      "links.next. The OAI-PMH ListRecords on the same host answers in "
      "35-70 s, beyond the engine's 20 s budget, which is why the JSON "
      "API is registered instead." },

  { .id = "JP25_JPARCH_WEKO_NII", .name = "National Institute of Informatics repository search",
    .name_ja = "国立情報学研究所 機関リポジトリ",
    .category = "research", .portal = "https://repository.nii.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://repository.nii.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "WEKO3 records API of the National Institute of Informatics "
      "repository, on its own hostname rather than a JAIRO Cloud "
      "subdomain. Same record shape as the JAIRO Cloud rows: title, "
      "creators with affiliation, publisher, identifiers and file URLs "
      "under metadata; 20 records came back for the probe term and an "
      "impossible entity returned total 0. Paged through links.next." },

  { .id = "JP25_JPARCH_WEKO_TKFD", .name = "Tokyo Foundation for Policy Research repository search",
    .name_ja = "東京財団リポジトリ",
    .category = "research", .portal = "https://repo-tkfd.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://repo-tkfd.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "WEKO3 records API of the Tokyo Foundation for Policy Research "
      "repository, on its own hostname rather than a JAIRO Cloud "
      "subdomain. Same record shape as the JAIRO Cloud rows: title, "
      "creators with affiliation, publisher, identifiers and file URLs "
      "under metadata; 20 records came back for the probe term and an "
      "impossible entity returned total 0. Paged through links.next." },

  { .id = "JP25_JPARCH_WEKO_TOTTORI", .name = "Tottori University repository search",
    .name_ja = "鳥取大学研究成果リポジトリ",
    .category = "research", .portal = "https://repository.lib.tottori-u.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://repository.lib.tottori-u.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "WEKO3 records API of the Tottori University repository, on its "
      "own hostname rather than a JAIRO Cloud subdomain. Same record "
      "shape as the JAIRO Cloud rows: title, creators with affiliation, "
      "publisher, identifiers and file URLs under metadata; 20 records "
      "came back for the probe term and an impossible entity returned "
      "total 0. Paged through links.next." },

  { .id = "JP25_JPARCH_WEKO_UTOKYO", .name = "University of Tokyo (UTokyo Repository) repository search",
    .name_ja = "東京大学学術機関リポジトリ",
    .category = "research", .portal = "https://repository.dl.itc.u-tokyo.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"weko3\",\"academic\",\"thesis\",\"paper\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://repository.dl.itc.u-tokyo.ac.jp/api/records/?q={q}&size=100",
    .array_path = "hits.hits",
    .date_keys = "metadata.publish_date,updated",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "links.self",
    .next_path = "links.next",
    .page_size = 100,
    .timeout_ms = 120000,
    .title_keys = "metadata.title.0,metadata.title",
    .description = "WEKO3 records API of the University of Tokyo (UTokyo Repository) "
      "repository, on its own hostname rather than a JAIRO Cloud "
      "subdomain. Same record shape as the JAIRO Cloud rows: title, "
      "creators with affiliation, publisher, identifiers and file URLs "
      "under metadata; 20 records came back for the probe term and an "
      "impossible entity returned total 0. Paged through links.next." },

  { .id = "JP25_JPARCH_KURENAI_SEARCH", .name = "Kyoto University Research Information Repository (KURENAI) "
      "search",
    .name_ja = "京都大学学術情報リポジトリ KURENAI 検索",
    .category = "research", .portal = "https://repository.kulib.kyoto-u.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"dspace\",\"kyoto-university\",\"academic\",\"thesis\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://repository.kulib.kyoto-u.ac.jp/server/api/discover/search/objects?query={q}&size=100",
    .array_path = "_embedded.searchResult._embedded.objects",
    .date_keys = "_embedded.indexableObject.lastModified",
    .id_keys = "_embedded.indexableObject.uuid",
    .interval = 0,
    .link_keys = "_embedded.indexableObject.handle",
    .next_path = "_embedded.searchResult._links.next.href",
    .page_size = 100,
    .title_keys = "_embedded.indexableObject.name",
    .description = "DSpace 7 discovery API of Kyoto University's repository, 255,765 "
      "items including every Kyoto doctoral thesis since 2013. Each hit "
      "carries the item name, uuid, handle, lastModified and the full "
      "metadata block; totalElements was 76,167 for 法 and 0 for an "
      "impossible entity. Pagination via _links.next." },

  { .id = "JP25_JPARCH_KURENAI_OAI", .name = "KURENAI OAI-PMH record feed",
    .name_ja = "京都大学 KURENAI OAI-PMH",
    .category = "research", .portal = "https://repository.kulib.kyoto-u.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"repository\",\"oai-pmh\",\"kyoto-university\",\"academic\"",
    .mode = HP_XML, .want = HP_ANY, .free_tier = 1,
    .url = "https://repository.kulib.kyoto-u.ac.jp/dspace-oai/request?verb=ListRecords&metadataPrefix=oai_dc",
    .array_path = "record",
    .date_keys = "header.datestamp",
    .id_keys = "header.identifier",
    .interval = 86400,
    .next_path = "resumptionToken",
    .next_tmpl = "https://repository.kulib.kyoto-u.ac.jp/dspace-oai/request?verb=ListRecords&resumptionToken={v}",
    .page_max = 2600,
    .timeout_ms = 60000,
    .title_keys = "metadata.oai_dc:dc.dc:title",
    .description = "OAI-PMH ListRecords of the same repository in Dublin Core: 100 "
      "records per response with header.identifier, header.datestamp "
      "and dc:title/creator/date/identifier; completeListSize is "
      "255,765 and the response arrived in 2.2 s (the JAIRO Cloud OAI "
      "endpoints take 35-70 s, so only this one is registered). The "
      "walk follows the resumptionToken (`<resumptionToken "
      "completeListSize=\"255765\" "
      "cursor=\"0\">oai_dc////100</resumptionToken>`) through next_path "
      "+ next_tmpl; page_max covers the whole set at 100 per page, and "
      "an empty token ends the walk." },

  { .id = "JP25_JPARCH_WARP_SEARCH", .name = "NDL Web Archiving Project (WARP) full-text search",
    .name_ja = "国立国会図書館インターネット資料収集保存事業 WARP 検索",
    .category = "archive", .portal = "https://warp.ndl.go.jp",
    .record_type = "archived-website",
    .tags = "\"japan\",\"ndl\",\"warp\",\"web-archive\",\"defunct-sites\",\"government\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://warp.ndl.go.jp/search?keyword={q}&target=internet",
    .base = "https://warp.ndl.go.jp",
    .href_must = "/web/",
    .interval = 0,
    .description = "Search across the National Diet Library's web archive, which has "
      "crawled every Japanese ministry, prefecture, municipality, "
      "university and public corporation site since 2002 and holds "
      "pages that no longer exist on the live web. Each anchor is an "
      "archived site or page with its capture timestamp in the "
      "/web/<ts>/<url> path; the probe returned 44 archived-page links "
      "and an impossible keyword returned none. The page is "
      "server-rendered; only /web/ anchors are taken." },

  { .id = "JP25_JPARCH_CULTURAL_JP_SEARCH", .name = "Cultural Japan aggregated collection search",
    .name_ja = "カルチュラル・ジャパン 横断検索API",
    .category = "culture", .portal = "https://cultural.jp",
    .record_type = "cultural-object",
    .tags = "\"japan\",\"museum\",\"archive\",\"cultural-heritage\",\"iiif\",\"japan-search\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.cultural.jp/search?keyword={q}&size=100",
    .array_path = "hits.hits",
    .id_keys = "id",
    .interval = 0,
    .link_keys = "manifest.0",
    .page_param = "page",
    .page_size = 100,
    .page_start = 1,
    .title_keys = "title.ja.0,title.en.0,id",
    .description = "The Cultural Japan aggregator (NII/ROIS-DS) indexes ColBase, the "
      "national museums, Japan Search partners, Europeana, the Met, the "
      "Smithsonian and hundreds of Japanese local archives. Each hit "
      "carries the object id, Japanese and English title, agent, place, "
      "period, holding institution, source database and a IIIF "
      "manifest. 50 of 10,000 hits came back for 刀 and an impossible "
      "keyword returned total 0; page= advances (page 2 returned "
      "different ids) while from= is ignored." },

  { .id = "JP25_JPARCH_KOBE_DA_RECENT", .name = "Kobe University Digital Archive - recent items listing",
    .name_ja = "神戸大学附属図書館デジタルアーカイブ 新着一覧",
    .category = "research", .portal = "https://da.lib.kobe-u.ac.jp",
    .record_type = "repository-item",
    .tags = "\"japan\",\"kobe-university\",\"digital-archive\",\"repository\",\"recent\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://da.lib.kobe-u.ac.jp/da/kernel/",
    .href_must = "hdl.handle.net",
    .interval = 86400,
    .description = "Front listing of Kobe University's digital archive (Kernel), 185 "
      "item anchors pointing at hdl.handle.net handles with the item "
      "title as the anchor text. Registered as a BULK listing, not a "
      "pivot: the ?q= parameter is ignored (an impossible term returned "
      "the identical 186 handle links), so no per-entity claim can be "
      "made from it." },

  { .id = "JP25_JPARCH_CODH_PMJT_BOOKS", .name = "CODH Pre-Modern Japanese Text dataset - book index",
    .name_ja = "日本古典籍データセット 書誌一覧",
    .category = "archive", .portal = "https://codh.rois.ac.jp/pmjt/",
    .record_type = "digitised-book",
    .tags = "\"japan\",\"codh\",\"pre-modern\",\"classical-texts\",\"nijl\",\"iiif\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://codh.rois.ac.jp/pmjt/book/",
    .base = "https://codh.rois.ac.jp/pmjt/book/",
    .href_must = "2000",
    .interval = 86400,
    .description = "Index of every digitised pre-modern book in the ROIS-DS Center "
      "for Open Data in the Humanities dataset (NIJL holdings released "
      "CC BY-SA): 6,252 book anchors, one per work, each linking to a "
      "page with the title, author, era, IIIF manifest and full-text "
      "transcription where one exists. Static page, refreshed on "
      "release." },

  { .id = "JP25_JPARCH_JA_DBPEDIA_LABEL", .name = "Japanese DBpedia - entity label search (SPARQL)",
    .name_ja = "DBpedia Japanese ラベル検索",
    .category = "reference", .portal = "https://ja.dbpedia.org",
    .record_type = "knowledge-graph-entity",
    .tags = "\"japan\",\"dbpedia\",\"wikipedia\",\"sparql\",\"entity\",\"linked-data\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ja.dbpedia.org/sparql?query=SELECT%20%3Fs%20%3Fl%20WHERE%20%7B%3Fs%20rdfs%3Alabel%20%3Fl%20.%20%3Fl%20bif%3Acontains%20%22%27{q}*%27%22%7D%20LIMIT%20100&format=application%2Fsparql-results%2Bjson",
    .headers = { "Accept: application/sparql-results+json" },
    .array_path = "results.bindings",
    .id_keys = "s.value",
    .interval = 0,
    .link_keys = "s.value",
    .title_keys = "l.value",
    .description = "Virtuoso SPARQL endpoint over the Japanese Wikipedia extraction. "
      "A prefix full-text search on rdfs:label returns every ja.dbpedia "
      "resource whose label starts with the term, each with its "
      "resource URI, which is the pivot into the whole infobox graph "
      "(birth dates, affiliations, predecessors, subsidiaries) for "
      "people, companies and places. 100 bindings for トヨタ*, an empty "
      "binding list for an impossible term; the CONTAINS() form was "
      "rejected because its impossible-term query ran past 60 s." },

  { .id = "JP25_JPARCH_WIKISOURCE_JA_SEARCH", .name = "Japanese Wikisource full-text search",
    .name_ja = "ウィキソース日本語版 全文検索",
    .category = "archive", .portal = "https://ja.wikisource.org",
    .record_type = "historical-text",
    .tags = "\"japan\",\"wikisource\",\"historical-documents\",\"gazette\",\"laws\",\"transcription\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ja.wikisource.org/w/api.php?action=query&list=search&srsearch={q}&srlimit=100&format=json",
    .array_path = "query.search",
    .date_keys = "timestamp",
    .id_keys = "pageid",
    .interval = 0,
    .page_param = "sroffset",
    .page_size = 100,
    .page_start = 0,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "Transcribed public-domain Japanese texts: Meiji-Showa gazettes "
      "(官報), imperial ordinances, pre-war laws, court judgments, "
      "company charters, treaties and speeches. Each hit has the page "
      "title, pageid, wordcount, timestamp and a snippet; 50 of 2,179 "
      "hits for 官報 and totalhits 0 for an impossible term. Continuation "
      "through continue.sroffset." },

  { .id = "JP25_JPARCH_WIKISOURCE_JA_RECENT", .name = "Japanese Wikisource recent changes",
    .name_ja = "ウィキソース日本語版 最近の更新",
    .category = "archive", .portal = "https://ja.wikisource.org",
    .record_type = "historical-text",
    .tags = "\"japan\",\"wikisource\",\"recent-changes\",\"transcription\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ja.wikisource.org/w/api.php?action=query&list=recentchanges&rclimit=500&format=json",
    .array_path = "query.recentchanges",
    .date_keys = "timestamp",
    .id_keys = "rcid",
    .interval = 3600,
    .title_keys = "title",
    .description = "Every edit and new transcription on Japanese Wikisource with "
      "page title, revid, user, timestamp and type; 100 entries "
      "returned for the probe. Tracks newly transcribed historical "
      "documents as they appear." },

  { .id = "JP25_JPARCH_AOZORA_PERSON_ALL", .name = "Aozora Bunko index - all authors",
    .name_ja = "青空文庫 著者一覧（全て）",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "author-entry",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/person_all.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "person",
    .interval = 86400,
    .description = "Aozora Bunko's complete author index: 1330 author anchors, each "
      "to the person page listing that writer's works, birth and death "
      "dates and copyright status. Aozora is the canonical "
      "public-domain corpus of Japanese literature and its author "
      "records are the reference for dates of death that govern "
      "copyright expiry." },

  { .id = "JP25_JPARCH_AOZORA_SAKUHIN_A1", .name = "Aozora Bunko index - works: ア row page 1",
    .name_ja = "青空文庫 作品一覧 ア行 1",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "literary-work",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/sakuhin_a1.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "cards/",
    .interval = 86400,
    .description = "One page of Aozora Bunko's work index (作品一覧 ア行 1): 50 work "
      "anchors to card pages that carry title, author, first "
      "publication, source edition and the text files. The index is "
      "split by kana row and page, each a distinct static URL; only the "
      "first page of each row is registered here and the description of "
      "the page itself says so." },

  { .id = "JP25_JPARCH_AOZORA_SAKUHIN_KA1", .name = "Aozora Bunko index - works: カ row page 1",
    .name_ja = "青空文庫 作品一覧 カ行 1",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "literary-work",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/sakuhin_ka1.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "cards/",
    .interval = 86400,
    .description = "One page of Aozora Bunko's work index (作品一覧 カ行 1): 50 work "
      "anchors to card pages that carry title, author, first "
      "publication, source edition and the text files. The index is "
      "split by kana row and page, each a distinct static URL; only the "
      "first page of each row is registered here and the description of "
      "the page itself says so." },

  { .id = "JP25_JPARCH_AOZORA_SAKUHIN_SA1", .name = "Aozora Bunko index - works: サ row page 1",
    .name_ja = "青空文庫 作品一覧 サ行 1",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "literary-work",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/sakuhin_sa1.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "cards/",
    .interval = 86400,
    .description = "One page of Aozora Bunko's work index (作品一覧 サ行 1): 50 work "
      "anchors to card pages that carry title, author, first "
      "publication, source edition and the text files. The index is "
      "split by kana row and page, each a distinct static URL; only the "
      "first page of each row is registered here and the description of "
      "the page itself says so." },

  { .id = "JP25_JPARCH_AOZORA_SAKUHIN_TA1", .name = "Aozora Bunko index - works: タ row page 1",
    .name_ja = "青空文庫 作品一覧 タ行 1",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "literary-work",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/sakuhin_ta1.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "cards/",
    .interval = 86400,
    .description = "One page of Aozora Bunko's work index (作品一覧 タ行 1): 50 work "
      "anchors to card pages that carry title, author, first "
      "publication, source edition and the text files. The index is "
      "split by kana row and page, each a distinct static URL; only the "
      "first page of each row is registered here and the description of "
      "the page itself says so." },

  { .id = "JP25_JPARCH_AOZORA_SAKUHIN_NA1", .name = "Aozora Bunko index - works: ナ row page 1",
    .name_ja = "青空文庫 作品一覧 ナ行 1",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "literary-work",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/sakuhin_na1.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "cards/",
    .interval = 86400,
    .description = "One page of Aozora Bunko's work index (作品一覧 ナ行 1): 50 work "
      "anchors to card pages that carry title, author, first "
      "publication, source edition and the text files. The index is "
      "split by kana row and page, each a distinct static URL; only the "
      "first page of each row is registered here and the description of "
      "the page itself says so." },

  { .id = "JP25_JPARCH_AOZORA_SAKUHIN_HA1", .name = "Aozora Bunko index - works: ハ row page 1",
    .name_ja = "青空文庫 作品一覧 ハ行 1",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "literary-work",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/sakuhin_ha1.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "cards/",
    .interval = 86400,
    .description = "One page of Aozora Bunko's work index (作品一覧 ハ行 1): 50 work "
      "anchors to card pages that carry title, author, first "
      "publication, source edition and the text files. The index is "
      "split by kana row and page, each a distinct static URL; only the "
      "first page of each row is registered here and the description of "
      "the page itself says so." },

  { .id = "JP25_JPARCH_AOZORA_SAKUHIN_MA1", .name = "Aozora Bunko index - works: マ row page 1",
    .name_ja = "青空文庫 作品一覧 マ行 1",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "literary-work",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/sakuhin_ma1.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "cards/",
    .interval = 86400,
    .description = "One page of Aozora Bunko's work index (作品一覧 マ行 1): 50 work "
      "anchors to card pages that carry title, author, first "
      "publication, source edition and the text files. The index is "
      "split by kana row and page, each a distinct static URL; only the "
      "first page of each row is registered here and the description of "
      "the page itself says so." },

  { .id = "JP25_JPARCH_AOZORA_SAKUHIN_YA1", .name = "Aozora Bunko index - works: ヤ行 page 1",
    .name_ja = "青空文庫 作品一覧 ヤ行 1",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "literary-work",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/sakuhin_ya1.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "cards/",
    .interval = 86400,
    .description = "One page of Aozora Bunko's work index (作品一覧 ヤ行 1): 50 work "
      "anchors to card pages that carry title, author, first "
      "publication, source edition and the text files. The index is "
      "split by kana row and page, each a distinct static URL; only the "
      "first page of each row is registered here and the description of "
      "the page itself says so." },

  { .id = "JP25_JPARCH_AOZORA_SAKUHIN_RA1", .name = "Aozora Bunko index - works: ラ行 page 1",
    .name_ja = "青空文庫 作品一覧 ラ行 1",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "literary-work",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/sakuhin_ra1.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "cards/",
    .interval = 86400,
    .description = "One page of Aozora Bunko's work index (作品一覧 ラ行 1): 50 work "
      "anchors to card pages that carry title, author, first "
      "publication, source edition and the text files. The index is "
      "split by kana row and page, each a distinct static URL; only the "
      "first page of each row is registered here and the description of "
      "the page itself says so." },

  { .id = "JP25_JPARCH_AOZORA_SAKUHIN_WA1", .name = "Aozora Bunko index - works: ワ行 page 1",
    .name_ja = "青空文庫 作品一覧 ワ行 1",
    .category = "archive", .portal = "https://www.aozora.gr.jp",
    .record_type = "literary-work",
    .tags = "\"japan\",\"aozora-bunko\",\"public-domain\",\"literature\",\"authors\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.aozora.gr.jp/index_pages/sakuhin_wa1.html",
    .base = "https://www.aozora.gr.jp/index_pages/",
    .href_must = "cards/",
    .interval = 86400,
    .description = "One page of Aozora Bunko's work index (作品一覧 ワ行 1): 50 work "
      "anchors to card pages that carry title, author, first "
      "publication, source edition and the text files. The index is "
      "split by kana row and page, each a distinct static URL; only the "
      "first page of each row is registered here and the description of "
      "the page itself says so." },

  { .id = "JP25_JPARCH_CDX_GEOCITIES", .name = "Wayback CDX - Yahoo! Geocities Japan (geocities.jp) captures",
    .name_ja = "Wayback CDX Yahoo!ジオシティーズ geocities.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=geocities.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The largest Japanese free-homepage host, closed 31 March 2019, "
      "taking millions of personal, hobby, club and small-business "
      "pages with it. Captures are the only surviving copy. Records are "
      "Internet Archive CDX index rows (urlkey, timestamp, original "
      "URL, mimetype, status, digest, length) for captures of "
      "geocities.jp with HTTP 200, collapsed to one row per URL; the "
      "probe returned 200 rows at limit=200 in a single response; note "
      "that under load archive.org answered the same query in 20-60 s "
      "against the engine 20 s budget, so a scheduled run can miss and "
      "retry. The first row of the CDX JSON is the column-name header "
      "and will be emitted as one non-capture record, which the engine "
      "cannot skip. Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_GEOCITIESCO", .name = "Wayback CDX - Geocities Japan (geocities.co.jp) captures",
    .name_ja = "Wayback CDX ジオシティーズ geocities.co.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=geocities.co.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The pre-Yahoo Geocities Japan namespace (Playtown, SiliconValley "
      "etc. neighbourhoods) used from 1997 until the merge into "
      "geocities.jp. Records are Internet Archive CDX index rows "
      "(urlkey, timestamp, original URL, mimetype, status, digest, "
      "length) for captures of geocities.co.jp with HTTP 200, collapsed "
      "to one row per URL; the probe returned 200 rows at limit=200 in "
      "a single response; note that under load archive.org answered the "
      "same query in 20-60 s against the engine 20 s budget, so a "
      "scheduled run can miss and retry. The first row of the CDX JSON "
      "is the column-name header and will be emitted as one non-capture "
      "record, which the engine cannot skip. Bounded by limit and "
      "stated as such." },

  { .id = "JP25_JPARCH_CDX_NIFTY3", .name = "Wayback CDX - @nifty homepage3 captures",
    .name_ja = "Wayback CDX @nifty ホームページ homepage3",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=homepage3.nifty.com/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "One of the @nifty member homepage hosts (homepage1-3.nifty.com) "
      "retired in 2016; heavily used by professionals, clubs and small "
      "firms. Records are Internet Archive CDX index rows (urlkey, "
      "timestamp, original URL, mimetype, status, digest, length) for "
      "captures of homepage3.nifty.com with HTTP 200, collapsed to one "
      "row per URL; the probe returned 200 rows at limit=200 in a "
      "single response; note that under load archive.org answered the "
      "same query in 20-60 s against the engine 20 s budget, so a "
      "scheduled run can miss and retry. The first row of the CDX JSON "
      "is the column-name header and will be emitted as one non-capture "
      "record, which the engine cannot skip. Bounded by limit and "
      "stated as such." },

  { .id = "JP25_JPARCH_CDX_NIFTY1", .name = "Wayback CDX - @nifty homepage1 captures",
    .name_ja = "Wayback CDX @nifty ホームページ homepage1",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=homepage1.nifty.com/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The first @nifty member homepage host, retired 2016. Records are "
      "Internet Archive CDX index rows (urlkey, timestamp, original "
      "URL, mimetype, status, digest, length) for captures of "
      "homepage1.nifty.com with HTTP 200, collapsed to one row per URL; "
      "the probe returned 200 rows at limit=200 in a single response; "
      "note that under load archive.org answered the same query in "
      "20-60 s against the engine 20 s budget, so a scheduled run can "
      "miss and retry. The first row of the CDX JSON is the column-name "
      "header and will be emitted as one non-capture record, which the "
      "engine cannot skip. Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_YAHOOBLOG", .name = "Wayback CDX - Yahoo! Blog Japan captures",
    .name_ja = "Wayback CDX Yahoo!ブログ",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=blogs.yahoo.co.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "Yahoo! Japan's blog service, closed December 2019; the archive "
      "is the only record of its posts. Records are Internet Archive "
      "CDX index rows (urlkey, timestamp, original URL, mimetype, "
      "status, digest, length) for captures of blogs.yahoo.co.jp with "
      "HTTP 200, collapsed to one row per URL; the probe returned 200 "
      "rows at limit=200 in a single response; note that under load "
      "archive.org answered the same query in 20-60 s against the "
      "engine 20 s budget, so a scheduled run can miss and retry. The "
      "first row of the CDX JSON is the column-name header and will be "
      "emitted as one non-capture record, which the engine cannot skip. "
      "Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_HATENA", .name = "Wayback CDX - Hatena Diary captures",
    .name_ja = "Wayback CDX はてなダイアリー",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=d.hatena.ne.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "Hatena Diary closed in 2019; many technical and political blogs "
      "of the 2000s existed only there. Records are Internet Archive "
      "CDX index rows (urlkey, timestamp, original URL, mimetype, "
      "status, digest, length) for captures of d.hatena.ne.jp with HTTP "
      "200, collapsed to one row per URL; the probe returned 200 rows "
      "at limit=200 in a single response; note that under load "
      "archive.org answered the same query in 20-60 s against the "
      "engine 20 s budget, so a scheduled run can miss and retry. The "
      "first row of the CDX JSON is the column-name header and will be "
      "emitted as one non-capture record, which the engine cannot skip. "
      "Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_GOO", .name = "Wayback CDX - goo blog captures",
    .name_ja = "Wayback CDX gooブログ",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=blog.goo.ne.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "NTT Resonant's goo blog, announced for closure in 2025, hosting "
      "two decades of personal and local-politics blogs. Records are "
      "Internet Archive CDX index rows (urlkey, timestamp, original "
      "URL, mimetype, status, digest, length) for captures of "
      "blog.goo.ne.jp with HTTP 200, collapsed to one row per URL; the "
      "probe returned 200 rows at limit=200 in a single response; note "
      "that under load archive.org answered the same query in 20-60 s "
      "against the engine 20 s budget, so a scheduled run can miss and "
      "retry. The first row of the CDX JSON is the column-name header "
      "and will be emitted as one non-capture record, which the engine "
      "cannot skip. Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_LINEBLOG", .name = "Wayback CDX - LINE BLOG captures",
    .name_ja = "Wayback CDX LINE BLOG",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=lineblog.me/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "Celebrity and influencer blog platform closed June 2023. Records "
      "are Internet Archive CDX index rows (urlkey, timestamp, original "
      "URL, mimetype, status, digest, length) for captures of "
      "lineblog.me with HTTP 200, collapsed to one row per URL; the "
      "probe returned 200 rows at limit=200 in a single response; note "
      "that under load archive.org answered the same query in 20-60 s "
      "against the engine 20 s budget, so a scheduled run can miss and "
      "retry. The first row of the CDX JSON is the column-name header "
      "and will be emitted as one non-capture record, which the engine "
      "cannot skip. Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_MELMA", .name = "Wayback CDX - melma! mailing-list archive captures",
    .name_ja = "Wayback CDX メルマ!",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=melma.com/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "Public archive of a mail-magazine service closed in 2020; "
      "contains newsletters of small firms, investment tipsters and "
      "political groups. Records are Internet Archive CDX index rows "
      "(urlkey, timestamp, original URL, mimetype, status, digest, "
      "length) for captures of melma.com with HTTP 200, collapsed to "
      "one row per URL; the probe returned 200 rows at limit=200 in a "
      "single response; note that under load archive.org answered the "
      "same query in 20-60 s against the engine 20 s budget, so a "
      "scheduled run can miss and retry. The first row of the CDX JSON "
      "is the column-name header and will be emitted as one non-capture "
      "record, which the engine cannot skip. Bounded by limit and "
      "stated as such." },

  { .id = "JP25_JPARCH_CDX_MIMIZUN", .name = "Wayback CDX - mimizun 2ch log mirror captures",
    .name_ja = "Wayback CDX みみずん 2ちゃんねる過去ログ",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=mimizun.com/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "A long-running mirror of 2channel past logs whose own search is "
      "a Shift_JIS form the engine cannot drive; the CDX index lists "
      "its archived thread pages. Records are Internet Archive CDX "
      "index rows (urlkey, timestamp, original URL, mimetype, status, "
      "digest, length) for captures of mimizun.com with HTTP 200, "
      "collapsed to one row per URL; the probe returned 200 rows at "
      "limit=200 in a single response; note that under load archive.org "
      "answered the same query in 20-60 s against the engine 20 s "
      "budget, so a scheduled run can miss and retry. The first row of "
      "the CDX JSON is the column-name header and will be emitted as "
      "one non-capture record, which the engine cannot skip. Bounded by "
      "limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_JUGEM", .name = "Wayback CDX - JUGEM blog captures",
    .name_ja = "Wayback CDX JUGEMブログ",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=*.jugem.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "GMO Pepabo's blog service; wildcard over user subdomains. "
      "Records are Internet Archive CDX index rows (urlkey, timestamp, "
      "original URL, mimetype, status, digest, length) for captures of "
      "*.jugem.jp with HTTP 200, collapsed to one row per URL; the "
      "probe returned 200 rows at limit=200 in a single response; note "
      "that under load archive.org answered the same query in 20-60 s "
      "against the engine 20 s budget, so a scheduled run can miss and "
      "retry. The first row of the CDX JSON is the column-name header "
      "and will be emitted as one non-capture record, which the engine "
      "cannot skip. Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_SONET", .name = "Wayback CDX - So-net blog captures",
    .name_ja = "Wayback CDX So-netブログ",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=*.blog.so-net.ne.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "Sony's So-net blog namespace before the 2019 rename to SS-blog; "
      "old URLs no longer resolve. Records are Internet Archive CDX "
      "index rows (urlkey, timestamp, original URL, mimetype, status, "
      "digest, length) for captures of *.blog.so-net.ne.jp with HTTP "
      "200, collapsed to one row per URL; the probe returned 200 rows "
      "at limit=200 in a single response; note that under load "
      "archive.org answered the same query in 20-60 s against the "
      "engine 20 s budget, so a scheduled run can miss and retry. The "
      "first row of the CDX JSON is the column-name header and will be "
      "emitted as one non-capture record, which the engine cannot skip. "
      "Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_MITI", .name = "Wayback CDX - Ministry of International Trade and Industry "
      "(MITI) site captures",
    .name_ja = "Wayback CDX 通商産業省 miti.go.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"government-old-domain\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=miti.go.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The pre-2001 ministry domain; policy papers and industry notices "
      "that were not migrated to meti.go.jp. Records are Internet "
      "Archive CDX index rows (urlkey, timestamp, original URL, "
      "mimetype, status, digest, length) for captures of miti.go.jp "
      "with HTTP 200, collapsed to one row per URL; the probe returned "
      "200 rows at limit=200 in a single response; note that under load "
      "archive.org answered the same query in 20-60 s against the "
      "engine 20 s budget, so a scheduled run can miss and retry. The "
      "first row of the CDX JSON is the column-name header and will be "
      "emitted as one non-capture record, which the engine cannot skip. "
      "Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_MHW", .name = "Wayback CDX - Ministry of Health and Welfare site captures",
    .name_ja = "Wayback CDX 厚生省 mhw.go.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"government-old-domain\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=mhw.go.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The pre-2001 domain of the health ministry before the merger "
      "into MHLW. Records are Internet Archive CDX index rows (urlkey, "
      "timestamp, original URL, mimetype, status, digest, length) for "
      "captures of mhw.go.jp with HTTP 200, collapsed to one row per "
      "URL; the probe returned 200 rows at limit=200 in a single "
      "response; note that under load archive.org answered the same "
      "query in 20-60 s against the engine 20 s budget, so a scheduled "
      "run can miss and retry. The first row of the CDX JSON is the "
      "column-name header and will be emitted as one non-capture "
      "record, which the engine cannot skip. Bounded by limit and "
      "stated as such." },

  { .id = "JP25_JPARCH_CDX_MOC", .name = "Wayback CDX - Ministry of Construction site captures",
    .name_ja = "Wayback CDX 建設省 moc.go.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"government-old-domain\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=moc.go.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The pre-2001 construction ministry domain, later MLIT. Records "
      "are Internet Archive CDX index rows (urlkey, timestamp, original "
      "URL, mimetype, status, digest, length) for captures of moc.go.jp "
      "with HTTP 200, collapsed to one row per URL; the probe returned "
      "200 rows at limit=200 in a single response; note that under load "
      "archive.org answered the same query in 20-60 s against the "
      "engine 20 s budget, so a scheduled run can miss and retry. The "
      "first row of the CDX JSON is the column-name header and will be "
      "emitted as one non-capture record, which the engine cannot skip. "
      "Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_STA", .name = "Wayback CDX - Science and Technology Agency site captures",
    .name_ja = "Wayback CDX 科学技術庁 sta.go.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"government-old-domain\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=sta.go.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The pre-2001 agency domain, later merged into MEXT. Records are "
      "Internet Archive CDX index rows (urlkey, timestamp, original "
      "URL, mimetype, status, digest, length) for captures of sta.go.jp "
      "with HTTP 200, collapsed to one row per URL; the probe returned "
      "200 rows at limit=200 in a single response; note that under load "
      "archive.org answered the same query in 20-60 s against the "
      "engine 20 s budget, so a scheduled run can miss and retry. The "
      "first row of the CDX JSON is the column-name header and will be "
      "emitted as one non-capture record, which the engine cannot skip. "
      "Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_JDA", .name = "Wayback CDX - Japan Defense Agency site captures",
    .name_ja = "Wayback CDX 防衛庁 jda.go.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"government-old-domain\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=jda.go.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The Defense Agency domain before it became the Ministry of "
      "Defense (mod.go.jp) in 2007. Records are Internet Archive CDX "
      "index rows (urlkey, timestamp, original URL, mimetype, status, "
      "digest, length) for captures of jda.go.jp with HTTP 200, "
      "collapsed to one row per URL; the probe returned 200 rows at "
      "limit=200 in a single response; note that under load archive.org "
      "answered the same query in 20-60 s against the engine 20 s "
      "budget, so a scheduled run can miss and retry. The first row of "
      "the CDX JSON is the column-name header and will be emitted as "
      "one non-capture record, which the engine cannot skip. Bounded by "
      "limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_EPA", .name = "Wayback CDX - Economic Planning Agency site captures",
    .name_ja = "Wayback CDX 経済企画庁 epa.go.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"government-old-domain\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=epa.go.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The pre-2001 domain of the Economic Planning Agency, absorbed "
      "into the Cabinet Office. Records are Internet Archive CDX index "
      "rows (urlkey, timestamp, original URL, mimetype, status, digest, "
      "length) for captures of epa.go.jp with HTTP 200, collapsed to "
      "one row per URL; the probe returned 200 rows at limit=200 in a "
      "single response; note that under load archive.org answered the "
      "same query in 20-60 s against the engine 20 s budget, so a "
      "scheduled run can miss and retry. The first row of the CDX JSON "
      "is the column-name header and will be emitted as one non-capture "
      "record, which the engine cannot skip. Bounded by limit and "
      "stated as such." },

  { .id = "JP25_JPARCH_CDX_MONBU", .name = "Wayback CDX - Ministry of Education (Monbusho) site captures",
    .name_ja = "Wayback CDX 文部省 monbu.go.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"government-old-domain\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=monbu.go.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The pre-2001 domain of the education ministry. Records are "
      "Internet Archive CDX index rows (urlkey, timestamp, original "
      "URL, mimetype, status, digest, length) for captures of "
      "monbu.go.jp with HTTP 200, collapsed to one row per URL; the "
      "probe returned 200 rows at limit=200 in a single response; note "
      "that under load archive.org answered the same query in 20-60 s "
      "against the engine 20 s budget, so a scheduled run can miss and "
      "retry. The first row of the CDX JSON is the column-name header "
      "and will be emitted as one non-capture record, which the engine "
      "cannot skip. Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_MHA", .name = "Wayback CDX - Ministry of Home Affairs site captures",
    .name_ja = "Wayback CDX 自治省 mha.go.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"government-old-domain\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=mha.go.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The pre-2001 domain of the home affairs ministry, later MIC. "
      "Records are Internet Archive CDX index rows (urlkey, timestamp, "
      "original URL, mimetype, status, digest, length) for captures of "
      "mha.go.jp with HTTP 200, collapsed to one row per URL; the probe "
      "returned 200 rows at limit=200 in a single response; note that "
      "under load archive.org answered the same query in 20-60 s "
      "against the engine 20 s budget, so a scheduled run can miss and "
      "retry. The first row of the CDX JSON is the column-name header "
      "and will be emitted as one non-capture record, which the engine "
      "cannot skip. Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_SORIFU", .name = "Wayback CDX - Prime Minister's Office (Sorifu) site captures",
    .name_ja = "Wayback CDX 総理府 sorifu.go.jp",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"government-old-domain\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=sorifu.go.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The pre-2001 Prime Minister's Office domain, replaced by "
      "cao.go.jp. Records are Internet Archive CDX index rows (urlkey, "
      "timestamp, original URL, mimetype, status, digest, length) for "
      "captures of sorifu.go.jp with HTTP 200, collapsed to one row per "
      "URL; the probe returned 200 rows at limit=200 in a single "
      "response; note that under load archive.org answered the same "
      "query in 20-60 s against the engine 20 s budget, so a scheduled "
      "run can miss and retry. The first row of the CDX JSON is the "
      "column-name header and will be emitted as one non-capture "
      "record, which the engine cannot skip. Bounded by limit and "
      "stated as such." },

  { .id = "JP25_JPARCH_CDX_MTGOX", .name = "Wayback CDX - Mt. Gox site captures",
    .name_ja = "Wayback CDX マウントゴックス",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=mtgox.com/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The Tokyo bitcoin exchange that collapsed in 2014; its pages, "
      "notices and press releases exist only in the archive. Records "
      "are Internet Archive CDX index rows (urlkey, timestamp, original "
      "URL, mimetype, status, digest, length) for captures of mtgox.com "
      "with HTTP 200, collapsed to one row per URL; the probe returned "
      "200 rows at limit=200 in a single response; note that under load "
      "archive.org answered the same query in 20-60 s against the "
      "engine 20 s budget, so a scheduled run can miss and retry. The "
      "first row of the CDX JSON is the column-name header and will be "
      "emitted as one non-capture record, which the engine cannot skip. "
      "Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_TAKEFUJI", .name = "Wayback CDX - Takefuji site captures",
    .name_ja = "Wayback CDX 武富士",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=takefuji.co.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The consumer lender that went bankrupt in 2010; corporate, IR "
      "and branch pages. Records are Internet Archive CDX index rows "
      "(urlkey, timestamp, original URL, mimetype, status, digest, "
      "length) for captures of takefuji.co.jp with HTTP 200, collapsed "
      "to one row per URL; the probe returned 200 rows at limit=200 in "
      "a single response; note that under load archive.org answered the "
      "same query in 20-60 s against the engine 20 s budget, so a "
      "scheduled run can miss and retry. The first row of the CDX JSON "
      "is the column-name header and will be emitted as one non-capture "
      "record, which the engine cannot skip. Bounded by limit and "
      "stated as such." },

  { .id = "JP25_JPARCH_CDX_YAMAICHI", .name = "Wayback CDX - Yamaichi Securities site captures",
    .name_ja = "Wayback CDX 山一證券",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=yamaichi.co.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The securities house that failed in 1997; among the earliest "
      "Japanese corporate sites in the archive. Records are Internet "
      "Archive CDX index rows (urlkey, timestamp, original URL, "
      "mimetype, status, digest, length) for captures of yamaichi.co.jp "
      "with HTTP 200, collapsed to one row per URL; the probe returned "
      "200 rows at limit=200 in a single response; note that under load "
      "archive.org answered the same query in 20-60 s against the "
      "engine 20 s budget, so a scheduled run can miss and retry. The "
      "first row of the CDX JSON is the column-name header and will be "
      "emitted as one non-capture record, which the engine cannot skip. "
      "Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_LTCB", .name = "Wayback CDX - Long-Term Credit Bank of Japan site captures",
    .name_ja = "Wayback CDX 日本長期信用銀行",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=ltcb.co.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "Nationalised in 1998; its site preceded the Shinsei Bank "
      "rebrand. Records are Internet Archive CDX index rows (urlkey, "
      "timestamp, original URL, mimetype, status, digest, length) for "
      "captures of ltcb.co.jp with HTTP 200, collapsed to one row per "
      "URL; the probe returned 200 rows at limit=200 in a single "
      "response; note that under load archive.org answered the same "
      "query in 20-60 s against the engine 20 s budget, so a scheduled "
      "run can miss and retry. The first row of the CDX JSON is the "
      "column-name header and will be emitted as one non-capture "
      "record, which the engine cannot skip. Bounded by limit and "
      "stated as such." },

  { .id = "JP25_JPARCH_CDX_JAPANLIFE", .name = "Wayback CDX - Japan Life site captures",
    .name_ja = "Wayback CDX ジャパンライフ",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=japanlife.co.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The magnetic-mattress rental scheme that collapsed in 2017 and "
      "became a criminal fraud case; its promotional pages are "
      "evidence. Records are Internet Archive CDX index rows (urlkey, "
      "timestamp, original URL, mimetype, status, digest, length) for "
      "captures of japanlife.co.jp with HTTP 200, collapsed to one row "
      "per URL; the probe returned 200 rows at limit=200 in a single "
      "response; note that under load archive.org answered the same "
      "query in 20-60 s against the engine 20 s budget, so a scheduled "
      "run can miss and retry. The first row of the CDX JSON is the "
      "column-name header and will be emitted as one non-capture "
      "record, which the engine cannot skip. Bounded by limit and "
      "stated as such." },

  { .id = "JP25_JPARCH_CDX_AGURA", .name = "Wayback CDX - Agura Bokujo site captures",
    .name_ja = "Wayback CDX 安愚楽牧場",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=agura-bokujo.co.jp/*&limit=200&filter=statuscode:200&collapse=urlkey",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The cattle-ownership investment scheme that failed in 2011 with "
      "4,200 oku yen of investor losses. Records are Internet Archive "
      "CDX index rows (urlkey, timestamp, original URL, mimetype, "
      "status, digest, length) for captures of agura-bokujo.co.jp with "
      "HTTP 200, collapsed to one row per URL; the probe returned 200 "
      "rows at limit=200 in a single response; note that under load "
      "archive.org answered the same query in 20-60 s against the "
      "engine 20 s budget, so a scheduled run can miss and retry. The "
      "first row of the CDX JSON is the column-name header and will be "
      "emitted as one non-capture record, which the engine cannot skip. "
      "Bounded by limit and stated as such." },

  { .id = "JP25_JPARCH_CDX_TEPCO_FUKUSHIMA", .name = "Wayback CDX - TEPCO Fukushima Daiichi pages captures (to 2012)",
    .name_ja = "Wayback CDX 東京電力 福島第一原子力発電所ページ",
    .category = "archive", .portal = "https://web.archive.org",
    .record_type = "wayback-capture",
    .tags = "\"japan\",\"wayback\",\"cdx\",\"defunct-site\",\"web-archive\",\"forgotten-site\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://web.archive.org/cdx/search/cdx?url=tepco.co.jp/nu/fukushima-np/*&limit=200&filter=statuscode:200&collapse=urlkey&to=2012",
    .csv_delim = "ws",
    .csv_no_header = 1,
    .date_keys = "col1",
    .id_keys = "col5",
    .interval = 86400,
    .link_keys = "col2",
    .max_items = 200,
    .timeout_ms = 180000,
    .title_keys = "col2",
    .description = "The plant's own pages as they stood before and during the March "
      "2011 accident, bounded to captures up to 2012. Records are "
      "Internet Archive CDX index rows (urlkey, timestamp, original "
      "URL, mimetype, status, digest, length) for captures of "
      "tepco.co.jp/nu/fukushima-np/* with HTTP 200, collapsed to one "
      "row per URL; the probe returned 200 rows at limit=200 in a "
      "single response; note that under load archive.org answered the "
      "same query in 20-60 s against the engine 20 s budget, so a "
      "scheduled run can miss and retry. The first row of the CDX JSON "
      "is the column-name header and will be emitted as one non-capture "
      "record, which the engine cannot skip. Bounded by limit and "
      "stated as such." },
};

HP_REGISTER_TABLE(HP3B25_JPARCHIVE)
