/* collectors/pivot/table/hp3b25_jprecovered.c -- batch 25: jprecovered
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch25.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch25.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch25.jprecovered.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3B25_JPRECOVERED[] = {
  { .id = "JP25_JPREC_JPNIC_AS_NUMBERS", .name = "JPNIC assigned AS numbers",
    .name_ja = "JPNIC AS番号割り当て一覧",
    .category = "infrastructure", .portal = "https://www.nic.ad.jp",
    .record_type = "as-assignment",
    .tags = "\"japan\",\"jpnic\",\"asn\",\"network\",\"registry\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.nic.ad.jp/ja/ip/as-numbers.txt",
    .body_keys = "連絡先",
    .csv_delim = "ws",
    .csv_skip_lines = 3,
    .id_keys = "AS番号",
    .interval = 86400,
    .title_keys = "AS名",
    .description = "JPNIC's own list of every AS number it has assigned (1,052 lines "
      "at probe time): the AS number, the AS name and the JPNIC contact "
      "handle of the holder, refreshed daily and dated in the title. A "
      "whitespace-aligned text table with three title lines and a ruler "
      "above and below the header; an AS that is allocated but not "
      "assigned appears as the number alone with a trailing asterisk "
      "and is kept as such. UTF-8. Needed csv_delim=ws and "
      "csv_skip_lines=3." },

  { .id = "JP25_JPREC_YAMAHA_RT_SECURITY", .name = "Yamaha RT router security advisories",
    .name_ja = "ヤマハ ルーター セキュリティ情報",
    .category = "security", .portal = "https://www.rtpro.yamaha.co.jp",
    .record_type = "security-advisory",
    .tags = "\"japan\",\"yamaha\",\"router\",\"advisory\",\"cve\",\"jvn\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.rtpro.yamaha.co.jp/RT/FAQ/Security/index.html",
    .href_must = ".html",
    .interval = 86400,
    .description = "Yamaha's own advisory index for its RT/RTX/NVR/UTX network "
      "products: 20+ advisories, each a bare relative href "
      "(CVE-2026-50751.html, JVNVU99896362.html) whose anchor text is "
      "the advisory title naming the affected product and the "
      "vulnerability. Shift_JIS on a .jp host, transcoded "
      "automatically. Needed directory-relative href resolution." },

  { .id = "JP25_JPREC_ECCUBE_WEAKNESS", .name = "EC-CUBE vulnerability advisories",
    .name_ja = "EC-CUBE 脆弱性情報",
    .category = "security", .portal = "https://www.ec-cube.net",
    .record_type = "security-advisory",
    .tags = "\"japan\",\"ec-cube\",\"ecommerce\",\"advisory\",\"vulnerability\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.ec-cube.net/info/weakness/",
    .href_must = "weakness.php?id=",
    .interval = 86400,
    .page_max = 20,
    .page_param = "pg_no",
    .description = "The EC-CUBE project's register of vulnerabilities in the "
      "platform that runs a large share of Japanese web shops: 20 "
      "advisories per page as ./weakness.php?id=N hrefs with the "
      "advisory title as anchor text, paged by ./?pg_no=N (4 pages at "
      "probe time). Needed directory-relative href resolution and the "
      "pg_no page parameter." },

  { .id = "JP25_JPREC_KANPOUAI_TOSAN_PAGED", .name = "kanpou.ai insolvency notices, every page",
    .name_ja = "官報 倒産情報 (kanpou.ai) 全ページ",
    .category = "business", .portal = "https://kanpou.ai",
    .record_type = "insolvency-notice",
    .tags = "\"japan\",\"kanpou\",\"bankruptcy\",\"insolvency\",\"houjin-bangou\"",
    .mode = HP_HTML, .want = HP_ANY, .free_tier = 1,
    .url = "https://kanpou.ai/tosan/p/{page}",
    .href_must = "/company/",
    .interval = 86400,
    .page_max = 500,
    .page_start = 2,
    .description = "The same privately maintained index of 破産手続開始, 民事再生 and 特別清算 "
      "notices from the Kanpo that JP25_JPBIZ_KANPOUAI_TOSAN reads page "
      "1 of, walked through every page: /tosan/p/N serves 50 companies "
      "per page (p/1 is a 308 to /tosan; p/300 still answers, p/1000 is "
      "404), each anchor /company/<13-digit 法人番号>. Needed {page} "
      "path-segment paging; the walk ends at the first page the site "
      "does not serve. page_start=2 because /tosan/p/1 is a 308 to "
      "/tosan that core/httpclient reports as status 308 rather than "
      "following, and page 1 IS /tosan, which JP25_JPBIZ_KANPOUAI_TOSAN "
      "reads every run — the two rows together are the whole "
      "collection." },
};

HP_REGISTER_TABLE(HP3B25_JPRECOVERED)
