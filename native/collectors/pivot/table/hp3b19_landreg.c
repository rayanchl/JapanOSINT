/* collectors/pivot/table/hp3b19_landreg.c -- batch 19: landreg
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch19.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch19.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch19.landreg.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3B19_LANDREG[] = {
  { .id = "UK_LAND_REGISTRY_PPI", .name = "HM Land Registry — Price Paid transaction records",
    .name_ja = "英国土地登記 取引価格記録",
    .category = "corporate", .portal = "https://landregistry.data.gov.uk",
    .record_type = "property-transaction",
    .tags = "\"property\",\"land\",\"uk\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://landregistry.data.gov.uk/data/ppi/transaction-record.json?_pageSize=100",
    .array_path = "result.items",
    .interval = 86400,
    .page_param = "_page",
    .page_start = 0,
    .page_zero_based = 1,
    .description = "Every residential property sale registered in England and Wales "
      "as linked data: the price paid, the transaction and transfer "
      "dates, the full address down to PAON and postcode, whether the "
      "property is freehold or leasehold, new-build status, and the "
      "property type. The Land Registry publishes this as a legal "
      "record of transfer, so a price here is what was actually paid "
      "rather than what was asked" },

  { .id = "UK_LAND_REGISTRY_TRANSACTION_COUNT", .name = "HM Land Registry — regional transaction counts",
    .name_ja = "英国土地登記 地域別取引件数",
    .category = "economy", .portal = "https://landregistry.data.gov.uk",
    .record_type = "property-statistic",
    .tags = "\"property\",\"land\",\"uk\",\"statistics\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://landregistry.data.gov.uk/data/ukhpi/region/united-kingdom/month/2024-01.json",
    .interval = 86400,
    .description = "The UK House Price Index for a region and month with the average "
      "price, the index value, the monthly and annual change, and the "
      "breakdown by property type, buyer status and funding. The "
      "official index that mortgage and policy decisions are keyed to" },

  { .id = "FR_IGN_CADASTRE_PARCELS", .name = "IGN APIcarto — French cadastral parcels",
    .name_ja = "IGN 地籍区画",
    .category = "corporate", .portal = "https://apicarto.ign.fr",
    .record_type = "cadastral-parcel",
    .tags = "\"property\",\"land\",\"france\",\"cadastre\",\"geo\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://apicarto.ign.fr/api/cadastre/parcelle?code_insee={qd}&section=0D",
    .array_path = "features",
    .interval = 86400,
    .description = "Individual cadastral parcels for a French commune, returned as "
      "GeoJSON with the parcel's full geometry, its cadastral "
      "reference, section, sheet number, surface area and the commune "
      "code. The parcel reference is the key that links a physical "
      "piece of ground to the ownership record held by the tax "
      "administration" },

  { .id = "FR_IGN_CADASTRE_COMMUNE", .name = "IGN APIcarto — French commune cadastral boundaries",
    .name_ja = "IGN コミューン地籍境界",
    .category = "corporate", .portal = "https://apicarto.ign.fr",
    .record_type = "cadastral-boundary",
    .tags = "\"property\",\"land\",\"france\",\"cadastre\",\"geo\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://apicarto.ign.fr/api/cadastre/commune?code_insee={qd}",
    .array_path = "features",
    .interval = 86400,
    .description = "The cadastral boundary of a French commune as GeoJSON with its "
      "INSEE code and name. The administrative frame every French "
      "parcel, address and statistical series is indexed against" },

  { .id = "NL_PDOK_LOCATIESERVER", .name = "PDOK Locatieserver — Dutch address and place search",
    .name_ja = "オランダ 住所・地名検索",
    .category = "reference", .portal = "https://api.pdok.nl",
    .record_type = "address-record",
    .tags = "\"property\",\"address\",\"netherlands\",\"geo\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.pdok.nl/bzk/locatieserver/search/v3_1/free?q={q}&rows=100",
    .array_path = "response.docs",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_start = 0,
    .page_zero_based = 1,
    .description = "The Dutch national address and place register: addresses, "
      "postcodes, streets, neighbourhoods, municipalities and cadastral "
      "parcels, each with its official identifier, centroid and the "
      "administrative hierarchy above it. BAG identifiers returned here "
      "resolve to the building register, which carries construction "
      "year, use class and floor area" },

  { .id = "NO_GEONORGE_ADDRESSES", .name = "Geonorge — Norwegian address register",
    .name_ja = "ノルウェー 住所登録",
    .category = "reference", .portal = "https://ws.geonorge.no",
    .record_type = "address-record",
    .tags = "\"property\",\"address\",\"norway\",\"geo\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ws.geonorge.no/adresser/v1/sok?sok={q}&treffPerSide=100",
    .array_path = "adresser",
    .interval = 86400,
    .page_param = "side",
    .page_size = 100,
    .page_start = 0,
    .page_zero_based = 1,
    .description = "The Norwegian official address register with each address's "
      "cadastral identifiers — municipality, holding, sub-holding and "
      "section numbers — plus coordinates in two reference systems, the "
      "postal area and the electoral district. The cadastral quad is "
      "the key to Norway's property ownership record, which is itself "
      "open" },

  { .id = "NO_GEONORGE_ADDRESS_POINT", .name = "Geonorge — Norwegian address by coordinate",
    .name_ja = "ノルウェー 座標逆引き住所",
    .category = "reference", .portal = "https://ws.geonorge.no",
    .record_type = "address-record",
    .tags = "\"property\",\"address\",\"norway\",\"geo\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ws.geonorge.no/adresser/v1/punktsok?lat=59.91&lon=10.75&radius=500&treffPerSide=100",
    .array_path = "adresser",
    .interval = 86400,
    .description = "Reverse lookup from a coordinate to every registered Norwegian "
      "address within a radius, with the cadastral identifiers and the "
      "distance to each. Turns an observed position — from imagery, a "
      "photograph or a device — into the specific properties it falls "
      "within" },

  { .id = "ES_CATASTRO_PROVINCIAS", .name = "Spanish Catastro — provincial reference service",
    .name_ja = "スペイン地籍 県別参照",
    .category = "corporate", .portal = "https://www.catastro.meh.es",
    .record_type = "cadastral-reference",
    .tags = "\"property\",\"land\",\"spain\",\"cadastre\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ovc.catastro.meh.es/ovcservweb/OVCSWLocalizacionRC/OVCCallejero.asmx/ConsultaProvincia",
    .interval = 604800,
    .description = "The Spanish cadastre's province list in the tax administration's "
      "own XML, the entry point to its street, parcel and reference "
      "lookups. Spain's cadastre publishes parcel-level surface, use "
      "and construction year without charge, which makes it one of the "
      "more open property records in Europe" },
};

HP_REGISTER_TABLE(HP3B19_LANDREG)
