/* collectors/pivot/table/hp3b19_cyber.c -- batch 19: cyber
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch19.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch19.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch19.cyber.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3B19_CYBER[] = {
  { .id = "UBUNTU_CVE_BY_PACKAGE", .name = "Ubuntu Security — CVEs affecting a source package",
    .name_ja = "Ubuntu セキュリティ パッケージ別CVE",
    .category = "threat", .portal = "https://ubuntu.com/security",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"linux\",\"ubuntu\",\"package\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ubuntu.com/security/cves.json?package={q}&limit=20",
    .array_path = "cves",
    .detail_key = "id",
    .detail_url = "https://ubuntu.com/security/cves/{v}.json",
    .page_param = "offset",
    .page_size = 20,
    .description = "Every CVE Canonical tracks against one source package, each with "
      "the per-release fix status, the CVSS vector, the assigned "
      "priority and the upstream references. The per-release status is "
      "what tells you whether a running LTS machine is actually "
      "exposed, which the raw NVD entry never says" },

  { .id = "UBUNTU_CVE_DETAIL", .name = "Ubuntu Security — CVE record",
    .name_ja = "Ubuntu セキュリティ CVE詳細",
    .category = "threat", .portal = "https://ubuntu.com/security",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"linux\",\"ubuntu\",\"cve\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ubuntu.com/security/cves/{qU}.json",
    .body_keys = "description",
    .title_keys = "id",
    .description = "The full Canonical assessment of one CVE: affected source "
      "packages, per-series status and fix version, priority, notes "
      "from the security team, patch links and the USNs that shipped "
      "the fix. This is the record behind the list, and it names the "
      "exact package version that closes the hole" },

  { .id = "UBUNTU_USN_BY_CVE", .name = "Ubuntu Security Notices by CVE",
    .name_ja = "Ubuntu セキュリティ通知",
    .category = "threat", .portal = "https://ubuntu.com/security/notices",
    .record_type = "advisory-record",
    .tags = "\"advisory\",\"linux\",\"ubuntu\",\"usn\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ubuntu.com/security/notices.json?cve_id={qU}",
    .array_path = "notices",
    .detail_key = "id",
    .detail_url = "https://ubuntu.com/security/notices/{v}.json",
    .page_param = "offset",
    .page_size = 10,
    .description = "Ubuntu Security Notices that reference a given CVE, each "
      "carrying the published date, the affected releases and the exact "
      "binary package versions shipped as the fix. Pivoting a CVE to "
      "the vendor advisory that closed it is the join most "
      "vulnerability feeds leave to the reader" },

  { .id = "REDHAT_CVE_BY_PACKAGE", .name = "Red Hat Security Data API — CVEs by package",
    .name_ja = "Red Hat セキュリティ パッケージ別CVE",
    .category = "threat", .portal = "https://access.redhat.com/security",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"linux\",\"redhat\",\"package\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://access.redhat.com/hydra/rest/securitydata/cve.json?package={q}&per_page=100",
    .headers = { "User-Agent: JapanOSINT-research/1.0 (+https://github.com/)" },
    .detail_key = "CVE",
    .detail_url = "https://access.redhat.com/hydra/rest/securitydata/cve/{v}.json",
    .page_param = "page",
    .page_start = 1,
    .description = "Red Hat's own CVE index filtered to one package, each entry with "
      "the severity, CVSS3 vector, public date, affected release list "
      "and the advisories that address it. The second hop pulls the "
      "full record including per-product fix state and mitigation text" },

  { .id = "ARCHLINUX_SEC_PACKAGE", .name = "Arch Linux Security Tracker — package issues",
    .name_ja = "Arch Linux セキュリティ パッケージ",
    .category = "threat", .portal = "https://security.archlinux.org",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"linux\",\"arch\",\"package\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://security.archlinux.org/package/{q}/json",
    .array_path = "issues",
    .detail_key = "name",
    .detail_url = "https://security.archlinux.org/{v}/json",
    .description = "Every open and resolved security issue for one Arch package, "
      "with the CVE list, the advisory group, the severity, the "
      "affected and fixed versions and the ticket status. Arch tracks "
      "upstream faster than most distributions so this often carries a "
      "fixed version before the enterprise trackers do" },

  { .id = "ARCHLINUX_SEC_CVE", .name = "Arch Linux Security Tracker — CVE record",
    .name_ja = "Arch Linux セキュリティ CVE",
    .category = "threat", .portal = "https://security.archlinux.org",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"linux\",\"arch\",\"cve\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://security.archlinux.org/{qU}/json",
    .title_keys = "name",
    .description = "One CVE as Arch assesses it: type, severity, affected packages, "
      "the advisory groups it belongs to and the remote or local attack "
      "classification. The type field categorises the flaw class in a "
      "way NVD's CWE mapping frequently omits" },

  { .id = "ARCHLINUX_SEC_GROUP", .name = "Arch Linux Security Tracker — advisory group",
    .name_ja = "Arch Linux 勧告グループ",
    .category = "threat", .portal = "https://security.archlinux.org",
    .record_type = "advisory-record",
    .tags = "\"advisory\",\"linux\",\"arch\",\"avg\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://security.archlinux.org/group/{qU}/json",
    .title_keys = "name",
    .description = "An Arch vulnerability group bundles the CVEs fixed by one "
      "package update, listing each issue, the affected and fixed "
      "package versions, the severity and the workaround. The group is "
      "the unit an administrator actually acts on" },

  { .id = "ALPINE_SECDB_BRANCH_MAIN", .name = "Alpine Linux secdb — main repository by branch",
    .name_ja = "Alpine セキュリティDB main",
    .category = "threat", .portal = "https://secdb.alpinelinux.org",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"linux\",\"alpine\",\"container\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://secdb.alpinelinux.org/{q}/main.json",
    .array_path = "packages",
    .description = "The Alpine security database for one release branch: every "
      "package in the main repository with the list of CVEs fixed in "
      "each version. Alpine is the base of a very large share of "
      "container images so this maps directly onto container exposure" },

  { .id = "ALPINE_SECDB_BRANCH_COMMUNITY", .name = "Alpine Linux secdb — community repository by branch",
    .name_ja = "Alpine セキュリティDB community",
    .category = "threat", .portal = "https://secdb.alpinelinux.org",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"linux\",\"alpine\",\"container\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://secdb.alpinelinux.org/{q}/community.json",
    .array_path = "packages",
    .description = "Same per-branch secdb for the community repository, which is "
      "where most application-level packages in Alpine images live. "
      "Each entry maps a package version to the CVEs it fixes" },

  { .id = "ALMALINUX_ERRATA_8", .name = "AlmaLinux 8 errata full dump",
    .name_ja = "AlmaLinux 8 エラータ全件",
    .category = "threat", .portal = "https://errata.almalinux.org",
    .record_type = "advisory-record",
    .tags = "\"advisory\",\"linux\",\"almalinux\",\"errata\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://errata.almalinux.org/8/errata.full.json",
    .array_path = "data",
    .interval = 86400,
    .description = "The complete AlmaLinux 8 errata register as one bulk file: every "
      "ALSA, ALBA and ALEA advisory with its type, severity, issued "
      "date, description, referenced CVEs and the exact RPM NEVRAs "
      "shipped. A full register rather than a search box, so the whole "
      "advisory history is reachable in one request" },

  { .id = "EUVD_CRITICAL", .name = "EUVD — critical vulnerabilities",
    .name_ja = "EU脆弱性DB 重大脆弱性",
    .category = "threat", .portal = "https://euvd.enisa.europa.eu",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"eu\",\"enisa\",\"euvd\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://euvdservices.enisa.europa.eu/api/criticalvulnerabilities",
    .detail_key = "id",
    .detail_url = "https://euvdservices.enisa.europa.eu/api/enisaid?id={v}",
    .interval = 21600,
    .description = "The European Union Vulnerability Database's own critical list, "
      "each entry with the EUVD id, aliases to CVE and GHSA, the base "
      "score and vector, the affected vendor and product tuples and the "
      "advisory references. EUVD is ENISA's independent enumeration, "
      "not a mirror of NVD, and carries EU CSIRT enrichment" },

  { .id = "EUVD_ENISAID", .name = "EUVD — vulnerability by EUVD identifier",
    .name_ja = "EU脆弱性DB ID照会",
    .category = "threat", .portal = "https://euvd.enisa.europa.eu",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"eu\",\"enisa\",\"euvd\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://euvdservices.enisa.europa.eu/api/enisaid?id={qU}",
    .body_keys = "description",
    .title_keys = "id",
    .description = "One EUVD record in full: description, EPSS score, exploitation "
      "status, the vendor and product tuples affected, the aliases into "
      "CVE and GHSA space and every advisory that references it. This "
      "is the detail endpoint behind every EUVD list" },

  { .id = "VULNLOOKUP_VULN", .name = "CIRCL Vulnerability-Lookup — vulnerability record",
    .name_ja = "CIRCL 脆弱性ルックアップ",
    .category = "threat", .portal = "https://vulnerability.circl.lu",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"circl\",\"aggregator\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://vulnerability.circl.lu/api/vulnerability/{qU}",
    .title_keys = "cveMetadata.cveId",
    .description = "CIRCL's aggregated record for one vulnerability identifier, "
      "returning the full CVE 5.0 JSON container including every ADP "
      "enrichment, the affected product ranges, the CVSS metrics from "
      "each scoring authority and the reference set. Accepts CVE, GHSA, "
      "PySec and EUVD identifiers against the same path" },

  { .id = "VULNLOOKUP_BROWSE_VENDOR", .name = "CIRCL Vulnerability-Lookup — browse by vendor",
    .name_ja = "CIRCL ベンダー別一覧",
    .category = "threat", .portal = "https://vulnerability.circl.lu",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"circl\",\"vendor\",\"cpe\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://vulnerability.circl.lu/api/browse/{ql}",
    .filter_query = 1,
    .description = "Every product CIRCL holds vulnerability data for under one "
      "vendor name, taken from the CPE dictionary. This is the "
      "enumeration step that turns a company name into the concrete "
      "product list you can then query for vulnerabilities" },

  { .id = "VULNLOOKUP_SEARCH_PRODUCT", .name = "CIRCL Vulnerability-Lookup — vulnerabilities by vendor and "
      "product",
    .name_ja = "CIRCL 製品別脆弱性",
    .category = "threat", .portal = "https://vulnerability.circl.lu",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"circl\",\"product\",\"cpe\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://vulnerability.circl.lu/api/search/{q}",
    .title_keys = "id",
    .description = "All vulnerabilities CIRCL holds for one vendor and product pair, "
      "grouped by the source database that supplied them so NVD, "
      "GitHub, CSAF and EUVD entries stay distinguishable. The grouping "
      "is what makes disagreement between sources visible instead of "
      "averaged away" },

  { .id = "GOVULNDB_ID", .name = "Go Vulnerability Database — report by identifier",
    .name_ja = "Go脆弱性DB レポート",
    .category = "threat", .portal = "https://vuln.go.dev",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"golang\",\"supplychain\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://vuln.go.dev/ID/{qU}.json",
    .body_keys = "details",
    .title_keys = "id",
    .description = "One Go vulnerability report in OSV format: the affected module "
      "and package paths, the exact symbol names that carry the flaw, "
      "the version ranges, the aliases to CVE and GHSA and the credited "
      "reporters. Symbol-level precision means a caller can tell "
      "whether the vulnerable code path is even reachable" },

  { .id = "OSV_ECOSYSTEMS", .name = "OSV — ecosystem index",
    .name_ja = "OSV エコシステム一覧",
    .category = "threat", .portal = "https://osv.dev",
    .record_type = "reference-record",
    .tags = "\"vulnerability\",\"osv\",\"ecosystem\",\"supplychain\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://osv-vulnerabilities.storage.googleapis.com/ecosystems.txt",
    .csv_no_header = 1,
    .interval = 604800,
    .description = "The authoritative list of every package ecosystem OSV carries "
      "advisories for, including the versioned distribution branches. "
      "It is the key set for every other OSV query and the fastest way "
      "to see which ecosystems have gained coverage" },

  { .id = "GITHUB_ADVISORY_ECOSYSTEM", .name = "GitHub Security Advisories by ecosystem",
    .name_ja = "GitHub セキュリティ勧告",
    .category = "threat", .portal = "https://github.com/advisories",
    .record_type = "advisory-record",
    .tags = "\"vulnerability\",\"github\",\"ghsa\",\"supplychain\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.github.com/advisories?ecosystem={q}&per_page=100",
    .detail_key = "ghsa_id",
    .detail_url = "https://api.github.com/advisories/{v}",
    .page_param = "page",
    .page_start = 1,
    .description = "Reviewed GitHub Security Advisories for one package ecosystem, "
      "each with the GHSA id, CVE alias, severity, CVSS vector, CWE "
      "list, the affected package name and version ranges and the "
      "patched version. The vulnerable-range plus patched-version pair "
      "is what a dependency scanner actually needs" },

  { .id = "MSRC_SUG_VULNERABILITY", .name = "Microsoft Security Update Guide — vulnerability list",
    .name_ja = "Microsoft セキュリティ更新ガイド",
    .category = "threat", .portal = "https://msrc.microsoft.com/update-guide",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"microsoft\",\"msrc\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.msrc.microsoft.com/sug/v2.0/en-US/vulnerability?$top=100",
    .array_path = "value",
    .detail_key = "cveNumber",
    .detail_url = "https://api.msrc.microsoft.com/sug/v2.0/en-US/vulnerability/{v}",
    .interval = 43200,
    .page_param = "$skip",
    .page_size = 100,
    .description = "Microsoft's Security Update Guide as structured JSON rather than "
      "the HTML console: each CVE with its release date, severity, "
      "impact class, exploitation assessment, exploitability index and "
      "the tag naming the affected product family. The exploitability "
      "index is Microsoft's own forecast and appears nowhere in NVD" },

  { .id = "PALOALTO_PSIRT_CVE", .name = "Palo Alto Networks PSIRT — advisory by CVE",
    .name_ja = "Palo Alto PSIRT 勧告",
    .category = "threat", .portal = "https://security.paloaltonetworks.com",
    .record_type = "advisory-record",
    .tags = "\"vulnerability\",\"vendor\",\"psirt\",\"paloalto\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://security.paloaltonetworks.com/json/{qU}",
    .title_keys = "document.title",
    .description = "The full CSAF advisory Palo Alto publishes for one CVE: affected "
      "PAN-OS versions and product tree, the remediation and workaround "
      "text, the CVSS scoring, the exploitation status and the "
      "acknowledgements. Network-edge appliance advisories are "
      "disproportionately actionable because the devices are "
      "internet-facing by design" },

  { .id = "CVELISTV5_DELTA_NEW", .name = "CVE Program cvelistV5 — newly published records in the current "
      "cycle",
    .name_ja = "CVEリストV5 差分",
    .category = "threat", .portal = "https://github.com/CVEProject/cvelistV5",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"cve\",\"mitre\",\"changefeed\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/CVEProject/cvelistV5/main/cves/delta.json",
    .array_path = "new",
    .detail_key = "githubLink",
    .detail_url = "{v}",
    .interval = 1800,
    .title_keys = "cveId",
    .description = "The CVE Program's own change feed, scoped to the records newly "
      "published in the last publishing cycle: each with the CVE "
      "identifier, the cve.org link, the update timestamp and the raw "
      "record URL, which the second hop fetches to pull the complete "
      "CVE 5.0 container. The updated set in the same document is "
      "served exhaustively by the deltaLog row rather than dropped here" },

  { .id = "CVELISTV5_DELTALOG", .name = "CVE Program cvelistV5 — delta history log",
    .name_ja = "CVEリストV5 差分履歴",
    .category = "threat", .portal = "https://github.com/CVEProject/cvelistV5",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"cve\",\"mitre\",\"changefeed\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/CVEProject/cvelistV5/main/cves/deltaLog.json",
    .interval = 86400,
    .description = "The retained history of every publishing cycle, so the full "
      "sequence of new and updated CVE records over time is "
      "reconstructible rather than only the most recent batch. Reissue "
      "history is the signal that a vendor quietly changed an "
      "affected-version range" },

  { .id = "EXPLOITDB_SHELLCODE_INDEX", .name = "Exploit-DB — shellcode file index",
    .name_ja = "Exploit-DB シェルコード索引",
    .category = "threat", .portal = "https://gitlab.com/exploit-database/exploitdb",
    .record_type = "exploit-record",
    .tags = "\"exploit\",\"shellcode\",\"exploitdb\",\"offensive\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://gitlab.com/exploit-database/exploitdb/-/raw/main/files_shellcodes.csv",
    .interval = 86400,
    .description = "The complete Exploit-DB shellcode index as CSV: every published "
      "shellcode with its id, file path, description, publication date, "
      "author, platform and type. Shellcode is the payload stage that a "
      "working exploit drops into, and the platform column is what says "
      "which architectures have ready-made payloads" },

  { .id = "METASPLOIT_MODULE_METADATA", .name = "Metasploit Framework — module metadata index",
    .name_ja = "Metasploit モジュール索引",
    .category = "threat", .portal = "https://github.com/rapid7/metasploit-framework",
    .record_type = "exploit-record",
    .tags = "\"exploit\",\"metasploit\",\"module\",\"offensive\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/rapid7/metasploit-framework/master/db/modules_metadata_base.json",
    .interval = 86400,
    .description = "Every Metasploit module as a structured record: name, fullname, "
      "disclosure date, rank, reliability and side-effect notes, "
      "targets, default ports, the referenced CVE and EDB identifiers "
      "and the author list. A CVE that appears here has weaponised, "
      "packaged exploit code, which is a different tier of risk from a "
      "proof of concept" },

  { .id = "NUCLEI_TEMPLATE_STATS", .name = "Nuclei templates — coverage statistics",
    .name_ja = "Nuclei テンプレート統計",
    .category = "threat", .portal = "https://github.com/projectdiscovery/nuclei-templates",
    .record_type = "reference-record",
    .tags = "\"detection\",\"nuclei\",\"template\",\"coverage\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/projectdiscovery/nuclei-templates/main/TEMPLATES-STATS.json",
    .interval = 43200,
    .description = "The breakdown of the Nuclei detection template corpus by tag, "
      "author, directory and severity. It answers which technologies "
      "and which CVEs have a public runnable detection today, which is "
      "the practical coverage map for anyone scanning their own estate" },

  { .id = "MISP_GALAXY_RANSOMWARE", .name = "MISP Galaxy — ransomware cluster",
    .name_ja = "MISPギャラクシー ランサムウェア",
    .category = "threat", .portal = "https://www.misp-project.org",
    .record_type = "threat-actor-record",
    .tags = "\"threat\",\"ransomware\",\"galaxy\",\"misp\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/MISP/misp-galaxy/main/clusters/ransomware.json",
    .array_path = "values",
    .interval = 86400,
    .description = "The MISP community's ransomware family catalogue: each family "
      "with its aliases, the extensions and ransom-note filenames it "
      "leaves, the payment addresses and email drops observed, and the "
      "reference set behind each attribution. The filesystem artefacts "
      "turn an incident into a named family without sample analysis" },

  { .id = "MISP_GALAXY_INTRUSION_SET", .name = "MISP Galaxy — MITRE intrusion set cluster",
    .name_ja = "MISPギャラクシー 侵入セット",
    .category = "threat", .portal = "https://www.misp-project.org",
    .record_type = "threat-actor-record",
    .tags = "\"threat\",\"apt\",\"attack\",\"galaxy\",\"misp\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/MISP/misp-galaxy/main/clusters/mitre-intrusion-set.json",
    .array_path = "values",
    .interval = 604800,
    .description = "Every MITRE ATT&CK intrusion set as a record with its synonyms "
      "across vendor naming schemes, the external references and the "
      "description. The synonym mapping is the piece that lets "
      "reporting from different vendors about the same actor be joined "
      "at all" },

  { .id = "MISP_GALAXY_TOOL", .name = "MISP Galaxy — tool and malware cluster",
    .name_ja = "MISPギャラクシー ツール",
    .category = "threat", .portal = "https://www.misp-project.org",
    .record_type = "malware-record",
    .tags = "\"threat\",\"malware\",\"tool\",\"galaxy\",\"misp\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/MISP/misp-galaxy/main/clusters/tool.json",
    .array_path = "values",
    .interval = 604800,
    .description = "The catalogue of offensive tools and malware families tracked by "
      "the MISP community, each with aliases, type classification, the "
      "threat actors known to use it and the references. Tool reuse "
      "across actors is one of the few durable links between otherwise "
      "separate campaigns" },

  { .id = "MISP_GALAXY_BOTNET", .name = "MISP Galaxy — botnet cluster",
    .name_ja = "MISPギャラクシー ボットネット",
    .category = "threat", .portal = "https://www.misp-project.org",
    .record_type = "malware-record",
    .tags = "\"threat\",\"botnet\",\"galaxy\",\"misp\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/MISP/misp-galaxy/main/clusters/botnet.json",
    .array_path = "values",
    .interval = 604800,
    .description = "Named botnets with their aliases, the malware family behind "
      "each, the takedown history and the references. Botnet naming is "
      "notoriously inconsistent between vendors and this cluster is the "
      "reconciliation table" },

  { .id = "ATTACK_STIX_MOBILE", .name = "MITRE ATT&CK — mobile STIX bundle",
    .name_ja = "MITRE ATT&CK モバイル",
    .category = "threat", .portal = "https://attack.mitre.org",
    .record_type = "technique-record",
    .tags = "\"threat\",\"attack\",\"stix\",\"mobile\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/mitre-attack/attack-stix-data/master/mobile-attack/mobile-attack.json",
    .array_path = "objects",
    .body_keys = "description",
    .interval = 604800,
    .title_keys = "name",
    .description = "The mobile ATT&CK matrix in STIX form, covering Android and iOS "
      "techniques, the mobile-specific software objects and the "
      "mitigations. Mobile surveillanceware is tracked here in a "
      "structured form that the vendor reporting on it is not" },

  { .id = "CWE_CATEGORY", .name = "MITRE CWE API — category record",
    .name_ja = "MITRE CWE カテゴリ",
    .category = "reference", .portal = "https://cwe.mitre.org",
    .record_type = "weakness-record",
    .tags = "\"vulnerability\",\"cwe\",\"taxonomy\",\"mitre\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://cwe-api.mitre.org/api/v1/cwe/category/{qd}",
    .array_path = "Categories",
    .title_keys = "Name",
    .description = "A CWE category groups weaknesses that share a common "
      "characteristic, and the record gives the summary, the membership "
      "relationships and the mapping notes. Categories are how a "
      "finding gets rolled up into a class of defect that an "
      "engineering programme can be measured against, which the "
      "individual weakness id cannot do" },

  { .id = "CWE_VIEW", .name = "MITRE CWE API — view record",
    .name_ja = "MITRE CWE ビュー",
    .category = "reference", .portal = "https://cwe.mitre.org",
    .record_type = "weakness-record",
    .tags = "\"vulnerability\",\"cwe\",\"taxonomy\",\"mitre\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://cwe-api.mitre.org/api/v1/cwe/view/{qd}",
    .array_path = "Views",
    .title_keys = "Name",
    .description = "A CWE view is a curated slice of the weakness catalogue such as "
      "research concepts, the software development view or the hardware "
      "design view, with its members and the rationale for the "
      "grouping. Views are how you enumerate the weakness space "
      "systematically rather than one identifier at a time" },

  { .id = "D3FEND_MAPPINGS", .name = "MITRE D3FEND — offensive to defensive mappings",
    .name_ja = "MITRE D3FEND 対応表",
    .category = "reference", .portal = "https://d3fend.mitre.org",
    .record_type = "technique-record",
    .tags = "\"defense\",\"d3fend\",\"ontology\",\"mitre\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://d3fend.mitre.org/api/ontology/inference/d3fend-full-mappings.json",
    .array_path = "results.bindings",
    .interval = 604800,
    .description = "The complete D3FEND mapping set as SPARQL result bindings: every "
      "ATT&CK offensive technique paired with the defensive "
      "countermeasure techniques that address it, plus the digital "
      "artifact that links them. It is the only public machine-readable "
      "bridge from attacker behaviour to a specific defensive control" },

  { .id = "EPSS_SCORE_PAGE", .name = "FIRST EPSS — full score set",
    .name_ja = "FIRST EPSS スコア一覧",
    .category = "threat", .portal = "https://www.first.org/epss",
    .record_type = "vulnerability-record",
    .tags = "\"vulnerability\",\"epss\",\"first\",\"exploitprediction\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.first.org/data/v1/epss?limit=100&offset=0",
    .array_path = "data",
    .interval = 86400,
    .page_param = "offset",
    .page_size = 100,
    .description = "The Exploit Prediction Scoring System over the whole CVE corpus, "
      "paged: each CVE with its probability of exploitation in the next "
      "thirty days and its percentile against every other CVE. Paging "
      "the whole set rather than querying one CVE at a time is what "
      "makes it usable as a ranking input" },

  { .id = "FIRST_COUNTRIES", .name = "FIRST — country reference table",
    .name_ja = "FIRST 国コード表",
    .category = "reference", .portal = "https://www.first.org",
    .record_type = "reference-record",
    .tags = "\"first\",\"csirt\",\"reference\",\"country\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.first.org/data/v1/countries?limit=100",
    .array_path = "data",
    .interval = 2592000,
    .page_param = "offset",
    .page_size = 100,
    .description = "The country reference table FIRST uses to key its incident "
      "response team directory, giving the ISO codes and names in the "
      "exact form the teams API expects. Small, but it is the join key "
      "for the CSIRT directory and prevents guessing at code forms" },

  { .id = "WORDPRESS_PLUGIN_REGISTRY", .name = "WordPress.org — plugin registry",
    .name_ja = "WordPress プラグイン登録簿",
    .category = "infrastructure", .portal = "https://wordpress.org/plugins",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"wordpress\",\"plugin\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.wordpress.org/plugins/info/1.2/?action=query_plugins&request[per_page]=100&request[page]={qd}",
    .array_path = "plugins",
    .detail_key = "slug",
    .detail_url = "https://api.wordpress.org/plugins/info/1.2/?action=plugin_information&request[slug]={v}",
    .page_param = "request[page]",
    .page_start = 1,
    .description = "Every plugin in the WordPress directory with its slug, author, "
      "version, active install count, last-updated date, tested-up-to "
      "version and rating. WordPress plugins are the single largest "
      "mass-exploited software population on the public internet, and "
      "the install count and staleness together predict where the next "
      "campaign lands" },

  { .id = "WORDPRESS_THEME_REGISTRY", .name = "WordPress.org — theme registry",
    .name_ja = "WordPress テーマ登録簿",
    .category = "infrastructure", .portal = "https://wordpress.org/themes",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"wordpress\",\"theme\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.wordpress.org/themes/info/1.2/?action=query_themes&request[per_page]=100&request[page]={qd}",
    .array_path = "themes",
    .detail_key = "slug",
    .detail_url = "https://api.wordpress.org/themes/info/1.2/?action=theme_information&request[slug]={v}",
    .page_param = "request[page]",
    .page_start = 1,
    .description = "The WordPress theme directory with slug, author, version, "
      "download count, last update and rating for each theme. Themes "
      "carry executable PHP and are a recurring initial-access vector, "
      "so the same staleness reasoning as plugins applies" },

  { .id = "JENKINS_UPDATE_CENTER", .name = "Jenkins update centre — plugin index and security warnings",
    .name_ja = "Jenkins 更新センター",
    .category = "infrastructure", .portal = "https://updates.jenkins.io",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"jenkins\",\"plugin\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://updates.jenkins.io/current/update-center.actual.json",
    .array_path = "warnings",
    .interval = 21600,
    .description = "The whole Jenkins plugin catalogue plus the security warning "
      "list, which names each vulnerable plugin, the affected version "
      "ranges and the advisory URL. Jenkins controllers hold build "
      "credentials and source access, so the plugin warning list is a "
      "direct map of CI compromise routes" },

  { .id = "JENKINS_PLUGIN_SITE", .name = "Jenkins plugin site API — plugin index",
    .name_ja = "Jenkins プラグイン一覧",
    .category = "infrastructure", .portal = "https://plugins.jenkins.io",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"jenkins\",\"plugin\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://plugins.jenkins.io/api/plugins/?page={qd}&limit=50",
    .array_path = "plugins",
    .detail_key = "name",
    .detail_url = "https://plugins.jenkins.io/api/plugin/{v}",
    .page_param = "page",
    .page_start = 1,
    .description = "Each Jenkins plugin with its maintainer list, dependency set, "
      "required core version, installation count and release history. "
      "The dependency graph across plugins is what makes a single "
      "vulnerable library reach a large share of installed controllers" },

  { .id = "NUGET_SERVICE_INDEX", .name = "NuGet — v3 service index",
    .name_ja = "NuGet サービス索引",
    .category = "infrastructure", .portal = "https://www.nuget.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"nuget\",\"dotnet\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.nuget.org/v3/index.json",
    .array_path = "resources",
    .interval = 2592000,
    .description = "The NuGet v3 service index: every resource endpoint the registry "
      "exposes with its type and version, including the catalogue, "
      "registration, search and package base address services. It is "
      "the discovery document that makes the rest of the .NET package "
      "registry addressable" },

  { .id = "NUGET_REGISTRATION", .name = "NuGet — package registration index",
    .name_ja = "NuGet パッケージ登録",
    .category = "infrastructure", .portal = "https://www.nuget.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"nuget\",\"dotnet\",\"package\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.nuget.org/v3/registration5-gz-semver2/{ql}/index.json",
    .description = "Every published version of one NuGet package with its catalogue "
      "entry: authors, licence, dependency groups per target framework, "
      "listing state, publication timestamp and deprecation or "
      "vulnerability annotations. The per-version dependency groups are "
      "what a .NET software bill of materials is actually built from" },

  { .id = "NUGET_CATALOG", .name = "NuGet — catalogue change feed index",
    .name_ja = "NuGet カタログ索引",
    .category = "infrastructure", .portal = "https://www.nuget.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"nuget\",\"dotnet\",\"changefeed\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.nuget.org/v3/catalog0/index.json",
    .array_path = "items",
    .interval = 21600,
    .description = "The append-only NuGet catalogue index, listing every page of "
      "package publication and deletion events with their timestamps "
      "and item counts. It is the complete change history of the .NET "
      "package ecosystem and the correct way to mirror it without "
      "polling search" },

  { .id = "CRANDB_PKGRELEASES", .name = "CRAN database — recent package releases",
    .name_ja = "CRAN パッケージ公開履歴",
    .category = "infrastructure", .portal = "https://crandb.r-pkg.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"cran\",\"r\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://crandb.r-pkg.org/-/pkgreleases?limit={qd}",
    .detail_key = "name",
    .detail_url = "https://crandb.r-pkg.org/{v}",
    .description = "Recent CRAN releases with the package name, version, publication "
      "date, maintainer, title and the crandb document id. CRAN is the "
      "analysis-tooling supply chain for a large share of research and "
      "finance code and has no advisory feed of its own, so the release "
      "stream is the observation point" },

  { .id = "CRANLOGS_PACKAGE", .name = "CRAN logs — package download totals",
    .name_ja = "CRAN ダウンロード数",
    .category = "infrastructure", .portal = "https://cranlogs.r-pkg.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"cran\",\"r\",\"downloads\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://cranlogs.r-pkg.org/downloads/total/last-month/{q}",
    .description = "Download totals for one CRAN package over the trailing month "
      "from the RStudio mirror logs. Download volume is the "
      "blast-radius estimate for an R package compromise and CRAN "
      "publishes no other usage measure" },

  { .id = "CRANLOGS_TOP", .name = "CRAN logs — most downloaded packages",
    .name_ja = "CRAN 人気パッケージ",
    .category = "infrastructure", .portal = "https://cranlogs.r-pkg.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"cran\",\"r\",\"downloads\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://cranlogs.r-pkg.org/top/last-week/{qd}",
    .array_path = "downloads",
    .description = "The most downloaded CRAN packages over the trailing week with "
      "counts. This is the prioritised list of which R packages would "
      "matter most if their maintainer account were taken over" },

  { .id = "ANACONDA_PACKAGE", .name = "Anaconda.org — conda package record",
    .name_ja = "Anaconda パッケージ",
    .category = "infrastructure", .portal = "https://anaconda.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"conda\",\"python\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.anaconda.org/package/conda-forge/{q}",
    .array_path = "files",
    .description = "Every built artefact of one conda-forge package: file name, "
      "platform, architecture, build string, upload timestamp, size, "
      "md5 and the full dependency specification. Conda ships compiled "
      "binaries rather than source, so the per-platform artefact list "
      "is the only complete view of what is actually distributed" },

  { .id = "ANACONDA_SEARCH", .name = "Anaconda.org — package search",
    .name_ja = "Anaconda パッケージ検索",
    .category = "infrastructure", .portal = "https://anaconda.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"conda\",\"python\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.anaconda.org/search?name={q}&limit=50",
    .page_param = "offset",
    .page_size = 50,
    .description = "Conda packages matching a name across all channels, each with "
      "the owning channel, summary, licence, latest version and "
      "platform list. Cross-channel visibility matters because "
      "typosquats and unofficial rebuilds live in channels other than "
      "conda-forge" },

  { .id = "GOPROXY_MODULE_VERSIONS", .name = "Go module proxy — version list",
    .name_ja = "Goモジュールプロキシ バージョン一覧",
    .category = "infrastructure", .portal = "https://proxy.golang.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"golang\",\"module\",\"registry\"",
    .mode = HP_CSV, .want = HP_DOMAIN, .free_tier = 1,
    .url = "https://proxy.golang.org/{ql}/@v/list",
    .csv_no_header = 1,
    .description = "Every version of a Go module the public proxy has ever served, "
      "as a plain line list. The proxy is immutable, so this is the "
      "authoritative record of what was published under a module path "
      "even after the upstream repository is deleted or rewritten" },

  { .id = "METACPAN_DISTRIBUTION", .name = "MetaCPAN — distribution record",
    .name_ja = "MetaCPAN ディストリビューション",
    .category = "infrastructure", .portal = "https://metacpan.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"perl\",\"cpan\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://fastapi.metacpan.org/v1/distribution/{q}",
    .description = "The MetaCPAN distribution record with the bug summary from RT "
      "and GitHub, the river-of-CPAN dependency position and the "
      "distribution name. The river position states how many downstream "
      "distributions depend on it, which is the Perl ecosystem's "
      "blast-radius measure" },

  { .id = "METACPAN_RELEASE_SEARCH", .name = "MetaCPAN — release search",
    .name_ja = "MetaCPAN リリース検索",
    .category = "infrastructure", .portal = "https://metacpan.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"perl\",\"cpan\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://fastapi.metacpan.org/v1/release/_search?q={q}&size=50",
    .array_path = "hits.hits",
    .page_param = "from",
    .page_size = 50,
    .description = "Every CPAN release matching an Elasticsearch query, each with "
      "the author, version, release date, licence, dependency list, the "
      "full file manifest and the abstract. The manifest and dependency "
      "list together make CPAN releases auditable without downloading "
      "the tarball" },

  { .id = "PACKAGIST_P2_PACKAGE", .name = "Packagist — composer v2 package metadata",
    .name_ja = "Packagist パッケージメタデータ",
    .category = "infrastructure", .portal = "https://packagist.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"php\",\"composer\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://repo.packagist.org/p2/{ql}.json",
    .description = "The canonical Composer v2 metadata document for one PHP package: "
      "every published version with its require and require-dev maps, "
      "autoload configuration, licence, source and dist URLs and the "
      "commit reference. This is the file Composer itself resolves "
      "against, so it is the ground truth for a PHP dependency tree" },

  { .id = "PACKAGIST_VENDOR_LIST", .name = "Packagist — packages by vendor",
    .name_ja = "Packagist ベンダー別一覧",
    .category = "infrastructure", .portal = "https://packagist.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"php\",\"composer\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://packagist.org/packages/list.json?vendor={ql}",
    .description = "Every package name published under one Packagist vendor "
      "namespace. Enumerating a vendor namespace is how you find the "
      "abandoned or rarely updated packages under an otherwise well "
      "maintained brand, which is where namespace-confusion attacks "
      "start" },

  { .id = "RUBYGEMS_REVERSE_DEPS", .name = "RubyGems — reverse dependencies",
    .name_ja = "RubyGems 被依存一覧",
    .category = "infrastructure", .portal = "https://rubygems.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"ruby\",\"rubygems\",\"dependency\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://rubygems.org/api/v1/gems/{ql}/reverse_dependencies.json",
    .description = "Every gem that declares a dependency on the named gem. Reverse "
      "dependencies are the direct measure of how far a compromise of "
      "one gem would propagate, and RubyGems is one of the few "
      "registries that publishes them" },

  { .id = "RUBYGEMS_VERSIONS", .name = "RubyGems — version history",
    .name_ja = "RubyGems バージョン履歴",
    .category = "infrastructure", .portal = "https://rubygems.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"ruby\",\"rubygems\",\"version\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://rubygems.org/api/v1/versions/{ql}.json",
    .description = "Every published version of one gem with its SHA, build date, "
      "platform, required Ruby version, licence set and yank status. "
      "Yanked versions are retained in this response, which is how you "
      "spot a version that was published and hastily withdrawn" },

  { .id = "RUBYGEMS_OWNERS", .name = "RubyGems — gem owners",
    .name_ja = "RubyGems 所有者",
    .category = "infrastructure", .portal = "https://rubygems.org",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"ruby\",\"rubygems\",\"ownership\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://rubygems.org/api/v1/gems/{ql}/owners.json",
    .description = "The accounts that can publish a given gem, with handle, id and "
      "gravatar identity. Publishing rights are the actual attack "
      "surface of a package registry, and the owner set is the only "
      "public view of it" },

  { .id = "HEX_PACKAGE", .name = "Hex — package record",
    .name_ja = "Hex パッケージ",
    .category = "infrastructure", .portal = "https://hex.pm",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"elixir\",\"erlang\",\"hex\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://hex.pm/api/packages/{ql}",
    .array_path = "releases",
    .detail_key = "url",
    .detail_url = "{v}",
    .description = "One Hex package with every release, the download counts, the "
      "owner set, the licence, the declared repository and "
      "documentation links and the insertion timestamps. Hex is the "
      "Erlang and Elixir supply chain and carries the telecoms and "
      "messaging infrastructure that runs on the BEAM" },

  { .id = "ENDOFLIFE_ALL", .name = "endoflife.date — tracked product index",
    .name_ja = "endoflife.date 製品一覧",
    .category = "reference", .portal = "https://endoflife.date",
    .record_type = "reference-record",
    .tags = "\"lifecycle\",\"eol\",\"patching\",\"inventory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://endoflife.date/api/all.json",
    .interval = 604800,
    .description = "The list of every product endoflife.date tracks a support "
      "lifecycle for. It is the key set for the per-product endpoint "
      "and the fastest way to see whether a technology in an inventory "
      "has a machine-readable end-of-support date at all" },

  { .id = "ENDOFLIFE_PRODUCT", .name = "endoflife.date — product lifecycle cycles",
    .name_ja = "endoflife.date 製品ライフサイクル",
    .category = "reference", .portal = "https://endoflife.date",
    .record_type = "reference-record",
    .tags = "\"lifecycle\",\"eol\",\"patching\",\"inventory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://endoflife.date/api/{ql}.json",
    .description = "Each release cycle of one product with its release date, "
      "end-of-active-support date, end-of-security-support date, latest "
      "patch version and long-term-support flag. Running past the "
      "security end date is a durable exposure that no CVE feed "
      "reports, because there will never be a fix to report" },

  { .id = "SCORECARD_PROJECT", .name = "OpenSSF Scorecard — project assessment",
    .name_ja = "OpenSSF スコアカード",
    .category = "infrastructure", .portal = "https://securityscorecards.dev",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"openssf\",\"scorecard\",\"security\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.securityscorecards.dev/projects/github.com/{q}",
    .array_path = "checks",
    .title_keys = "name",
    .description = "The OpenSSF Scorecard result for one repository: the aggregate "
      "score plus each individual check covering branch protection, "
      "code review, dangerous workflows, dependency update tooling, "
      "signed releases, token permissions and pinned dependencies, with "
      "the score and the reasoning for each. It is the closest thing to "
      "an audited security posture for an open source dependency" },

  { .id = "REKOR_LOG_STATUS", .name = "Sigstore Rekor — transparency log state",
    .name_ja = "Sigstore Rekor ログ状態",
    .category = "infrastructure", .portal = "https://rekor.sigstore.dev",
    .record_type = "transparency-record",
    .tags = "\"supplychain\",\"sigstore\",\"rekor\",\"transparency\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://rekor.sigstore.dev/api/v1/log",
    .array_path = "inactiveShards",
    .interval = 3600,
    .description = "The signed tree head of the Sigstore transparency log: tree "
      "size, root hash, signature and the inactive shard set. It is the "
      "anchor that makes every software signature recorded in Rekor "
      "independently verifiable, and the tree size is the count of all "
      "attestations ever logged" },

  { .id = "REKOR_LOG_ENTRY", .name = "Sigstore Rekor — log entry by index",
    .name_ja = "Sigstore Rekor ログエントリ",
    .category = "infrastructure", .portal = "https://rekor.sigstore.dev",
    .record_type = "transparency-record",
    .tags = "\"supplychain\",\"sigstore\",\"rekor\",\"provenance\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://rekor.sigstore.dev/api/v1/log/entries?logIndex={qd}",
    .description = "One Rekor transparency log entry: the signed artefact digest, "
      "the signature, the certificate or public key used, the "
      "integrated timestamp and the inclusion proof. This is where "
      "software provenance attestations physically live, and each entry "
      "names the identity that signed a release" },

  { .id = "ARTIFACTHUB_PACKAGE_SEARCH", .name = "Artifact Hub — package search",
    .name_ja = "Artifact Hub パッケージ検索",
    .category = "infrastructure", .portal = "https://artifacthub.io",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"kubernetes\",\"helm\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://artifacthub.io/api/v1/packages/search?ts_query_web={q}&limit=60&offset=0",
    .array_path = "packages",
    .page_param = "offset",
    .page_size = 60,
    .description = "Cloud native packages such as Helm charts, OLM operators, Falco "
      "rules and OPA policies matching a term, each with the publishing "
      "repository, whether that repository is verified or official, the "
      "version, the signing state and the security report summary. The "
      "signing and verification flags are the supply chain signal" },

  { .id = "ARTIFACTHUB_REPOSITORY_SEARCH", .name = "Artifact Hub — repository index",
    .name_ja = "Artifact Hub リポジトリ一覧",
    .category = "infrastructure", .portal = "https://artifacthub.io",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"kubernetes\",\"helm\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://artifacthub.io/api/v1/repositories/search?limit=60&offset={qd}",
    .page_param = "offset",
    .page_size = 60,
    .description = "Every repository registered with Artifact Hub, with its owning "
      "organisation, kind, source URL, verified-publisher flag and "
      "official flag. Enumerating the publishers is how you find which "
      "organisation actually stands behind a chart that a cluster is "
      "pulling" },

  { .id = "DEPSDEV_V3_PACKAGE", .name = "Open Source Insights — package versions",
    .name_ja = "Open Source Insights パッケージ",
    .category = "infrastructure", .portal = "https://deps.dev",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"depsdev\",\"dependency\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.deps.dev/v3/systems/{ql}/packages/{q}",
    .array_path = "versions",
    .description = "Every known version of a package in one ecosystem with its "
      "default and deprecation flags, taken from Google's Open Source "
      "Insights graph. The v3 stable API is a different surface from "
      "the alpha one and is the entry point for the version, dependency "
      "and advisory subresources" },

  { .id = "DEPSDEV_ADVISORY", .name = "Open Source Insights — advisory record",
    .name_ja = "Open Source Insights 勧告",
    .category = "threat", .portal = "https://deps.dev",
    .record_type = "advisory-record",
    .tags = "\"supplychain\",\"depsdev\",\"advisory\",\"ghsa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.deps.dev/v3alpha/advisories/{qU}",
    .description = "One advisory as Open Source Insights holds it: the aliases "
      "across GHSA and CVE space, the title, the CVSS vector and score "
      "and the severity. It is the join point between the dependency "
      "graph and the vulnerability corpus, so a resolved version can be "
      "tested against it directly" },

  { .id = "BREW_FORMULA", .name = "Homebrew — formula record",
    .name_ja = "Homebrew フォーミュラ",
    .category = "infrastructure", .portal = "https://formulae.brew.sh",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"homebrew\",\"macos\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://formulae.brew.sh/api/formula/{ql}.json",
    .description = "One Homebrew formula in full: the versioned stable and head "
      "sources, the bottle checksums per macOS release and "
      "architecture, the build and runtime dependencies, the caveats "
      "and the deprecation or disable reason. Homebrew is the de facto "
      "package manager on developer macOS fleets and the bottle "
      "checksums pin exactly what those machines executed" },

  { .id = "BREW_CASK_INDEX", .name = "Homebrew — full cask index",
    .name_ja = "Homebrew カスク一覧",
    .category = "infrastructure", .portal = "https://formulae.brew.sh",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"homebrew\",\"macos\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://formulae.brew.sh/api/cask.json",
    .interval = 86400,
    .description = "Every Homebrew cask as a bulk record set: the application name, "
      "the vendor download URL, the version, the SHA256, the "
      "auto-update flag and the uninstall and zap directives. Casks "
      "fetch binaries straight from vendor servers, so this file is the "
      "map of which third-party download hosts a developer fleet trusts" },

  { .id = "DOCKERHUB_REPO_TAGS", .name = "Docker Hub — repository tags",
    .name_ja = "Docker Hub タグ一覧",
    .category = "infrastructure", .portal = "https://hub.docker.com",
    .record_type = "container-record",
    .tags = "\"supplychain\",\"container\",\"docker\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://hub.docker.com/v2/repositories/{ql}/tags?page_size=100",
    .array_path = "results",
    .next_path = "next",
    .description = "Every tag of one Docker Hub repository with the digest, the "
      "per-architecture image list, the compressed size, the "
      "last-pushed timestamp and the pushing account. The digest-to-tag "
      "mapping is what lets you tell whether a mutable tag was silently "
      "repointed at a different image" },

  { .id = "TERRAFORM_MODULES", .name = "Terraform Registry — module index",
    .name_ja = "Terraform モジュール一覧",
    .category = "infrastructure", .portal = "https://registry.terraform.io",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"terraform\",\"iac\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://registry.terraform.io/v1/modules?limit=50&offset={qd}",
    .array_path = "modules",
    .detail_key = "id",
    .detail_url = "https://registry.terraform.io/v1/modules/{v}",
    .page_param = "offset",
    .page_size = 50,
    .description = "Published Terraform modules with namespace, provider, version, "
      "source repository, download count and verification flag. "
      "Infrastructure-as-code modules execute with cloud administrative "
      "credentials, which makes registry provenance a higher-stakes "
      "question here than in an application package registry" },

  { .id = "TERRAFORM_PROVIDERS", .name = "Terraform Registry — provider index",
    .name_ja = "Terraform プロバイダ一覧",
    .category = "infrastructure", .portal = "https://registry.terraform.io",
    .record_type = "package-record",
    .tags = "\"supplychain\",\"terraform\",\"iac\",\"registry\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://registry.terraform.io/v1/providers?limit=50&offset={qd}",
    .array_path = "providers",
    .detail_key = "id",
    .detail_url = "https://registry.terraform.io/v1/providers/{v}",
    .page_param = "offset",
    .page_size = 50,
    .description = "Every Terraform provider with its namespace, source repository, "
      "published version, tier and download count. Providers are "
      "compiled plugins that Terraform executes locally against cloud "
      "credentials, so the tier field naming official, partner or "
      "community is a direct trust statement" },

  { .id = "NODEJS_RELEASE_INDEX", .name = "Node.js — release index",
    .name_ja = "Node.js リリース一覧",
    .category = "infrastructure", .portal = "https://nodejs.org",
    .record_type = "release-record",
    .tags = "\"supplychain\",\"nodejs\",\"runtime\",\"lifecycle\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://nodejs.org/dist/index.json",
    .interval = 86400,
    .description = "Every Node.js release ever published with its version, date, the "
      "bundled npm, V8, OpenSSL and libuv versions, the LTS codename "
      "and the full list of build artefacts. The bundled OpenSSL "
      "version is the one that matters when an OpenSSL advisory lands, "
      "and it is only discoverable here" },

  { .id = "CHROME_RELEASES", .name = "Chrome Version History — releases by platform",
    .name_ja = "Chrome バージョン履歴",
    .category = "infrastructure", .portal = "https://developer.chrome.com",
    .record_type = "release-record",
    .tags = "\"browser\",\"chrome\",\"release\",\"version\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://versionhistory.googleapis.com/v1/chrome/platforms/{ql}/channels/stable/versions/all/releases",
    .array_path = "releases",
    .next_path = "nextPageToken",
    .description = "Every Chrome stable release for one platform with the exact "
      "version, the serving interval and the fraction of the population "
      "it was served to. Because Chrome security fixes are identified "
      "by the version they shipped in, this is the timeline that dates "
      "any Chrome CVE to a real rollout" },

  { .id = "CHROME_PLATFORMS", .name = "Chrome Version History — platform index",
    .name_ja = "Chrome プラットフォーム一覧",
    .category = "infrastructure", .portal = "https://developer.chrome.com",
    .record_type = "reference-record",
    .tags = "\"browser\",\"chrome\",\"release\",\"reference\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://versionhistory.googleapis.com/v1/chrome/platforms",
    .array_path = "platforms",
    .interval = 2592000,
    .description = "The set of platforms and channels the Chrome version history "
      "service covers, from Windows and Android to WebView and Chrome "
      "OS. It is the key set for the release endpoint and shows which "
      "surfaces get separate release trains" },

  { .id = "PHP_RELEASES", .name = "PHP — release index",
    .name_ja = "PHP リリース一覧",
    .category = "infrastructure", .portal = "https://www.php.net",
    .record_type = "release-record",
    .tags = "\"supplychain\",\"php\",\"runtime\",\"lifecycle\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.php.net/releases/index.php?json&max={qd}",
    .description = "Every PHP release grouped by major version with the release "
      "date, announcement link, the tarball names and their MD5 and "
      "SHA256 checksums. PHP runs a very large share of internet-facing "
      "applications and the checksum set is what verifies a build "
      "actually came from php.net" },

  { .id = "RANSOMWATCH_POSTS", .name = "Ransomwatch — leak site victim posts",
    .name_ja = "Ransomwatch 被害者投稿",
    .category = "threat", .portal = "https://github.com/joshhighet/ransomwatch",
    .record_type = "victim-record",
    .tags = "\"ransomware\",\"leaksite\",\"extortion\",\"victim\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/joshhighet/ransomwatch/main/posts.json",
    .interval = 1800,
    .description = "Every victim named on a ransomware group's extortion site since "
      "the project began, with the victim name, the group that posted "
      "it, the discovery timestamp and the leak site URL. Extortion "
      "posts are the earliest public evidence that an organisation was "
      "breached, usually weeks before any disclosure" },

  { .id = "RANSOMWATCH_GROUPS", .name = "Ransomwatch — leak site group index",
    .name_ja = "Ransomwatch グループ一覧",
    .category = "threat", .portal = "https://github.com/joshhighet/ransomwatch",
    .record_type = "threat-actor-record",
    .tags = "\"ransomware\",\"leaksite\",\"extortion\",\"infrastructure\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/joshhighet/ransomwatch/main/groups.json",
    .interval = 3600,
    .description = "Every tracked extortion group with its onion and clearnet host "
      "list, the parser used, whether the site is currently reachable "
      "and the metadata captured per host. The host list is the "
      "infrastructure inventory of the ransomware ecosystem and it is "
      "what survives a rebrand" },

  { .id = "THREATFOX_CSV_RECENT", .name = "ThreatFox — recent IOCs as CSV",
    .name_ja = "ThreatFox 最新IOC",
    .category = "threat", .portal = "https://threatfox.abuse.ch",
    .record_type = "ioc-record",
    .tags = "\"threat\",\"ioc\",\"c2\",\"malware\",\"abusech\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://threatfox.abuse.ch/export/csv/recent/",
    .csv_no_header = 1,
    .interval = 900,
    .description = "Recently reported indicators of compromise with the IOC value "
      "and type, the threat type, the malware family, the confidence "
      "level, the reporter and the tags. The CSV export carries the "
      "reporter and confidence columns that the JSON recent feed omits, "
      "which is what lets you weight an indicator instead of trusting "
      "it flat" },

  { .id = "MALWAREBAZAAR_CSV_RECENT", .name = "MalwareBazaar — recent samples as CSV",
    .name_ja = "MalwareBazaar 最新検体",
    .category = "threat", .portal = "https://bazaar.abuse.ch",
    .record_type = "malware-record",
    .tags = "\"threat\",\"malware\",\"sample\",\"hash\",\"abusech\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://bazaar.abuse.ch/export/csv/recent/",
    .interval = 900,
    .description = "Recently submitted malware samples with the SHA256, MD5 and SHA1 "
      "hashes, the reporter, the file name and type, the signature "
      "family, the ClamAV and VirusTotal verdicts and the imphash and "
      "ssdeep values. The imphash and ssdeep columns are what cluster "
      "samples into campaigns without running them" },

  { .id = "URLHAUS_TEXT_ONLINE", .name = "URLhaus — currently online payload URLs",
    .name_ja = "URLhaus 稼働中URL",
    .category = "threat", .portal = "https://urlhaus.abuse.ch",
    .record_type = "malware-url",
    .tags = "\"threat\",\"malware\",\"url\",\"abusech\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://urlhaus.abuse.ch/downloads/text_online/",
    .csv_no_header = 1,
    .interval = 900,
    .description = "The set of malware distribution URLs URLhaus currently observes "
      "as live, as opposed to the full historical export. Restricting "
      "to online URLs is what makes the list usable as a blocking feed "
      "rather than an archive" },

  { .id = "FEODO_IPBLOCKLIST_RECOMMENDED", .name = "Feodo Tracker — recommended C2 blocklist",
    .name_ja = "Feodo 推奨ブロックリスト",
    .category = "threat", .portal = "https://feodotracker.abuse.ch",
    .record_type = "botnet-c2",
    .tags = "\"threat\",\"c2\",\"botnet\",\"abusech\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://feodotracker.abuse.ch/downloads/ipblocklist_recommended.json",
    .interval = 900,
    .description = "The curated Feodo Tracker botnet command and control list, each "
      "entry with the address, port, malware family, hosting country, "
      "ASN and the first and last seen timestamps. The recommended list "
      "is the false-positive-filtered subset intended for actual "
      "blocking" },

  { .id = "BLOCKLIST_DE_SSH", .name = "blocklist.de — SSH attack sources",
    .name_ja = "blocklist.de SSH攻撃元",
    .category = "threat", .portal = "https://www.blocklist.de",
    .record_type = "attacking-host",
    .tags = "\"threat\",\"ssh\",\"bruteforce\",\"blocklist\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://lists.blocklist.de/lists/ssh.txt",
    .csv_no_header = 1,
    .interval = 1800,
    .description = "Addresses reported by the blocklist.de participant network for "
      "SSH brute-force attacks in the last 48 hours. Split by service "
      "rather than aggregated, so an SSH-only list is directly usable "
      "for SSH-facing hosts without over-blocking" },

  { .id = "FIREHOL_LEVEL2", .name = "FireHOL — level 2 aggregated blocklist",
    .name_ja = "FireHOL レベル2一覧",
    .category = "threat", .portal = "https://iplists.firehol.org",
    .record_type = "attacking-host",
    .tags = "\"threat\",\"blocklist\",\"firehol\",\"reputation\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://iplists.firehol.org/files/firehol_level2.netset",
    .csv_no_header = 1,
    .interval = 1800,
    .description = "The FireHOL level 2 netset, aggregating attack sources reported "
      "in the last 48 hours across the contributing sensor feeds into a "
      "deduplicated CIDR list. Level 2 is the tier intended for active "
      "blocking rather than the wider level 3 research set" },

  { .id = "CERTPL_DOMAINS_JSON", .name = "CERT Polska — warning list as JSON",
    .name_ja = "CERT Polska 警告ドメイン一覧",
    .category = "threat", .portal = "https://hole.cert.pl",
    .record_type = "phishing-domain",
    .tags = "\"phishing\",\"blocklist\",\"poland\",\"cert\"",
    .mode = HP_JSON, .want = HP_DOMAIN, .free_tier = 1,
    .url = "https://hole.cert.pl/domains/v2/domains.json",
    .interval = 21600,
    .description = "The Polish national warning list of domains used for fraud, as "
      "structured JSON with the register position, the domain, the "
      "insertion and deletion dates and the entry state. The JSON form "
      "carries the state and date fields that the CSV export flattens "
      "away, so historical entries stay distinguishable from active "
      "ones" },

  { .id = "MALTRAIL_COBALTSTRIKE", .name = "Maltrail — Cobalt Strike trails",
    .name_ja = "Maltrail Cobalt Strike",
    .category = "threat", .portal = "https://github.com/stamparm/maltrail",
    .record_type = "botnet-c2",
    .tags = "\"threat\",\"c2\",\"cobaltstrike\",\"maltrail\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/stamparm/maltrail/master/trails/static/malware/cobaltstrike.txt",
    .csv_no_header = 1,
    .interval = 21600,
    .description = "The Maltrail static trail list for Cobalt Strike infrastructure, "
      "listing addresses and domains observed hosting team servers. "
      "Cobalt Strike is the most widely used post-exploitation "
      "framework in both criminal and state operations so its "
      "infrastructure is disproportionately informative" },

  { .id = "DSHIELD_THREATFEEDS", .name = "SANS ISC DShield — daily threat feed volume",
    .name_ja = "DShield 脅威フィード一覧",
    .category = "threat", .portal = "https://isc.sans.edu",
    .record_type = "reference-record",
    .tags = "\"threat\",\"dshield\",\"sans\",\"feed\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://isc.sans.edu/api/threatfeeds?json",
    .interval = 86400,
    .description = "The number of addresses the SANS Internet Storm Centre added to "
      "its aggregated threat feed on each of the last thirty-one days. "
      "A sustained rise is the sensor network telling you that scanning "
      "and attack source diversity is growing, which is the context "
      "every individual indicator is missing" },

  { .id = "DSHIELD_THREATLIST", .name = "SANS ISC DShield — named threat list membership",
    .name_ja = "DShield 脅威リスト",
    .category = "threat", .portal = "https://isc.sans.edu",
    .record_type = "attacking-host",
    .tags = "\"threat\",\"dshield\",\"reputation\",\"feed\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://isc.sans.edu/api/threatlist/{ql}?json",
    .description = "The addresses in one named DShield threat list, each with the "
      "date it was first reported and the date it was last seen. Naming "
      "the list is what gives an address its meaning: a host on the "
      "research-scanner list is benign infrastructure while the same "
      "address on an attack list is not, and only this endpoint "
      "distinguishes them" },

  { .id = "DSHIELD_PORTHISTORY", .name = "SANS ISC DShield — port activity history",
    .name_ja = "DShield ポート活動履歴",
    .category = "threat", .portal = "https://isc.sans.edu",
    .record_type = "scan-record",
    .tags = "\"threat\",\"dshield\",\"scanning\",\"port\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://isc.sans.edu/api/porthistory/{qd}?json",
    .description = "The daily history of packets, targets and sources observed "
      "against one TCP or UDP port across the DShield sensor network. A "
      "step change in the target count for a port is the earliest "
      "public signal that a new exploit for the service behind it is "
      "being used at scale" },

  { .id = "DSHIELD_BACKSCATTER", .name = "SANS ISC DShield — backscatter observations",
    .name_ja = "DShield バックスキャッタ",
    .category = "threat", .portal = "https://isc.sans.edu",
    .record_type = "scan-record",
    .tags = "\"threat\",\"dshield\",\"ddos\",\"backscatter\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://isc.sans.edu/api/backscatter/?json",
    .interval = 3600,
    .description = "Backscatter observed by the DShield sensors, meaning the "
      "response packets sent to spoofed source addresses during a "
      "denial of service attack, aggregated by source port with the "
      "packet count and the number of distinct sources and targets "
      "behind each. Backscatter is indirect evidence that an attack is "
      "under way against someone who will never report it" },

  { .id = "DSHIELD_IP_DETAIL", .name = "SANS ISC DShield — address report",
    .name_ja = "DShield IPレポート",
    .category = "threat", .portal = "https://isc.sans.edu",
    .record_type = "attacking-host",
    .tags = "\"threat\",\"dshield\",\"reputation\",\"ip\"",
    .mode = HP_JSON, .want = HP_IP, .free_tier = 1,
    .url = "https://isc.sans.edu/api/ip/{q}?json",
    .description = "The DShield record for one address: the number of attacks and "
      "targets reported, the first and last report dates, the network "
      "block, the ASN and AS name, the country and the abuse contact. "
      "The abuse contact and count together are what turn an "
      "observation into a report you can actually send" },

  { .id = "CT_ARGON_STH", .name = "Google Argon CT log — signed tree head",
    .name_ja = "Argon CTログ 署名済みツリーヘッド",
    .category = "infrastructure", .portal = "https://certificate.transparency.dev",
    .record_type = "transparency-record",
    .tags = "\"certificate\",\"ct\",\"transparency\",\"tls\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ct.googleapis.com/logs/us1/argon2026h1/ct/v1/get-sth",
    .interval = 3600,
    .description = "The current signed tree head of a Google Argon certificate "
      "transparency log shard: tree size, timestamp, root hash and the "
      "log's signature over them. The tree size is the running count of "
      "every certificate logged in the shard and the signature is what "
      "makes the log auditable rather than merely published" },

  { .id = "DNS_CLOUDFLARE_CAA", .name = "Cloudflare 1.1.1.1 — CAA records over DNS over HTTPS",
    .name_ja = "Cloudflare DoH CAAレコード",
    .category = "infrastructure", .portal = "https://developers.cloudflare.com/1.1.1.1",
    .record_type = "dns-record",
    .tags = "\"dns\",\"doh\",\"caa\",\"certificate\"",
    .mode = HP_JSON, .want = HP_DOMAIN, .free_tier = 1,
    .url = "https://cloudflare-dns.com/dns-query?name={qh}&type=CAA",
    .headers = { "Accept: application/dns-json" },
    .array_path = "Answer",
    .description = "The Certification Authority Authorization records for a domain, "
      "naming exactly which certificate authorities the owner permits "
      "to issue for it, plus the issuewild policy and the incident "
      "reporting address. A certificate in a transparency log from a CA "
      "outside this set is direct evidence of misissuance" },

  { .id = "OONI_CIRCUMVENTION_STATS", .name = "OONI — circumvention tool reachability by country",
    .name_ja = "OONI 回避ツール統計",
    .category = "research", .portal = "https://ooni.org",
    .record_type = "measurement-record",
    .tags = "\"censorship\",\"ooni\",\"circumvention\",\"vpn\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.ooni.io/api/_/circumvention_stats_by_country",
    .array_path = "results",
    .interval = 21600,
    .description = "Per-country success rates for circumvention tools including "
      "Psiphon, Tor and Tor bridges, from OONI's global probe network. "
      "When a country's numbers collapse it means the censor has "
      "started blocking the escape routes themselves, which is a "
      "distinct and later stage from blocking websites" },

  { .id = "OONI_IM_NETWORKS", .name = "OONI — messaging app blocking by network",
    .name_ja = "OONI メッセンジャー遮断状況",
    .category = "research", .portal = "https://ooni.org",
    .record_type = "measurement-record",
    .tags = "\"censorship\",\"ooni\",\"messaging\",\"blocking\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.ooni.io/api/_/im_networks?probe_cc={qU}",
    .description = "Which networks inside a country are blocking each instant "
      "messaging app such as Signal, Telegram, WhatsApp and Facebook "
      "Messenger, with the ASN, the AS name and the measurement counts. "
      "Per-network resolution is what distinguishes a national order "
      "from one operator's own filtering" },

  { .id = "OONI_WEBSITE_NETWORKS", .name = "OONI — website blocking by network",
    .name_ja = "OONI ウェブ遮断ネットワーク",
    .category = "research", .portal = "https://ooni.org",
    .record_type = "measurement-record",
    .tags = "\"censorship\",\"ooni\",\"web\",\"blocking\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.ooni.io/api/_/website_networks?probe_cc={qU}",
    .array_path = "results",
    .description = "The networks in one country from which OONI has web connectivity "
      "measurements, each with the ASN and AS name. It is the coverage "
      "map that tells you whether an absence of reported blocking means "
      "no blocking or simply no probe on that operator" },

  { .id = "OONI_DOMAIN_INDEX", .name = "OONI — measured domain index",
    .name_ja = "OONI 計測対象ドメイン一覧",
    .category = "research", .portal = "https://ooni.org",
    .record_type = "measurement-record",
    .tags = "\"censorship\",\"ooni\",\"domain\",\"testlist\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.ooni.io/api/_/domains",
    .array_path = "results",
    .interval = 86400,
    .description = "Every domain OONI has measurements for, with the canonical "
      "domain name and the associated category code from the Citizen "
      "Lab test list. The category code is what turns raw blocking "
      "counts into a statement about what kind of speech a country "
      "restricts" },

  { .id = "OONI_DOMAIN_METADATA", .name = "OONI — domain metadata",
    .name_ja = "OONI ドメイン情報",
    .category = "research", .portal = "https://ooni.org",
    .record_type = "measurement-record",
    .tags = "\"censorship\",\"ooni\",\"domain\",\"testlist\"",
    .mode = HP_JSON, .want = HP_DOMAIN, .free_tier = 1,
    .url = "https://api.ooni.io/api/_/domain_metadata?domain={qh}",
    .description = "The OONI record for one domain: its canonical form and the "
      "Citizen Lab test list category it was classified into. This is "
      "the lookup that says whether a domain is on the global "
      "censorship test list at all, and under which category it is "
      "measured" },

  { .id = "OONI_GLOBAL_OVERVIEW", .name = "OONI — global measurement overview",
    .name_ja = "OONI 全体統計",
    .category = "research", .portal = "https://ooni.org",
    .record_type = "measurement-record",
    .tags = "\"censorship\",\"ooni\",\"coverage\",\"statistics\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.ooni.io/api/_/global_overview",
    .interval = 3600,
    .description = "The running totals of the OONI network: how many measurements "
      "have been collected, from how many countries and how many "
      "distinct networks. It is the denominator for every other OONI "
      "figure and the honest statement of how much of the world is "
      "actually observed" },

  { .id = "IODA_ENTITIES_COUNTRY", .name = "IODA — country entity index",
    .name_ja = "IODA 国エンティティ一覧",
    .category = "research", .portal = "https://ioda.inetintel.cc.gatech.edu",
    .record_type = "measurement-record",
    .tags = "\"outage\",\"ioda\",\"country\",\"connectivity\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.ioda.inetintel.cc.gatech.edu/v2/entities/query?entityType=country",
    .array_path = "data",
    .interval = 86400,
    .page_param = "page",
    .page_start = 0,
    .description = "Every country IODA monitors for internet connectivity, with the "
      "entity code, name and the attributes used to key its outage "
      "signals. It is the identifier table behind every IODA outage "
      "query and includes territories that most country lists omit" },

  { .id = "IODA_ENTITIES_ASN", .name = "IODA — autonomous system entity index",
    .name_ja = "IODA AS一覧",
    .category = "research", .portal = "https://ioda.inetintel.cc.gatech.edu",
    .record_type = "measurement-record",
    .tags = "\"outage\",\"ioda\",\"asn\",\"connectivity\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.ioda.inetintel.cc.gatech.edu/v2/entities/query?entityType=asn&limit=50",
    .array_path = "data",
    .interval = 86400,
    .page_param = "page",
    .page_start = 0,
    .description = "The autonomous systems IODA tracks connectivity signals for, "
      "each with the AS number, name, the country it is attributed to "
      "and the IP address count behind it. Outages are meaningful at "
      "the operator level and this is the operator table" },

  { .id = "IODA_SIGNALS_RAW_COUNTRY", .name = "IODA — raw connectivity signals for a country",
    .name_ja = "IODA 国別生シグナル",
    .category = "research", .portal = "https://ioda.inetintel.cc.gatech.edu",
    .record_type = "measurement-record",
    .tags = "\"outage\",\"ioda\",\"timeseries\",\"shutdown\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.ioda.inetintel.cc.gatech.edu/v2/signals/raw/country/{qU}?from=1754697600&until=1754784000",
    .description = "The three independent measurement sources IODA fuses for one "
      "country over a time window, namely BGP visibility, active "
      "probing response rates and darknet background traffic, as raw "
      "time series at full resolution. Having all three separately is "
      "what distinguishes a routing withdrawal from a filtering "
      "shutdown. The endpoint is windowed rather than paged: it takes "
      "only from and until, rejects a request without them, and ignores "
      "page and limit entirely, so one call returns the whole series "
      "for the window it names" },

  { .id = "IODA_OUTAGE_EVENTS", .name = "IODA — outage events",
    .name_ja = "IODA 障害イベント",
    .category = "research", .portal = "https://ioda.inetintel.cc.gatech.edu",
    .record_type = "outage-record",
    .tags = "\"outage\",\"ioda\",\"shutdown\",\"connectivity\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.ioda.inetintel.cc.gatech.edu/v2/outages/events?from=1754697600&until=1754784000&entityType=country&limit=500",
    .array_path = "data",
    .interval = 900,
    .page_param = "page",
    .page_start = 0,
    .description = "Discrete detected outage events rather than alert points, each "
      "with the affected entity, the datasource that saw it, the start "
      "time, the duration and the severity score. Events are the "
      "deduplicated form and are what you compare against a claimed "
      "shutdown order" },

  { .id = "IHR_HEGEMONY_PREFIXES", .name = "IIJ Internet Health Report — prefix hegemony",
    .name_ja = "IIJ IHR プレフィックス依存度",
    .category = "infrastructure", .portal = "https://ihr.iijlab.net",
    .record_type = "routing-record",
    .tags = "\"bgp\",\"routing\",\"hegemony\",\"ihr\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ihr.iijlab.net/ihr/api/hegemony/prefixes/?originasn={qd}&af=4&country=JP",
    .next_path = "next",
    .description = "Per-prefix AS hegemony scores for the prefixes an origin AS "
      "announces, showing which transit networks the paths to each "
      "prefix actually depend on, with the timestamp, the dependency AS "
      "and the score. Prefix-level dependency is finer than AS-level "
      "and shows where a single transit failure would take a specific "
      "network off the map" },

  { .id = "RIPESTAT_COUNTRY_ASNS", .name = "RIPEstat — autonomous systems by country",
    .name_ja = "RIPEstat 国別AS一覧",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "routing-record",
    .tags = "\"bgp\",\"ripe\",\"country\",\"asn\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/country-asns/data.json?resource={ql}&lod=1",
    .description = "Every AS number registered to or routed from one country, split "
      "into routed and non-routed sets. It is the national network "
      "inventory and the correct starting point for any country-scoped "
      "routing or exposure study" },

  { .id = "RIPESTAT_BGP_UPDATE_ACTIVITY", .name = "RIPEstat — BGP update activity",
    .name_ja = "RIPEstat BGP更新活動",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "routing-record",
    .tags = "\"bgp\",\"ripe\",\"instability\",\"updates\"",
    .mode = HP_JSON, .want = HP_ASN, .free_tier = 1,
    .url = "https://stat.ripe.net/data/bgp-update-activity/data.json?resource=as{qd}",
    .description = "The count of BGP announcements and withdrawals seen for a "
      "resource over time, bucketed by interval. Update churn is the "
      "measurable form of routing instability and spikes line up with "
      "outages, flapping links and hijack events" },

  { .id = "RIPESTAT_ROUTING_HISTORY", .name = "RIPEstat — routing history",
    .name_ja = "RIPEstat 経路履歴",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "routing-record",
    .tags = "\"bgp\",\"ripe\",\"history\",\"prefix\"",
    .mode = HP_JSON, .want = HP_ASN, .free_tier = 1,
    .url = "https://stat.ripe.net/data/routing-history/data.json?resource=as{qd}",
    .description = "Every prefix an AS has originated over time with the visibility "
      "timeline for each and the collector peer counts. This is the "
      "record that shows when a network gained or lost address space, "
      "and it is how a hijack shows up retrospectively" },

  { .id = "RIPESTAT_PREFIX_ROUTING_CONSISTENCY", .name = "RIPEstat — prefix routing consistency",
    .name_ja = "RIPEstat 経路整合性",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "routing-record",
    .tags = "\"bgp\",\"ripe\",\"irr\",\"consistency\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/prefix-routing-consistency/data.json?resource={q}",
    .array_path = "data.routes",
    .description = "Whether what is actually announced in BGP for a prefix matches "
      "what the routing registries say should be announced, per origin "
      "AS. Inconsistency between BGP and the IRR is the classic "
      "precondition for both accidental leaks and deliberate hijacks" },

  { .id = "RIPESTAT_HISTORICAL_WHOIS", .name = "RIPEstat — historical whois",
    .name_ja = "RIPEstat 履歴WHOIS",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "registry-record",
    .tags = "\"whois\",\"ripe\",\"history\",\"registry\"",
    .mode = HP_JSON, .want = HP_ASN, .free_tier = 1,
    .url = "https://stat.ripe.net/data/historical-whois/data.json?resource=as{qd}",
    .description = "Previous versions of the registry objects for a resource, with "
      "the version timestamps and the objects that referenced them. "
      "Registry history is how you establish who controlled an AS or "
      "prefix at the time of an incident rather than who controls it "
      "now" },

  { .id = "RIPESTAT_MAXMIND_GEO", .name = "RIPEstat — MaxMind GeoLite geolocation",
    .name_ja = "RIPEstat 位置情報",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "geolocation-record",
    .tags = "\"geolocation\",\"ripe\",\"ip\",\"maxmind\"",
    .mode = HP_JSON, .want = HP_IP, .free_tier = 1,
    .url = "https://stat.ripe.net/data/maxmind-geo-lite/data.json?resource={q}",
    .array_path = "data.located_resources",
    .lat_key = "latitude",
    .lon_key = "longitude",
    .description = "The MaxMind GeoLite placement for an address or prefix with the "
      "country, city, coordinates and the covered resource ranges. "
      "Served through RIPEstat so it is queryable without a MaxMind "
      "licence, and the covered ranges show how coarse the placement "
      "really is" },

  { .id = "RIPESTAT_IANA_REGISTRY_INFO", .name = "RIPEstat — IANA registry information",
    .name_ja = "RIPEstat IANA登録情報",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "registry-record",
    .tags = "\"iana\",\"ripe\",\"registry\",\"allocation\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/iana-registry-info/data.json?resource={q}",
    .array_path = "data.resources",
    .description = "What the IANA top-level registries say about an address or AS "
      "number: which RIR it was delegated to, the designation, the date "
      "and the status. It is the root of the delegation chain and "
      "settles which registry is authoritative for a resource" },

  { .id = "RIPESTAT_ASN_NEIGHBOURS_HISTORY", .name = "RIPEstat — AS neighbour history",
    .name_ja = "RIPEstat 隣接AS履歴",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "routing-record",
    .tags = "\"bgp\",\"ripe\",\"peering\",\"history\"",
    .mode = HP_JSON, .want = HP_ASN, .free_tier = 1,
    .url = "https://stat.ripe.net/data/asn-neighbours-history/data.json?resource=as{qd}",
    .array_path = "data.neighbours",
    .description = "The historical set of BGP neighbours for an AS with the "
      "timestamps and the power of each adjacency. Changes in the "
      "neighbour set are how transit provider switches, new peering and "
      "sudden dependence on an unexpected upstream become visible" },

  { .id = "RIPE_ATLAS_MEASUREMENT_LATEST", .name = "RIPE Atlas — latest results for a measurement",
    .name_ja = "RIPE Atlas 最新計測結果",
    .category = "research", .portal = "https://atlas.ripe.net",
    .record_type = "measurement-record",
    .tags = "\"ripe\",\"atlas\",\"measurement\",\"activeprobe\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://atlas.ripe.net/api/v2/measurements/{qd}/latest/",
    .description = "The most recent result from every probe participating in one "
      "Atlas measurement, with the probe id, the source address, the "
      "timestamps and the per-probe result payload. This is the raw "
      "active-measurement data behind Atlas rather than a summary, and "
      "it is keyed by probe so per-network divergence is visible" },

  { .id = "RIPE_ATLAS_ANCHOR_DETAIL", .name = "RIPE Atlas — anchor record",
    .name_ja = "RIPE Atlas アンカー詳細",
    .category = "research", .portal = "https://atlas.ripe.net",
    .record_type = "measurement-record",
    .tags = "\"ripe\",\"atlas\",\"anchor\",\"infrastructure\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://atlas.ripe.net/api/v2/anchors/{qd}/",
    .description = "One Atlas anchor in full: the hostname, the IPv4 and IPv6 "
      "addresses, the AS numbers, the hosting organisation, the "
      "geographic coordinates and the linked probe. Anchors are the "
      "well-connected fixed endpoints every other probe measures "
      "against, so they are the reference frame of the whole network" },

  { .id = "MLAB_SITE_SWITCHES", .name = "M-Lab — site switch inventory",
    .name_ja = "M-Lab スイッチ一覧",
    .category = "research", .portal = "https://www.measurementlab.net",
    .record_type = "infrastructure-record",
    .tags = "\"measurement\",\"mlab\",\"infrastructure\",\"network\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://siteinfo.mlab-oti.measurementlab.net/v1/sites/switches.json",
    .interval = 604800,
    .description = "Every M-Lab site with its switch model, uplink speed, the AS it "
      "connects through and the transit provider. M-Lab's measurement "
      "quality depends entirely on where its machines sit in the "
      "network, so the switch and uplink inventory is what qualifies "
      "the data" },

  { .id = "MLAB_SITE_ANNOTATIONS", .name = "M-Lab — site annotations",
    .name_ja = "M-Lab サイト注釈",
    .category = "research", .portal = "https://www.measurementlab.net",
    .record_type = "infrastructure-record",
    .tags = "\"measurement\",\"mlab\",\"infrastructure\",\"geolocation\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://siteinfo.mlab-oti.measurementlab.net/v2/sites/annotations.json",
    .interval = 604800,
    .description = "The geographic and network annotations for every M-Lab site: the "
      "city, country, coordinates, the ASN and the network operator "
      "hosting it. These annotations are the join key for interpreting "
      "any M-Lab throughput or latency measurement geographically" },

  { .id = "RIPENCC_DELEGATED_STATS", .name = "RIPE NCC — extended delegated statistics",
    .name_ja = "RIPE NCC 割当統計",
    .category = "infrastructure", .portal = "https://www.ripe.net",
    .record_type = "registry-record",
    .tags = "\"rir\",\"ripe\",\"allocation\",\"registry\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://ftp.ripe.net/pub/stats/ripencc/delegated-ripencc-extended-latest",
    .csv_no_header = 1,
    .interval = 86400,
    .description = "The authoritative daily record of every IPv4 range, IPv6 range "
      "and AS number RIPE NCC has delegated, with the recipient "
      "country, the allocation date, the status and the opaque "
      "organisation identifier. The organisation id links resources "
      "held by the same entity across separate allocations, which whois "
      "alone will not do" },

  { .id = "ARIN_DELEGATED_STATS", .name = "ARIN — extended delegated statistics",
    .name_ja = "ARIN 割当統計",
    .category = "infrastructure", .portal = "https://www.arin.net",
    .record_type = "registry-record",
    .tags = "\"rir\",\"arin\",\"allocation\",\"registry\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://ftp.arin.net/pub/stats/arin/delegated-arin-extended-latest",
    .csv_no_header = 1,
    .interval = 86400,
    .description = "The same authoritative delegation record for the North American "
      "registry: every address range and AS number ARIN has issued with "
      "the country, date, status and organisation identifier. Together "
      "with the other four RIR files it is the complete public map of "
      "who holds what on the internet" },

  { .id = "LACNIC_DELEGATED_STATS", .name = "LACNIC — extended delegated statistics",
    .name_ja = "LACNIC 割当統計",
    .category = "infrastructure", .portal = "https://www.lacnic.net",
    .record_type = "registry-record",
    .tags = "\"rir\",\"lacnic\",\"allocation\",\"registry\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://ftp.lacnic.net/pub/stats/lacnic/delegated-lacnic-extended-latest",
    .csv_no_header = 1,
    .interval = 86400,
    .description = "The Latin American and Caribbean registry's delegation file, "
      "covering a region whose address holdings are poorly represented "
      "in commercial datasets. Each line gives the resource, the "
      "recipient country, the date, the status and the organisation "
      "identifier" },

  { .id = "TEAM_CYMRU_FULLBOGONS", .name = "Team Cymru — full bogon reference",
    .name_ja = "Team Cymru ボゴン一覧",
    .category = "infrastructure", .portal = "https://www.team-cymru.com",
    .record_type = "routing-record",
    .tags = "\"bgp\",\"bogon\",\"routing\",\"hygiene\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.team-cymru.org/Services/Bogons/fullbogons-ipv4.txt",
    .csv_no_header = 1,
    .interval = 14400,
    .description = "Every IPv4 prefix that is unallocated or reserved and therefore "
      "must never appear in the global routing table or as a packet "
      "source. Traffic from a bogon is definitionally spoofed, and a "
      "bogon appearing in BGP is definitionally a leak or a hijack" },

  { .id = "CERT_PY_WP_POSTS", .name = "CERT-PY Paraguay — advisories as structured records",
    .name_ja = "パラグアイCERT 勧告",
    .category = "threat", .portal = "https://www.cert.gov.py",
    .record_type = "advisory-record",
    .tags = "\"cert\",\"csirt\",\"paraguay\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.cert.gov.py/wp-json/wp/v2/posts?per_page=50&page={qd}",
    .body_keys = "content.rendered",
    .date_keys = "date",
    .detail_key = "id",
    .detail_url = "https://www.cert.gov.py/wp-json/wp/v2/posts/{v}",
    .page_param = "page",
    .page_start = 1,
    .title_keys = "title.rendered",
    .description = "Paraguay's national CERT advisories and alerts as structured "
      "records rather than a rendered page: title, publication and "
      "modification timestamps, slug, category ids and the full body. "
      "Latin American national CERT output is almost absent from "
      "aggregated threat feeds" },

  { .id = "CERT_PH_WP_POSTS", .name = "NCERT Philippines — advisories as structured records",
    .name_ja = "フィリピンNCERT 勧告",
    .category = "threat", .portal = "https://www.ncert.gov.ph",
    .record_type = "advisory-record",
    .tags = "\"cert\",\"csirt\",\"philippines\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.ncert.gov.ph/wp-json/wp/v2/posts?per_page=50&page={qd}",
    .body_keys = "content.rendered",
    .date_keys = "date",
    .detail_key = "id",
    .detail_url = "https://www.ncert.gov.ph/wp-json/wp/v2/posts/{v}",
    .page_param = "page",
    .page_start = 1,
    .title_keys = "title.rendered",
    .description = "The Philippine national computer emergency response team's "
      "advisories and incident notices as structured records with "
      "timestamps, categories and full body text. Southeast Asian CERT "
      "advisories frequently name locally targeted campaigns that never "
      "reach Western reporting" },

  { .id = "CERT_KH_WP_POSTS", .name = "CamCERT Cambodia — advisories as structured records",
    .name_ja = "カンボジアCamCERT 勧告",
    .category = "threat", .portal = "https://www.camcert.gov.kh",
    .record_type = "advisory-record",
    .tags = "\"cert\",\"csirt\",\"cambodia\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.camcert.gov.kh/wp-json/wp/v2/posts?per_page=50&page={qd}",
    .body_keys = "content.rendered",
    .date_keys = "date",
    .detail_key = "id",
    .detail_url = "https://www.camcert.gov.kh/wp-json/wp/v2/posts/{v}",
    .page_param = "page",
    .page_start = 1,
    .title_keys = "title.rendered",
    .description = "Cambodia's national CERT advisories with publication timestamps, "
      "categories and full body content. Cambodia sits at the centre of "
      "the regional scam-compound infrastructure, which makes its "
      "national CERT output unusually relevant to fraud investigation" },

  { .id = "CSIRT_TT_WP_POSTS", .name = "TT-CSIRT Trinidad and Tobago — advisories as structured records",
    .name_ja = "トリニダードCSIRT 勧告",
    .category = "threat", .portal = "https://ttcsirt.gov.tt",
    .record_type = "advisory-record",
    .tags = "\"cert\",\"csirt\",\"caribbean\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://ttcsirt.gov.tt/wp-json/wp/v2/posts?per_page=50&page={qd}",
    .body_keys = "content.rendered",
    .date_keys = "date",
    .detail_key = "id",
    .detail_url = "https://ttcsirt.gov.tt/wp-json/wp/v2/posts/{v}",
    .page_param = "page",
    .page_start = 1,
    .title_keys = "title.rendered",
    .description = "The Trinidad and Tobago national CSIRT's advisories and alerts "
      "as structured records. The Caribbean has almost no "
      "representation in threat intelligence aggregation and this is "
      "one of the few national teams in the region publishing a "
      "machine-readable stream" },

  { .id = "NPCERT_WP_POSTS", .name = "npCERT Nepal — advisories as structured records",
    .name_ja = "ネパールCERT 勧告",
    .category = "threat", .portal = "https://npcert.org",
    .record_type = "advisory-record",
    .tags = "\"cert\",\"csirt\",\"nepal\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://npcert.org/wp-json/wp/v2/posts?per_page=50&page={qd}",
    .body_keys = "content.rendered",
    .date_keys = "date",
    .detail_key = "id",
    .detail_url = "https://npcert.org/wp-json/wp/v2/posts/{v}",
    .page_param = "page",
    .page_start = 1,
    .title_keys = "title.rendered",
    .description = "Nepal's national CERT advisories, incident notes and awareness "
      "material as structured records with timestamps and full body "
      "text. South Asian CERT reporting outside India is effectively "
      "invisible in aggregated feeds" },

  { .id = "MKDCIRT_WP_POSTS", .name = "MKD-CIRT North Macedonia — advisories as structured records",
    .name_ja = "北マケドニアCIRT 勧告",
    .category = "threat", .portal = "https://mkd-cirt.mk",
    .record_type = "advisory-record",
    .tags = "\"cert\",\"csirt\",\"northmacedonia\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://mkd-cirt.mk/wp-json/wp/v2/posts?per_page=50&page={qd}",
    .body_keys = "content.rendered",
    .date_keys = "date",
    .detail_key = "id",
    .detail_url = "https://mkd-cirt.mk/wp-json/wp/v2/posts/{v}",
    .page_param = "page",
    .page_start = 1,
    .title_keys = "title.rendered",
    .description = "The North Macedonian national CIRT's advisories and "
      "vulnerability notices as structured records. Western Balkan CERT "
      "output is thinly covered and this team publishes a substantial, "
      "regularly updated advisory stream" },

  { .id = "CERT_HR_WP_POSTS", .name = "CERT.hr Croatia — advisories as structured records",
    .name_ja = "クロアチアCERT 勧告",
    .category = "threat", .portal = "https://www.cert.hr",
    .record_type = "advisory-record",
    .tags = "\"cert\",\"csirt\",\"croatia\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.cert.hr/wp-json/wp/v2/posts?per_page=50&page={qd}",
    .body_keys = "content.rendered",
    .date_keys = "date",
    .detail_key = "id",
    .detail_url = "https://www.cert.hr/wp-json/wp/v2/posts/{v}",
    .page_param = "page",
    .page_start = 1,
    .title_keys = "title.rendered",
    .description = "Croatia's national CERT advisories as structured records with "
      "timestamps, categories and full body text, rather than the "
      "truncated summaries the RSS feed carries. The JSON form also "
      "exposes the category taxonomy so advisories can be separated "
      "from general news" },

  { .id = "GOVCERT_BG_WP_POSTS", .name = "GovCERT Bulgaria — advisories as structured records",
    .name_ja = "ブルガリアGovCERT 勧告",
    .category = "threat", .portal = "https://www.govcert.bg",
    .record_type = "advisory-record",
    .tags = "\"cert\",\"csirt\",\"bulgaria\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.govcert.bg/wp-json/wp/v2/posts?per_page=50&page={qd}",
    .body_keys = "content.rendered",
    .date_keys = "date",
    .detail_key = "id",
    .detail_url = "https://www.govcert.bg/wp-json/wp/v2/posts/{v}",
    .page_param = "page",
    .page_start = 1,
    .title_keys = "title.rendered",
    .description = "The Bulgarian government CERT's advisories and alerts as "
      "structured records with full body text and the category "
      "taxonomy. The JSON surface carries the whole archive by page "
      "rather than the twenty most recent items the feed exposes" },

  { .id = "ECUCERT_WP_POSTS", .name = "EcuCERT Ecuador — advisories as structured records",
    .name_ja = "エクアドルEcuCERT 勧告",
    .category = "threat", .portal = "https://www.ecucert.gob.ec",
    .record_type = "advisory-record",
    .tags = "\"cert\",\"csirt\",\"ecuador\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.ecucert.gob.ec/wp-json/wp/v2/posts?per_page=50&page={qd}",
    .headers = { "User-Agent: JapanOSINT-research/1.0 (+https://github.com/)" },
    .body_keys = "content.rendered",
    .date_keys = "date",
    .detail_key = "id",
    .detail_url = "https://www.ecucert.gob.ec/wp-json/wp/v2/posts/{v}",
    .page_param = "page",
    .page_start = 1,
    .title_keys = "title.rendered",
    .description = "Ecuador's national CERT advisories as structured records with "
      "timestamps and full body content. EcuCERT sits under the "
      "telecoms regulator and its advisories carry operator-level "
      "detail that a general national CERT would not publish" },

  { .id = "CERTGH_WP_POSTS", .name = "CERT-GH Ghana — advisories as structured records",
    .name_ja = "ガーナCERT 勧告",
    .category = "threat", .portal = "https://cert-gh.org",
    .record_type = "advisory-record",
    .tags = "\"cert\",\"csirt\",\"ghana\",\"advisory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://cert-gh.org/wp-json/wp/v2/posts?per_page=50&page={qd}",
    .headers = { "User-Agent: JapanOSINT-research/1.0 (+https://github.com/)" },
    .body_keys = "content.rendered",
    .date_keys = "date",
    .detail_key = "id",
    .detail_url = "https://cert-gh.org/wp-json/wp/v2/posts/{v}",
    .page_param = "page",
    .page_start = 1,
    .title_keys = "title.rendered",
    .description = "Ghana's national CERT advisories and alerts as structured "
      "records with timestamps and full body text. West African CERT "
      "output is almost entirely absent from aggregated threat feeds "
      "despite the region's fraud and mobile-money attack volume" },
};

HP_REGISTER_TABLE(HP3B19_CYBER)
