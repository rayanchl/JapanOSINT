/* collectors/pivot/table/hp3b19_crypto.c -- batch 19: crypto
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch19.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch19.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch19.crypto.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3B19_CRYPTO[] = {
  { .id = "OFAC_SANCTIONED_CRYPTO_ETH", .name = "OFAC — sanctioned Ethereum addresses",
    .name_ja = "OFAC 制裁対象イーサリアムアドレス",
    .category = "government", .portal = "https://github.com",
    .record_type = "sanctioned-address",
    .tags = "\"sanctions\",\"crypto\",\"ofac\",\"ethereum\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/0xB10C/ofac-sanctioned-digital-currency-addresses/lists/sanctioned_addresses_ETH.txt",
    .csv_no_header = 1,
    .interval = 3600,
    .description = "Every Ethereum address named in a US Treasury OFAC designation, "
      "extracted from the SDN list into a plain address list and "
      "refreshed as designations change. An address on this list may "
      "not lawfully be transacted with by any US person, which makes "
      "membership an operative legal fact rather than a risk score" },

  { .id = "OFAC_SANCTIONED_CRYPTO_BTC", .name = "OFAC — sanctioned Bitcoin addresses",
    .name_ja = "OFAC 制裁対象ビットコインアドレス",
    .category = "government", .portal = "https://github.com",
    .record_type = "sanctioned-address",
    .tags = "\"sanctions\",\"crypto\",\"ofac\",\"bitcoin\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/0xB10C/ofac-sanctioned-digital-currency-addresses/lists/sanctioned_addresses_XBT.txt",
    .csv_no_header = 1,
    .interval = 3600,
    .description = "Bitcoin addresses named in OFAC designations. Bitcoin's "
      "transaction graph is fully public, so a designated address can "
      "be followed forward and backward through its counterparties "
      "without any privileged access" },

  { .id = "MEMPOOL_BLOCKS", .name = "mempool.space — recent Bitcoin blocks",
    .name_ja = "mempool.space 最新ブロック",
    .category = "economy", .portal = "https://mempool.space",
    .record_type = "blockchain-block",
    .tags = "\"crypto\",\"bitcoin\",\"blockchain\",\"mempool\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://mempool.space/api/v1/blocks",
    .interval = 900,
    .description = "Recent Bitcoin blocks with height, hash, timestamp, transaction "
      "count, total fees, weight, the mining pool that found each block "
      "and the difficulty. Pool attribution over time is the measurable "
      "record of hashrate concentration, which is the central question "
      "about the network's censorship resistance" },

  { .id = "BLOCKSTREAM_ADDRESS_TXS", .name = "Blockstream — Bitcoin address transaction history",
    .name_ja = "Blockstream アドレス取引履歴",
    .category = "economy", .portal = "https://blockstream.info",
    .record_type = "crypto-transaction",
    .tags = "\"crypto\",\"bitcoin\",\"transaction\",\"forensics\"",
    .mode = HP_JSON, .want = HP_BTC, .free_tier = 1,
    .url = "https://blockstream.info/api/address/{q}/txs",
    .description = "Full transactions touching an address, each with every input and "
      "output, the addresses and values on both sides, the fee, the "
      "block height and the confirmation time. The input set of a "
      "spending transaction is the evidence behind every "
      "common-ownership clustering heuristic, and this endpoint returns "
      "it unaggregated" },

  { .id = "TRONSCAN_ACCOUNT", .name = "Tronscan — TRON account record",
    .name_ja = "Tronscan TRONアカウント情報",
    .category = "economy", .portal = "https://tronscan.org",
    .record_type = "crypto-address",
    .tags = "\"crypto\",\"tron\",\"address\",\"stablecoin\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://apilist.tronscanapi.com/api/account?address={q}",
    .description = "A TRON account with its TRX and TRC-20 token balances, "
      "transaction counts, creation date and any public name tag. TRON "
      "carries the majority of USDT transfer volume and is consequently "
      "the dominant rail for sanctions evasion and scam proceeds, which "
      "makes it materially more relevant than its market profile "
      "suggests" },

  { .id = "DEFILLAMA_CHAINS", .name = "DefiLlama — blockchain network registry",
    .name_ja = "DefiLlama チェーン一覧",
    .category = "economy", .portal = "https://defillama.com",
    .record_type = "blockchain-network",
    .tags = "\"crypto\",\"defi\",\"blockchain\",\"finance\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.llama.fi/v2/chains",
    .interval = 21600,
    .description = "Blockchain networks with their identifiers, native token symbol, "
      "CoinGecko id and total value locked. The reference table for "
      "resolving a chain identifier seen in a bridge transaction to a "
      "named network" },

  { .id = "COINGECKO_EXCHANGES", .name = "CoinGecko — cryptoasset exchange registry",
    .name_ja = "CoinGecko 取引所一覧",
    .category = "economy", .portal = "https://www.coingecko.com",
    .record_type = "crypto-exchange",
    .tags = "\"crypto\",\"exchange\",\"markets\",\"finance\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.coingecko.com/api/v3/exchanges?per_page=100",
    .interval = 3600,
    .page_param = "page",
    .page_start = 1,
    .description = "Cryptoasset exchanges with country of incorporation, year "
      "established, whether they have a centralised order book, trust "
      "score, reported volume and the URL. Country of incorporation is "
      "the fact that determines which sanctions and AML regime an "
      "exchange is actually subject to" },
};

HP_REGISTER_TABLE(HP3B19_CRYPTO)
