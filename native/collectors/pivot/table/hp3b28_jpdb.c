/* collectors/pivot/table/hp3b28_jpdb.c -- batch 28: jpdb
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch28.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch28.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch28.jpdb.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3B28_JPDB[] = {
  { .id = "JO28_CINII_RESEARCH_OPENSEARCH", .name = "CiNii Research — OpenSearch article/metadata search",
    .name_ja = "CiNii Research オープンサーチ",
    .category = "reference", .portal = "https://cir.nii.ac.jp",
    .record_type = "academic-article-record",
    .tags = "\"japan\",\"cinii\",\"academic\",\"research\",\"bibliography\",\"articles\"",
    .mode = HP_XML, .want = HP_ANY, .free_tier = 1,
    .url = "http://cir.nii.ac.jp/opensearch/articles?q={q}&count=20&lang=ja&format=rss",
    .array_path = "item",
    .date_keys = "prism:publicationDate",
    .id_keys = "dc:identifier,link",
    .interval = 0,
    .link_keys = "link",
    .page_param = "start",
    .page_size = 20,
    .page_start = 1,
    .title_keys = "title",
    .description = "Confirmed live: a real query for 人工知能 (artificial intelligence) "
      "returned an RSS 1.0/RDF feed with 20 item elements carrying "
      "title, link, dc:creator, dc:publisher, prism:publicationDate and "
      "dc:identifier (DOI/NDL_BIB_ID) fields, e.g. a real 1989 article "
      "from 人工知能学会. Filter-honoured check passed: the same endpoint "
      "queried with the nonsense string zzqxxnonexistentquery12345 "
      "returned opensearch:totalResults=0 and zero item elements, "
      "confirming the q parameter genuinely filters rather than "
      "returning a static collection. Full API requires registration "
      "for higher-volume/production use per "
      "support.nii.ac.jp/ja/cinii/api/api_outline, but this "
      "unauthenticated OpenSearch path answered directly." },
};

HP_REGISTER_TABLE(HP3B28_JPDB)
