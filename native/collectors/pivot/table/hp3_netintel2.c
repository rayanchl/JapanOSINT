/* collectors/pivot/table/hp3_netintel2.c -- batch 18: netintel2
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch18.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch18.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch18.netintel2.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3_NETINTEL2[] = {
  { .id = "RIPESTAT_ROUTING_STATUS", .name = "RIPEstat — routing status of a resource",
    .name_ja = "RIPEstat 経路状態",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "routing-status",
    .tags = "\"web\",\"bgp\",\"routing\",\"visibility\"",
    .mode = HP_JSON, .want = HP_ASN, .free_tier = 1,
    .url = "https://stat.ripe.net/data/routing-status/data.json?resource=AS{qd}",
    .description = "How much of a network is actually visible in the global routing "
      "table right now: announced prefix and address counts for IPv4 "
      "and IPv6, the observed origin ASNs, and the first and last time "
      "the resource was seen. A network that disappears from routing "
      "has been disconnected, and this dates it to the hour" },

  { .id = "RIPESTAT_BGP_UPDATES", .name = "RIPEstat — BGP update activity",
    .name_ja = "RIPEstat BGP更新活動",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "bgp-update",
    .tags = "\"web\",\"bgp\",\"routing\",\"churn\"",
    .mode = HP_JSON, .want = HP_ASN, .free_tier = 1,
    .url = "https://stat.ripe.net/data/bgp-updates/data.json?resource=AS{qd}",
    .array_path = "data.updates",
    .description = "The stream of announcements and withdrawals affecting a "
      "resource, with the timestamp, the collecting route collector, "
      "the AS path and the community values on each update. AS-path "
      "churn is the raw evidence of a route leak, a hijack or an "
      "upstream failure" },

  { .id = "RIPESTAT_AS_PATH_LENGTH", .name = "RIPEstat — AS path length distribution",
    .name_ja = "RIPEstat AS経路長分布",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "routing-metric",
    .tags = "\"web\",\"bgp\",\"routing\",\"topology\"",
    .mode = HP_JSON, .want = HP_ASN, .free_tier = 1,
    .url = "https://stat.ripe.net/data/as-path-length/data.json?resource=AS{qd}",
    .array_path = "data.stats",
    .id_keys = "number",
    .title_keys = "location",
    .description = "How many AS hops away a network sits from each route collector, "
      "as a distribution. A sudden increase in path length is the "
      "signature of losing a direct peering relationship and falling "
      "back to transit" },

  { .id = "RIPESTAT_PREFIX_OVERVIEW", .name = "RIPEstat — prefix overview",
    .name_ja = "RIPEstat プレフィックス概要",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "prefix-record",
    .tags = "\"web\",\"bgp\",\"prefix\",\"routing\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/prefix-overview/data.json?resource={q}",
    .id_keys = "resource",
    .title_keys = "resource",
    .description = "The originating ASNs for a prefix with the holder name of each, "
      "whether the prefix is announced, and the related registry "
      "blocks. The first check on whether an address range is being "
      "announced by who it is registered to" },

  { .id = "RIPESTAT_RELATED_PREFIXES", .name = "RIPEstat — related and overlapping prefixes",
    .name_ja = "RIPEstat 関連プレフィックス",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "prefix-record",
    .tags = "\"web\",\"bgp\",\"prefix\",\"routing\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/related-prefixes/data.json?resource={q}",
    .array_path = "data.prefixes",
    .id_keys = "prefix",
    .title_keys = "prefix",
    .description = "Prefixes that overlap or sit adjacent to the queried one, with "
      "their origins and relationship type. A more-specific "
      "announcement by a different origin inside a legitimate block is "
      "the classic hijack shape, and this call surfaces it directly" },

  { .id = "RIPESTAT_COUNTRY_RESOURCE_STATS", .name = "RIPEstat — country routing statistics over time",
    .name_ja = "RIPEstat 国別経路統計",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "country-routing",
    .tags = "\"web\",\"bgp\",\"country\",\"statistics\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/country-resource-stats/data.json?resource={qU}",
    .array_path = "data.stats",
    .id_keys = "stats_date",
    .title_keys = "stats_date",
    .description = "A time series of how many ASNs and prefixes a country announced "
      "at each point. This is the measurement that quantified national "
      "internet shutdowns in Iran, Myanmar and Sudan, and it is "
      "available for every country" },

  { .id = "RIPESTAT_ASN_COUNTRY", .name = "RIPEstat — ASN country of registration",
    .name_ja = "RIPEstat AS登録国",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "asn-geolocation",
    .tags = "\"web\",\"bgp\",\"asn\",\"country\"",
    .mode = HP_JSON, .want = HP_ASN, .free_tier = 1,
    .url = "https://stat.ripe.net/data/rir-stats-country/data.json?resource=AS{qd}",
    .id_keys = "resource",
    .title_keys = "resource",
    .description = "The country each RIR records for a resource, over time. A "
      "resource whose recorded country changes has been transferred, "
      "and the date of that transfer is often the only public trace of "
      "a network changing hands" },

  { .id = "RIPESTAT_ADDRESS_SPACE_HIERARCHY", .name = "RIPEstat — address space allocation hierarchy",
    .name_ja = "RIPEstat アドレス空間階層",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "allocation-hierarchy",
    .tags = "\"web\",\"registry\",\"allocation\",\"routing\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/address-space-hierarchy/data.json?resource={q}",
    .id_keys = "inetnum",
    .title_keys = "netname",
    .description = "The chain of allocations above and below an address block: the "
      "parent allocation, the exact match and every more-specific "
      "assignment, each with its registry description and holder. "
      "Distinguishes an allocation made to a genuine operator from one "
      "sublet to a downstream reseller" },

  { .id = "RIPESTAT_REVERSE_DNS", .name = "RIPEstat — reverse DNS delegation",
    .name_ja = "RIPEstat 逆引きDNS委任",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "reverse-dns",
    .tags = "\"web\",\"dns\",\"reverse\",\"routing\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/reverse-dns/data.json?resource={q}",
    .description = "The reverse DNS zones delegated for an address block and the "
      "nameservers they point at. Reverse delegation names the party "
      "actually operating a range, which is frequently not the "
      "registered holder" },

  { .id = "RIPESTAT_REVERSE_DNS_IP", .name = "RIPEstat — reverse DNS for an address",
    .name_ja = "RIPEstat IP逆引き",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "reverse-dns",
    .tags = "\"web\",\"dns\",\"reverse\",\"ip\"",
    .mode = HP_JSON, .want = HP_IP, .free_tier = 1,
    .url = "https://stat.ripe.net/data/reverse-dns-ip/data.json?resource={q}",
    .description = "The PTR record for a single address. Hostnames in reverse DNS "
      "routinely leak the operator, the datacentre, the rack and the "
      "role of a machine, because nobody treats them as sensitive" },

  { .id = "RIPESTAT_VISIBILITY", .name = "RIPEstat — prefix visibility across collectors",
    .name_ja = "RIPEstat プレフィックス可視性",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "routing-visibility",
    .tags = "\"web\",\"bgp\",\"routing\",\"visibility\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/visibility/data.json?resource={q}",
    .description = "Which RIPE route collectors see a prefix and which do not. "
      "Partial visibility is the measurable signature of a route leak "
      "confined to one region, or of a deliberately scoped announcement" },

  { .id = "RIPESTAT_LOOKING_GLASS", .name = "RIPEstat — looking glass across collectors",
    .name_ja = "RIPEstat ルッキンググラス",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "bgp-lookingglass",
    .tags = "\"web\",\"bgp\",\"routing\",\"lookingglass\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/looking-glass/data.json?resource={q}",
    .array_path = "data.rrcs",
    .description = "The routing table entry for a prefix at each collector peer, "
      "with the AS path, next hop, communities and the last time it "
      "changed. The closest a public tool gets to reading a real "
      "router's table" },

  { .id = "RIPESTAT_ATLAS_PROBES", .name = "RIPEstat — RIPE Atlas probes in a network",
    .name_ja = "RIPEstat Atlas計測プローブ",
    .category = "infrastructure", .portal = "https://stat.ripe.net",
    .record_type = "measurement-probe",
    .tags = "\"web\",\"measurement\",\"atlas\",\"network\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://stat.ripe.net/data/atlas-probes/data.json?resource=AS{qd}",
    .array_path = "data.probes",
    .description = "The RIPE Atlas measurement probes hosted inside a given network, "
      "with their status, coordinates and address family support. "
      "Probes are the vantage points from which that network can be "
      "measured from the inside" },

  { .id = "CLOUDFLARE_DNS_QUERY", .name = "Cloudflare DNS over HTTPS — record resolution",
    .name_ja = "Cloudflare DoH 名前解決",
    .category = "infrastructure", .portal = "https://cloudflare-dns.com",
    .record_type = "dns-record",
    .tags = "\"web\",\"dns\",\"doh\",\"resolution\"",
    .mode = HP_JSON, .want = HP_DOMAIN, .free_tier = 1,
    .url = "https://cloudflare-dns.com/dns-query?name={qh}&type=A",
    .headers = { "Accept: application/dns-json" },
    .description = "Live DNS resolution over HTTPS with the full answer section, "
      "TTLs, the authenticated-data flag and the response status. "
      "Resolving from an independent public resolver is how a locally "
      "poisoned or nationally filtered answer is detected" },

  { .id = "GOOGLE_DNS_QUERY", .name = "Google Public DNS — record resolution",
    .name_ja = "Google Public DNS 名前解決",
    .category = "infrastructure", .portal = "https://dns.google",
    .record_type = "dns-record",
    .tags = "\"web\",\"dns\",\"doh\",\"resolution\"",
    .mode = HP_JSON, .want = HP_DOMAIN, .free_tier = 1,
    .url = "https://dns.google/resolve?name={qh}&type=ANY",
    .description = "A second independent public resolver's view of a name, with the "
      "answer section, TTLs and DNSSEC validation status. Disagreement "
      "between two global resolvers on the same name is direct evidence "
      "of DNS-level manipulation somewhere in the path" },

  { .id = "DNS_TXT_LOOKUP", .name = "Google Public DNS — TXT records",
    .name_ja = "Google Public DNS TXTレコード",
    .category = "infrastructure", .portal = "https://dns.google",
    .record_type = "dns-record",
    .tags = "\"web\",\"dns\",\"txt\",\"spf\"",
    .mode = HP_JSON, .want = HP_DOMAIN, .free_tier = 1,
    .url = "https://dns.google/resolve?name={qh}&type=TXT",
    .description = "TXT records for a domain, which carry SPF and DMARC policy, "
      "domain-verification tokens for every SaaS product the "
      "organisation uses, and DKIM selectors. The verification tokens "
      "are an unintentional inventory of an organisation's vendor "
      "relationships" },

  { .id = "DNS_NS_LOOKUP", .name = "Google Public DNS — NS delegation",
    .name_ja = "Google Public DNS NSレコード",
    .category = "infrastructure", .portal = "https://dns.google",
    .record_type = "dns-record",
    .tags = "\"web\",\"dns\",\"ns\",\"delegation\"",
    .mode = HP_JSON, .want = HP_DOMAIN, .free_tier = 1,
    .url = "https://dns.google/resolve?name={qh}&type=NS",
    .description = "The authoritative nameservers for a domain. Shared nameservers "
      "are one of the strongest pivots in infrastructure analysis, "
      "because operators reuse DNS hosting across otherwise unconnected "
      "domains" },

  { .id = "DNS_CAA_LOOKUP", .name = "Google Public DNS — CAA records",
    .name_ja = "Google Public DNS CAAレコード",
    .category = "infrastructure", .portal = "https://dns.google",
    .record_type = "dns-record",
    .tags = "\"web\",\"dns\",\"caa\",\"tls\"",
    .mode = HP_JSON, .want = HP_DOMAIN, .free_tier = 1,
    .url = "https://dns.google/resolve?name={qh}&type=CAA",
    .description = "Certification Authority Authorization records, naming which CAs "
      "a domain permits to issue for it. A CAA record is a deliberate "
      "policy statement, and a change to it around an incident is "
      "meaningful" },

  { .id = "HUDSONROCK_STYLE_IPINFO", .name = "ipinfo.io — IP attribution record",
    .name_ja = "ipinfo.io IP属性情報",
    .category = "infrastructure", .portal = "https://ipinfo.io",
    .record_type = "ip-record",
    .tags = "\"web\",\"ip\",\"attribution\",\"network\"",
    .mode = HP_JSON, .want = HP_IP, .free_tier = 1,
    .url = "https://ipinfo.io/{q}/json",
    .description = "A third independent IP attribution source giving city, region, "
      "country, coordinates, the ASN and organisation string, the "
      "hostname and the timezone. Three-way agreement between "
      "geolocation providers is the practical standard for asserting "
      "where an address is" },

  { .id = "BGPTOOLS_STYLE_ASRANK", .name = "CAIDA AS Rank — autonomous system ranking",
    .name_ja = "CAIDA AS順位",
    .category = "infrastructure", .portal = "https://asrank.caida.org",
    .record_type = "asn-ranking",
    .tags = "\"web\",\"bgp\",\"ranking\",\"topology\"",
    .mode = HP_JSON, .want = HP_ASN, .free_tier = 1,
    .url = "https://api.asrank.caida.org/v2/restful/asns/{qd}",
    .description = "CAIDA's measured ranking of an autonomous system by customer "
      "cone size, with the organisation, country, degree and the "
      "transit relationships inferred from the global routing table. "
      "Customer cone is the rigorous measure of how much of the "
      "internet depends on a given network" },

  { .id = "CAIDA_ASRANK_ORG", .name = "CAIDA AS Rank — operator organisations",
    .name_ja = "CAIDA 事業者組織",
    .category = "infrastructure", .portal = "https://asrank.caida.org",
    .record_type = "network-operator",
    .tags = "\"web\",\"bgp\",\"organisation\",\"topology\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.asrank.caida.org/v2/restful/organizations?first=100",
    .interval = 86400,
    .description = "The organisations behind autonomous systems as CAIDA resolves "
      "them, with country, rank, the ASNs each controls and the "
      "aggregate customer cone. Resolves the many-ASNs-one-company "
      "problem that raw registry data leaves open" },
};

HP_REGISTER_TABLE(HP3_NETINTEL2)
