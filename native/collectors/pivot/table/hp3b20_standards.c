/* collectors/pivot/table/hp3b20_standards.c -- batch 20: metrology, measurement traceability and Internet standards process
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch20.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch20.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch20.standards.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3B20_STANDARDS[] = {
  { .id = "BIPM_KCDB_DOMAIN", .name = "BIPM KCDB - measurement domains",
    .name_ja = "BIPM KCDB 計測分野",
    .category = "reference", .portal = "https://www.bipm.org/kcdb",
    .record_type = "reference-record",
    .tags = "\"standards\",\"metrology\",\"bipm\",\"kcdb\",\"measurement\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.bipm.org/api/kcdb/referenceData/domain",
    .id_keys = "code",
    .interval = 2592000,
    .title_keys = "name",
    .description = "The three top-level domains the BIPM key comparison database is "
      "partitioned into - physics and metrology, chemistry and biology, "
      "and ionising radiation - with the code each is keyed by. Small, "
      "and it is the root of the whole KCDB namespace: every other "
      "reference list and every calibration capability is scoped by one "
      "of these codes" },

  { .id = "BIPM_KCDB_QUANTITY", .name = "BIPM KCDB - measurable quantities",
    .name_ja = "BIPM KCDB 計測量",
    .category = "reference", .portal = "https://www.bipm.org/kcdb",
    .record_type = "reference-record",
    .tags = "\"standards\",\"metrology\",\"bipm\",\"kcdb\",\"quantity\",\"si\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.bipm.org/api/kcdb/referenceData/quantity",
    .id_keys = "id",
    .interval = 2592000,
    .title_keys = "label,value",
    .description = "Every quantity the international measurement system recognises "
      "as separately calibratable - nearly nineteen hundred of them, "
      "from absorbed dose to water vapour transmission rate - each with "
      "its KCDB identifier, label and value string. This is the "
      "controlled vocabulary that national metrology institutes declare "
      "their capabilities against" },

  { .id = "BIPM_KCDB_METROLOGY_AREA", .name = "BIPM KCDB - metrology areas",
    .name_ja = "BIPM KCDB 計測領域",
    .category = "reference", .portal = "https://www.bipm.org/kcdb",
    .record_type = "reference-record",
    .tags = "\"standards\",\"metrology\",\"bipm\",\"kcdb\",\"area\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.bipm.org/api/kcdb/referenceData/metrologyArea?domainCode=PHYSICS",
    .id_keys = "id",
    .interval = 2592000,
    .title_keys = "label,value",
    .description = "The metrology areas within the physics domain - acoustics, "
      "electricity and magnetism, length, mass, photometry, "
      "thermometry, time and frequency and the rest - with their KCDB "
      "codes. These are the divisions along which the mutual "
      "recognition arrangement is actually organised and audited" },

  { .id = "BIPM_KCDB_COUNTRY", .name = "BIPM KCDB - participating countries and economies",
    .name_ja = "BIPM KCDB 参加国",
    .category = "reference", .portal = "https://www.bipm.org/kcdb",
    .record_type = "reference-record",
    .tags = "\"standards\",\"metrology\",\"bipm\",\"kcdb\",\"country\",\"nmi\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.bipm.org/api/kcdb/referenceData/country",
    .id_keys = "id",
    .interval = 2592000,
    .title_keys = "label,value",
    .description = "The hundred and five states and economies that participate in "
      "the CIPM mutual recognition arrangement, each with its KCDB "
      "identifier and label. Membership of the MRA is what makes one "
      "country's calibration certificate acceptable in another, so this "
      "list is the map of where measurement traceability crosses "
      "borders" },

  { .id = "RFC_EDITOR_ERRATA", .name = "RFC Editor - published errata",
    .name_ja = "RFCエディタ 正誤表",
    .category = "reference", .portal = "https://www.rfc-editor.org/errata.php",
    .record_type = "errata-record",
    .tags = "\"standards\",\"ietf\",\"rfc\",\"errata\",\"internet\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.rfc-editor.org/errata.json",
    .body_keys = "orig_text",
    .date_keys = "submit_date",
    .id_keys = "errata_id",
    .interval = 86400,
    .title_keys = "doc-id",
    .description = "Every erratum ever filed against an RFC, with the errata "
      "identifier, the RFC it corrects, its verification status, "
      "whether it is technical or editorial, the section, the original "
      "text, the corrected text, the submitter and the notes from the "
      "verifying stream. An RFC's errata are where the standard is "
      "actually wrong, and no other feed carries them as structured "
      "data" },

  { .id = "RFC_EDITOR_INDEX", .name = "RFC Editor - full RFC index",
    .name_ja = "RFCエディタ RFC索引",
    .category = "reference", .portal = "https://www.rfc-editor.org",
    .record_type = "standard-record",
    .tags = "\"standards\",\"ietf\",\"rfc\",\"index\",\"internet\"",
    .mode = HP_XML, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.rfc-editor.org/rfc-index.xml",
    .array_path = "rfc-entry",
    .id_keys = "doc-id",
    .interval = 86400,
    .title_keys = "title",
    .description = "The authoritative RFC index in XML: every RFC ever published "
      "with its number, title, author list, publication date, page "
      "count, format, current status, stream, working group, DOI and "
      "the obsoletes, obsoleted-by, updates and updated-by relations. "
      "The relation graph is the part that says whether a standard is "
      "still live" },

  { .id = "IETF_DT_PERSON", .name = "IETF Datatracker - persons",
    .name_ja = "IETF データトラッカー 人物",
    .category = "reference", .portal = "https://datatracker.ietf.org",
    .record_type = "person-record",
    .tags = "\"standards\",\"ietf\",\"datatracker\",\"person\",\"participant\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://datatracker.ietf.org/api/v1/person/person/?format=json&limit=100",
    .array_path = "objects",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "name",
    .description = "Every person the IETF datatracker knows: canonical name, ASCII "
      "transliteration, short form, biography, pronouns and the linked "
      "user account. This is the identity table behind every draft "
      "authorship, working group role, review assignment and nomination "
      "in the rest of the datatracker" },

  { .id = "IETF_DT_DOCEVENT", .name = "IETF Datatracker - document events",
    .name_ja = "IETF データトラッカー 文書イベント",
    .category = "reference", .portal = "https://datatracker.ietf.org",
    .record_type = "event-record",
    .tags = "\"standards\",\"ietf\",\"datatracker\",\"docevent\",\"process\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://datatracker.ietf.org/api/v1/doc/docevent/?format=json&limit=100",
    .array_path = "objects",
    .date_keys = "time",
    .id_keys = "id",
    .interval = 21600,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "desc",
    .description = "The event log of the standards process: every state change, "
      "ballot, publication request, expiry and comment against every "
      "draft, with the actor, the timestamp, the revision and the "
      "free-text description. This is the audit trail that turns a "
      "document's final status into the story of how it got there" },

  { .id = "IETF_DT_REVIEWREQUEST", .name = "IETF Datatracker - review requests",
    .name_ja = "IETF データトラッカー レビュー依頼",
    .category = "reference", .portal = "https://datatracker.ietf.org",
    .record_type = "review-record",
    .tags = "\"standards\",\"ietf\",\"datatracker\",\"review\",\"directorate\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://datatracker.ietf.org/api/v1/review/reviewrequest/?format=json&limit=100",
    .array_path = "objects",
    .date_keys = "time",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "doc,type",
    .description = "Directorate review requests - security, operations, general "
      "area, routing - naming the document, the requested revision, the "
      "requesting party, the review team, the type, the deadline and "
      "the current state. Which drafts got a security review, and which "
      "did not, is a directly useful fact about a standard" },

  { .id = "IETF_DT_NOMCOM", .name = "IETF Datatracker - nominating committees",
    .name_ja = "IETF データトラッカー 指名委員会",
    .category = "reference", .portal = "https://datatracker.ietf.org",
    .record_type = "committee-record",
    .tags = "\"standards\",\"ietf\",\"datatracker\",\"nomcom\",\"governance\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://datatracker.ietf.org/api/v1/nomcom/nomcom/?format=json&limit=100",
    .array_path = "objects",
    .id_keys = "id",
    .interval = 604800,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "group",
    .description = "Every IETF nominating committee: the group it belongs to, the "
      "first call for volunteers, the initial text, whether it is still "
      "accepting volunteers, the reminder interval and its public key. "
      "NomCom is how the IESG and IAB are actually staffed, which makes "
      "this the governance record of the organisation" },
};

HP_REGISTER_TABLE(HP3B20_STANDARDS)
