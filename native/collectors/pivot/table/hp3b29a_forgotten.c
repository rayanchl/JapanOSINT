/* collectors/pivot/table/hp3b29a_forgotten.c -- batch 29: forgotten
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch29.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch29.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch29.forgotten.txt — scaffolded from it ONCE.
 * This file is the maintained copy from here on: repairs carry the live counts
 * that justify them, which the manifest has nowhere to put. Regenerating over
 * it reverts them, so the generator refuses to without --force. See CLAUDE.md,
 * "Batch tooling".
 */
#include "lib/hpengine.h"

static const hp_source HP3B29A_FORGOTTEN[] = {
  { .id = "JO29A_FGT_CA_DATA_CKAN", .name = "State of California (data.ca.gov) — CKAN dataset catalogue",
    .name_ja = "カリフォルニア州 オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://data.ca.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.ca.gov/api/3/action/package_search?rows=1000&start=0&sort=id%20asc",
    .headers = { "User-Agent: JapanOSINT/1.0" },
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 1000,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The State of California (data.ca.gov) open-data portal runs CKAN "
      "and exposes package_search without authentication; at probe time "
      "(2026-09-14) result.count was 4581 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 1000 (5 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_SANANTONIO_CKAN", .name = "City of San Antonio — CKAN dataset catalogue",
    .name_ja = "サンアントニオ市 オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://data.sanantonio.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.sanantonio.gov/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .headers = { "User-Agent: JapanOSINT/1.0" },
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The City of San Antonio open-data portal runs CKAN and exposes "
      "package_search without authentication; at probe time "
      "(2026-09-14) result.count was 164 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (2 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_HOUSTON_CKAN", .name = "City of Houston — CKAN dataset catalogue",
    .name_ja = "ヒューストン市 オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://data.houstontx.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.houstontx.gov/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .headers = { "User-Agent: JapanOSINT/1.0" },
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The City of Houston open-data portal runs CKAN and exposes "
      "package_search without authentication; at probe time "
      "(2026-09-14) result.count was 94 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (1 page). Walked with sort=id asc: the default CKAN order "
      "(score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_MILWAUKEE_CKAN", .name = "City of Milwaukee — CKAN dataset catalogue",
    .name_ja = "ミルウォーキー市 オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://data.milwaukee.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.milwaukee.gov/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .headers = { "User-Agent: JapanOSINT/1.0" },
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The City of Milwaukee open-data portal runs CKAN and exposes "
      "package_search without authentication; at probe time "
      "(2026-09-14) result.count was 186 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (2 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_VIRGINIA_CKAN", .name = "Commonwealth of Virginia — CKAN dataset catalogue",
    .name_ja = "バージニア州 オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://data.virginia.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.virginia.gov/api/3/action/package_search?rows=1000&start=0&sort=id%20asc",
    .headers = { "User-Agent: JapanOSINT/1.0" },
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_max = 36,
    .page_param = "start",
    .page_size = 1000,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The Commonwealth of Virginia open-data portal runs CKAN and "
      "exposes package_search without authentication; at probe time "
      "(2026-09-14) result.count was 32458 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 1000 (33 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_OPENDATA_DK_CKAN", .name = "OpenData.dk (Danish municipalities) — CKAN dataset catalogue",
    .name_ja = "デンマーク自治体 OpenData.dk オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://admin.opendata.dk",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"denmark\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://admin.opendata.dk/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The OpenData.dk (Danish municipalities) open-data portal runs "
      "CKAN and exposes package_search without authentication; at probe "
      "time (2026-09-14) result.count was 631 datasets. Each record is "
      "one published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (7 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_GVA_DADESOBERTES_CKAN", .name = "Generalitat Valenciana — CKAN dataset catalogue",
    .name_ja = "バレンシア州政府 オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://dadesobertes.gva.es",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"spain\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://dadesobertes.gva.es/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_max = 17,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The Generalitat Valenciana open-data portal runs CKAN and "
      "exposes package_search without authentication; at probe time "
      "(2026-09-14) result.count was 1307 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (14 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_BELOHORIZONTE_CKAN", .name = "Prefeitura de Belo Horizonte — CKAN dataset catalogue",
    .name_ja = "ベロオリゾンテ市 オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://dados.pbh.gov.br",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"brazil\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://dados.pbh.gov.br/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The Prefeitura de Belo Horizonte open-data portal runs CKAN and "
      "exposes package_search without authentication; at probe time "
      "(2026-09-14) result.count was 606 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (7 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_ALAGOAS_CKAN", .name = "Governo de Alagoas — CKAN dataset catalogue",
    .name_ja = "アラゴアス州 オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://dados.al.gov.br",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"brazil\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://dados.al.gov.br/catalogo/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The Governo de Alagoas open-data portal runs CKAN and exposes "
      "package_search without authentication; at probe time "
      "(2026-09-14) result.count was 394 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (4 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_OKLAHOMA_CKAN", .name = "State of Oklahoma — CKAN dataset catalogue",
    .name_ja = "オクラホマ州 オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://data.ok.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.ok.gov/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .headers = { "User-Agent: JapanOSINT/1.0" },
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The State of Oklahoma open-data portal runs CKAN and exposes "
      "package_search without authentication; at probe time "
      "(2026-09-14) result.count was 395 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (4 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_KYIV_CITY_CKAN", .name = "Kyiv City State Administration — CKAN dataset catalogue",
    .name_ja = "キーウ市 オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://data.kyivcity.gov.ua",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"ukraine\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.kyivcity.gov.ua/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The Kyiv City State Administration open-data portal runs CKAN "
      "and exposes package_search without authentication; at probe time "
      "(2026-09-14) result.count was 93 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (1 page). Walked with sort=id asc: the default CKAN order "
      "(score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_OPENCOM_NO_CKAN", .name = "OpenCom.no (Norwegian municipalities) — CKAN dataset catalogue",
    .name_ja = "ノルウェー自治体 OpenCom オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://opencom.no",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"norway\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://opencom.no/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The OpenCom.no (Norwegian municipalities) open-data portal runs "
      "CKAN and exposes package_search without authentication; at probe "
      "time (2026-09-14) result.count was 360 datasets. Each record is "
      "one published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (4 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_HELSINKI_HRI_CKAN", .name = "Helsinki Region Infoshare — CKAN dataset catalogue",
    .name_ja = "ヘルシンキ地域 HRI オープンデータカタログ（CKAN）",
    .category = "government", .portal = "https://hri.fi",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"finland\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://hri.fi/data/api/3/action/package_search?rows=100&start=0&sort=id%20asc",
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 86400,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "The Helsinki Region Infoshare open-data portal runs CKAN and "
      "exposes package_search without authentication; at probe time "
      "(2026-09-14) result.count was 593 datasets. Each record is one "
      "published dataset with its id, name, title, publishing "
      "organisation, notes, tags, licence, modification dates and the "
      "resources array of downloadable files. Paged by start in steps "
      "of 100 (6 pages). Walked with sort=id asc: the default CKAN "
      "order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_BRISBANE_ODS", .name = "Brisbane City Council — OpenDataSoft dataset catalogue",
    .name_ja = "ブリスベン市 オープンデータカタログ（OpenDataSoft）",
    .category = "government", .portal = "https://data.brisbane.qld.gov.au",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"opendatasoft\",\"catalog\",\"forgotten\",\"australia\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.brisbane.qld.gov.au/api/explore/v2.1/catalog/datasets?limit=100&offset=0&order_by=dataset_id",
    .array_path = "results",
    .body_keys = "metas.default.description",
    .date_keys = "metas.default.modified",
    .id_keys = "dataset_id",
    .interval = 86400,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "metas.default.title",
    .description = "The Brisbane City Council portal runs OpenDataSoft; its Explore "
      "v2.1 catalog endpoint is open and at probe time (2026-09-14) "
      "reported total_count=446 datasets. Each record is one dataset "
      "with dataset_id, the default metadata block (title, description, "
      "publisher, theme, keywords, licence, modified date) and whether "
      "it holds queryable records. Paged by offset in steps of 100 (5 "
      "pages). Walked with order_by=dataset_id so offset paging reads a "
      "stable order." },

  { .id = "JO29A_FGT_ORLEANS_METROPOLE_ODS", .name = "Orléans Métropole — OpenDataSoft dataset catalogue",
    .name_ja = "オルレアン都市圏 オープンデータカタログ（OpenDataSoft）",
    .category = "government", .portal = "https://data.orleans-metropole.fr",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"opendatasoft\",\"catalog\",\"forgotten\",\"france\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.orleans-metropole.fr/api/explore/v2.1/catalog/datasets?limit=100&offset=0&order_by=dataset_id",
    .array_path = "results",
    .body_keys = "metas.default.description",
    .date_keys = "metas.default.modified",
    .id_keys = "dataset_id",
    .interval = 86400,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "metas.default.title",
    .description = "The Orléans Métropole portal runs OpenDataSoft; its Explore v2.1 "
      "catalog endpoint is open and at probe time (2026-09-14) reported "
      "total_count=213 datasets. Each record is one dataset with "
      "dataset_id, the default metadata block (title, description, "
      "publisher, theme, keywords, licence, modified date) and whether "
      "it holds queryable records. Paged by offset in steps of 100 (3 "
      "pages). Walked with order_by=dataset_id so offset paging reads a "
      "stable order." },

  { .id = "JO29A_FGT_HAUTS_DE_SEINE_ODS", .name = "Département des Hauts-de-Seine — OpenDataSoft dataset catalogue",
    .name_ja = "オー＝ド＝セーヌ県 オープンデータカタログ（OpenDataSoft）",
    .category = "government", .portal = "https://opendata.hauts-de-seine.fr",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"opendatasoft\",\"catalog\",\"forgotten\",\"france\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://opendata.hauts-de-seine.fr/api/explore/v2.1/catalog/datasets?limit=100&offset=0&order_by=dataset_id",
    .array_path = "results",
    .body_keys = "metas.default.description",
    .date_keys = "metas.default.modified",
    .id_keys = "dataset_id",
    .interval = 86400,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "metas.default.title",
    .description = "The Département des Hauts-de-Seine portal runs OpenDataSoft; its "
      "Explore v2.1 catalog endpoint is open and at probe time "
      "(2026-09-14) reported total_count=253 datasets. Each record is "
      "one dataset with dataset_id, the default metadata block (title, "
      "description, publisher, theme, keywords, licence, modified date) "
      "and whether it holds queryable records. Paged by offset in steps "
      "of 100 (3 pages). Walked with order_by=dataset_id so offset "
      "paging reads a stable order." },

  { .id = "JO29A_FGT_BRUXELLES_VILLE_ODS", .name = "Ville de Bruxelles — OpenDataSoft dataset catalogue",
    .name_ja = "ブリュッセル市 オープンデータカタログ（OpenDataSoft）",
    .category = "government", .portal = "https://opendata.bruxelles.be",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"opendatasoft\",\"catalog\",\"forgotten\",\"belgium\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://opendata.bruxelles.be/api/explore/v2.1/catalog/datasets?limit=100&offset=0&order_by=dataset_id",
    .array_path = "results",
    .body_keys = "metas.default.description",
    .date_keys = "metas.default.modified",
    .id_keys = "dataset_id",
    .interval = 86400,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "metas.default.title",
    .description = "The Ville de Bruxelles portal runs OpenDataSoft; its Explore "
      "v2.1 catalog endpoint is open and at probe time (2026-09-14) "
      "reported total_count=208 datasets. Each record is one dataset "
      "with dataset_id, the default metadata block (title, description, "
      "publisher, theme, keywords, licence, modified date) and whether "
      "it holds queryable records. Paged by offset in steps of 100 (3 "
      "pages). Walked with order_by=dataset_id so offset paging reads a "
      "stable order." },

  { .id = "JO29A_FGT_WALLONIA_ODWB_ODS", .name = "Open Data Wallonie-Bruxelles — OpenDataSoft dataset catalogue",
    .name_ja = "ワロン・ブリュッセル オープンデータ オープンデータカタログ（OpenDataSoft）",
    .category = "government", .portal = "https://www.odwb.be",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"opendatasoft\",\"catalog\",\"forgotten\",\"belgium\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.odwb.be/api/explore/v2.1/catalog/datasets?limit=100&offset=0&order_by=dataset_id",
    .array_path = "results",
    .body_keys = "metas.default.description",
    .date_keys = "metas.default.modified",
    .id_keys = "dataset_id",
    .interval = 86400,
    .page_max = 16,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "metas.default.title",
    .description = "The Open Data Wallonie-Bruxelles portal runs OpenDataSoft; its "
      "Explore v2.1 catalog endpoint is open and at probe time "
      "(2026-09-14) reported total_count=1290 datasets. Each record is "
      "one dataset with dataset_id, the default metadata block (title, "
      "description, publisher, theme, keywords, licence, modified date) "
      "and whether it holds queryable records. Paged by offset in steps "
      "of 100 (13 pages). Walked with order_by=dataset_id so offset "
      "paging reads a stable order." },

  { .id = "JO29A_FGT_POTSDAM_ODS", .name = "Landeshauptstadt Potsdam — OpenDataSoft dataset catalogue",
    .name_ja = "ポツダム市 オープンデータカタログ（OpenDataSoft）",
    .category = "government", .portal = "https://opendata.potsdam.de",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"opendatasoft\",\"catalog\",\"forgotten\",\"germany\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://opendata.potsdam.de/api/explore/v2.1/catalog/datasets?limit=100&offset=0&order_by=dataset_id",
    .array_path = "results",
    .body_keys = "metas.default.description",
    .date_keys = "metas.default.modified",
    .id_keys = "dataset_id",
    .interval = 86400,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "metas.default.title",
    .description = "The Landeshauptstadt Potsdam portal runs OpenDataSoft; its "
      "Explore v2.1 catalog endpoint is open and at probe time "
      "(2026-09-14) reported total_count=114 datasets. Each record is "
      "one dataset with dataset_id, the default metadata block (title, "
      "description, publisher, theme, keywords, licence, modified date) "
      "and whether it holds queryable records. Paged by offset in steps "
      "of 100 (2 pages). Walked with order_by=dataset_id so offset "
      "paging reads a stable order." },

  { .id = "JO29A_FGT_CAISSE_DEPOTS_ODS", .name = "Caisse des Dépôts — OpenDataSoft dataset catalogue",
    .name_ja = "フランス預金供託金庫 オープンデータカタログ（OpenDataSoft）",
    .category = "government", .portal = "https://opendata.caissedesdepots.fr",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"opendatasoft\",\"catalog\",\"forgotten\",\"france\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://opendata.caissedesdepots.fr/api/explore/v2.1/catalog/datasets?limit=100&offset=0&order_by=dataset_id",
    .array_path = "results",
    .body_keys = "metas.default.description",
    .date_keys = "metas.default.modified",
    .id_keys = "dataset_id",
    .interval = 86400,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "metas.default.title",
    .description = "The Caisse des Dépôts portal runs OpenDataSoft; its Explore v2.1 "
      "catalog endpoint is open and at probe time (2026-09-14) reported "
      "total_count=87 datasets. Each record is one dataset with "
      "dataset_id, the default metadata block (title, description, "
      "publisher, theme, keywords, licence, modified date) and whether "
      "it holds queryable records. Paged by offset in steps of 100 (1 "
      "page). Walked with order_by=dataset_id so offset paging reads a "
      "stable order." },

  { .id = "JO29A_FGT_MULHOUSE_ODS", .name = "Mulhouse Alsace Agglomération — OpenDataSoft dataset catalogue",
    .name_ja = "ミュルーズ都市圏 オープンデータカタログ（OpenDataSoft）",
    .category = "government", .portal = "https://data.mulhouse-alsace.fr",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"opendatasoft\",\"catalog\",\"forgotten\",\"france\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.mulhouse-alsace.fr/api/explore/v2.1/catalog/datasets?limit=100&offset=0&order_by=dataset_id",
    .array_path = "results",
    .body_keys = "metas.default.description",
    .date_keys = "metas.default.modified",
    .id_keys = "dataset_id",
    .interval = 86400,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "metas.default.title",
    .description = "The Mulhouse Alsace Agglomération portal runs OpenDataSoft; its "
      "Explore v2.1 catalog endpoint is open and at probe time "
      "(2026-09-14) reported total_count=146 datasets. Each record is "
      "one dataset with dataset_id, the default metadata block (title, "
      "description, publisher, theme, keywords, licence, modified date) "
      "and whether it holds queryable records. Paged by offset in steps "
      "of 100 (2 pages). Walked with order_by=dataset_id so offset "
      "paging reads a stable order." },

  { .id = "JO29A_FGT_SOMERVILLE_SOCRATA", .name = "City of Somerville, Massachusetts — Socrata asset catalogue",
    .name_ja = "サマービル市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.somervillema.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.somervillema.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Somerville, "
      "Massachusetts (data.somervillema.gov): every asset the domain "
      "publishes — datasets, maps, charts, filtered views and external "
      "links. At probe time (2026-09-14) resultSetSize was 53. Each "
      "record carries resource.id (the 4-4 asset id), name, "
      "description, type, attribution, created/updated timestamps, "
      "column names, the domain category and tags, the owner and the "
      "permalink. Paged by offset in steps of 100 (1 page). Walked with "
      "order=dataset_id: unordered offset paging was measured on "
      "data.bayareametro.gov to repeat 2 assets and skip 2 others "
      "across 35 pages, while the ordered walk returned all 3417 "
      "distinct ids." },

  { .id = "JO29A_FGT_ACT_GOV_SOCRATA", .name = "ACT Government (Australian Capital Territory) — Socrata asset "
      "catalogue",
    .name_ja = "オーストラリア首都特別地域 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.act.gov.au",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"australia\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.act.gov.au&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for ACT Government (Australian "
      "Capital Territory) (data.act.gov.au): every asset the domain "
      "publishes — datasets, maps, charts, filtered views and external "
      "links. At probe time (2026-09-14) resultSetSize was 784. Each "
      "record carries resource.id (the 4-4 asset id), name, "
      "description, type, attribution, created/updated timestamps, "
      "column names, the domain category and tags, the owner and the "
      "permalink. Paged by offset in steps of 100 (8 pages). Walked "
      "with order=dataset_id: unordered offset paging was measured on "
      "data.bayareametro.gov to repeat 2 assets and skip 2 others "
      "across 35 pages, while the ordered walk returned all 3417 "
      "distinct ids." },

  { .id = "JO29A_FGT_VERMONT_SOCRATA", .name = "State of Vermont — Socrata asset catalogue",
    .name_ja = "バーモント州 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.vermont.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.vermont.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for State of Vermont "
      "(data.vermont.gov): every asset the domain publishes — datasets, "
      "maps, charts, filtered views and external links. At probe time "
      "(2026-09-14) resultSetSize was 270. Each record carries "
      "resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (3 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_BUFFALO_SOCRATA", .name = "City of Buffalo, New York — Socrata asset catalogue",
    .name_ja = "バッファロー市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.buffalony.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.buffalony.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Buffalo, New York "
      "(data.buffalony.gov): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 106. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (2 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_NORFOLK_SOCRATA", .name = "City of Norfolk, Virginia — Socrata asset catalogue",
    .name_ja = "ノーフォーク市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.norfolk.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.norfolk.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Norfolk, Virginia "
      "(data.norfolk.gov): every asset the domain publishes — datasets, "
      "maps, charts, filtered views and external links. At probe time "
      "(2026-09-14) resultSetSize was 221. Each record carries "
      "resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (3 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_GAINESVILLE_SOCRATA", .name = "City of Gainesville, Florida — Socrata asset catalogue",
    .name_ja = "ゲインズビル市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.cityofgainesville.org",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.cityofgainesville.org&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Gainesville, Florida "
      "(data.cityofgainesville.org): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 209. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (3 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_PRINCE_GEORGES_SOCRATA", .name = "Prince George's County, Maryland — Socrata asset catalogue",
    .name_ja = "プリンスジョージズ郡 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.princegeorgescountymd.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.princegeorgescountymd.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for Prince George's County, "
      "Maryland (data.princegeorgescountymd.gov): every asset the "
      "domain publishes — datasets, maps, charts, filtered views and "
      "external links. At probe time (2026-09-14) resultSetSize was "
      "248. Each record carries resource.id (the 4-4 asset id), name, "
      "description, type, attribution, created/updated timestamps, "
      "column names, the domain category and tags, the owner and the "
      "permalink. Paged by offset in steps of 100 (3 pages). Walked "
      "with order=dataset_id: unordered offset paging was measured on "
      "data.bayareametro.gov to repeat 2 assets and skip 2 others "
      "across 35 pages, while the ordered walk returned all 3417 "
      "distinct ids." },

  { .id = "JO29A_FGT_EDMONTON_SOCRATA", .name = "City of Edmonton — Socrata asset catalogue",
    .name_ja = "エドモントン市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.edmonton.ca",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"canada\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.edmonton.ca&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_max = 24,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Edmonton "
      "(data.edmonton.ca): every asset the domain publishes — datasets, "
      "maps, charts, filtered views and external links. At probe time "
      "(2026-09-14) resultSetSize was 2088. Each record carries "
      "resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (21 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_CALGARY_SOCRATA", .name = "City of Calgary — Socrata asset catalogue",
    .name_ja = "カルガリー市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.calgary.ca",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"canada\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.calgary.ca&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_max = 13,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Calgary "
      "(data.calgary.ca): every asset the domain publishes — datasets, "
      "maps, charts, filtered views and external links. At probe time "
      "(2026-09-14) resultSetSize was 937. Each record carries "
      "resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (10 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_WINNIPEG_SOCRATA", .name = "City of Winnipeg — Socrata asset catalogue",
    .name_ja = "ウィニペグ市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.winnipeg.ca",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"canada\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.winnipeg.ca&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Winnipeg "
      "(data.winnipeg.ca): every asset the domain publishes — datasets, "
      "maps, charts, filtered views and external links. At probe time "
      "(2026-09-14) resultSetSize was 521. Each record carries "
      "resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (6 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_NEW_ORLEANS_SOCRATA", .name = "City of New Orleans — Socrata asset catalogue",
    .name_ja = "ニューオーリンズ市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.nola.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.nola.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of New Orleans "
      "(data.nola.gov): every asset the domain publishes — datasets, "
      "maps, charts, filtered views and external links. At probe time "
      "(2026-09-14) resultSetSize was 298. Each record carries "
      "resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (3 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_CINCINNATI_SOCRATA", .name = "City of Cincinnati — Socrata asset catalogue",
    .name_ja = "シンシナティ市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.cincinnati-oh.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.cincinnati-oh.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Cincinnati "
      "(data.cincinnati-oh.gov): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 163. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (2 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_FORTWORTH_SOCRATA", .name = "City of Fort Worth — Socrata asset catalogue",
    .name_ja = "フォートワース市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.fortworthtexas.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.fortworthtexas.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Fort Worth "
      "(data.fortworthtexas.gov): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 14. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (1 page). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_HONOLULU_SOCRATA", .name = "City and County of Honolulu — Socrata asset catalogue",
    .name_ja = "ホノルル市郡 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.honolulu.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.honolulu.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City and County of Honolulu "
      "(data.honolulu.gov): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 451. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (5 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_PROVIDENCE_SOCRATA", .name = "City of Providence — Socrata asset catalogue",
    .name_ja = "プロビデンス市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.providenceri.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.providenceri.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Providence "
      "(data.providenceri.gov): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 297. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (3 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_ROSEVILLE_SOCRATA", .name = "City of Roseville, California — Socrata asset catalogue",
    .name_ja = "ローズビル市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.roseville.ca.us",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.roseville.ca.us&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Roseville, "
      "California (data.roseville.ca.us): every asset the domain "
      "publishes — datasets, maps, charts, filtered views and external "
      "links. At probe time (2026-09-14) resultSetSize was 206. Each "
      "record carries resource.id (the 4-4 asset id), name, "
      "description, type, attribution, created/updated timestamps, "
      "column names, the domain category and tags, the owner and the "
      "permalink. Paged by offset in steps of 100 (3 pages). Walked "
      "with order=dataset_id: unordered offset paging was measured on "
      "data.bayareametro.gov to repeat 2 assets and skip 2 others "
      "across 35 pages, while the ordered walk returned all 3417 "
      "distinct ids." },

  { .id = "JO29A_FGT_BAYAREA_METRO_SOCRATA", .name = "Metropolitan Transportation Commission / ABAG (Bay Area Metro) — "
      "Socrata asset catalogue",
    .name_ja = "サンフランシスコ湾岸都市圏 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.bayareametro.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.bayareametro.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_max = 38,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for Metropolitan Transportation "
      "Commission / ABAG (Bay Area Metro) (data.bayareametro.gov): "
      "every asset the domain publishes — datasets, maps, charts, "
      "filtered views and external links. At probe time (2026-09-14) "
      "resultSetSize was 3417. Each record carries resource.id (the 4-4 "
      "asset id), name, description, type, attribution, created/updated "
      "timestamps, column names, the domain category and tags, the "
      "owner and the permalink. Paged by offset in steps of 100 (35 "
      "pages). Walked with order=dataset_id: unordered offset paging "
      "was measured on data.bayareametro.gov to repeat 2 assets and "
      "skip 2 others across 35 pages, while the ordered walk returned "
      "all 3417 distinct ids." },

  { .id = "JO29A_FGT_BATONROUGE_SOCRATA", .name = "City of Baton Rouge / Parish of East Baton Rouge — Socrata asset "
      "catalogue",
    .name_ja = "バトンルージュ オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.brla.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.brla.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Baton Rouge / Parish "
      "of East Baton Rouge (data.brla.gov): every asset the domain "
      "publishes — datasets, maps, charts, filtered views and external "
      "links. At probe time (2026-09-14) resultSetSize was 498. Each "
      "record carries resource.id (the 4-4 asset id), name, "
      "description, type, attribution, created/updated timestamps, "
      "column names, the domain category and tags, the owner and the "
      "permalink. Paged by offset in steps of 100 (5 pages). Walked "
      "with order=dataset_id: unordered offset paging was measured on "
      "data.bayareametro.gov to repeat 2 assets and skip 2 others "
      "across 35 pages, while the ordered walk returned all 3417 "
      "distinct ids." },

  { .id = "JO29A_FGT_MESA_SOCRATA", .name = "City of Mesa, Arizona — Socrata asset catalogue",
    .name_ja = "メサ市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.mesaaz.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.mesaaz.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Mesa, Arizona "
      "(data.mesaaz.gov): every asset the domain publishes — datasets, "
      "maps, charts, filtered views and external links. At probe time "
      "(2026-09-14) resultSetSize was 41. Each record carries "
      "resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (1 page). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_BLOOMINGTON_SOCRATA", .name = "City of Bloomington, Indiana — Socrata asset catalogue",
    .name_ja = "ブルーミントン市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.bloomington.in.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.bloomington.in.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Bloomington, Indiana "
      "(data.bloomington.in.gov): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 358. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (4 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_EVERETT_SOCRATA", .name = "City of Everett, Washington — Socrata asset catalogue",
    .name_ja = "エバレット市 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.everettwa.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.everettwa.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for City of Everett, Washington "
      "(data.everettwa.gov): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 332. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (4 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_SANTACLARA_COUNTY_SOCRATA", .name = "County of Santa Clara — Socrata asset catalogue",
    .name_ja = "サンタクララ郡 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.sccgov.org",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.sccgov.org&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_max = 21,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for County of Santa Clara "
      "(data.sccgov.org): every asset the domain publishes — datasets, "
      "maps, charts, filtered views and external links. At probe time "
      "(2026-09-14) resultSetSize was 1758. Each record carries "
      "resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (18 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_SONOMA_COUNTY_SOCRATA", .name = "County of Sonoma — Socrata asset catalogue",
    .name_ja = "ソノマ郡 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.sonomacounty.ca.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.sonomacounty.ca.gov&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for County of Sonoma "
      "(data.sonomacounty.ca.gov): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 139. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (2 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_RIVERSIDE_COUNTY_SOCRATA", .name = "County of Riverside — Socrata asset catalogue",
    .name_ja = "リバーサイド郡 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.countyofriverside.us",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.countyofriverside.us&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for County of Riverside "
      "(data.countyofriverside.us): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 45. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (1 page). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_NOVASCOTIA_SOCRATA", .name = "Province of Nova Scotia — Socrata asset catalogue",
    .name_ja = "ノバスコシア州 オープンデータカタログ（Socrata）",
    .category = "government", .portal = "https://data.novascotia.ca",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"socrata\",\"catalog\",\"forgotten\",\"canada\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://api.us.socrata.com/api/catalog/v1?domains=data.novascotia.ca&limit=100&offset=0&order=dataset_id",
    .array_path = "results",
    .body_keys = "resource.description",
    .date_keys = "resource.updatedAt,resource.createdAt",
    .id_keys = "resource.id",
    .interval = 86400,
    .link_keys = "permalink",
    .page_max = 16,
    .page_param = "offset",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "resource.name",
    .description = "Socrata Discovery API catalogue for Province of Nova Scotia "
      "(data.novascotia.ca): every asset the domain publishes — "
      "datasets, maps, charts, filtered views and external links. At "
      "probe time (2026-09-14) resultSetSize was 1277. Each record "
      "carries resource.id (the 4-4 asset id), name, description, type, "
      "attribution, created/updated timestamps, column names, the "
      "domain category and tags, the owner and the permalink. Paged by "
      "offset in steps of 100 (13 pages). Walked with order=dataset_id: "
      "unordered offset paging was measured on data.bayareametro.gov to "
      "repeat 2 assets and skip 2 others across 35 pages, while the "
      "ordered walk returned all 3417 distinct ids." },

  { .id = "JO29A_FGT_MEDICAID_DKAN", .name = "Centers for Medicare and Medicaid Services (data.medicaid.gov) — "
      "DKAN dataset metastore",
    .name_ja = "米国メディケイド データセット目録（DKAN）",
    .category = "government", .portal = "https://data.medicaid.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"dkan\",\"catalog\",\"health\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.medicaid.gov/api/1/metastore/schemas/dataset/items",
    .body_keys = "description",
    .date_keys = "modified,issued",
    .id_keys = "identifier",
    .interval = 86400,
    .title_keys = "title",
    .description = "DKAN 2 metastore of Centers for Medicare and Medicaid Services "
      "(data.medicaid.gov): the dataset items endpoint returns the "
      "complete DCAT-US catalogue in one unauthenticated response — 554 "
      "datasets (554 distinct identifiers) at probe time (2026-09-14). "
      "Each record carries identifier, title, description, keywords, "
      "theme, modified/issued dates, accrual periodicity, publisher, "
      "contact point and the distribution array with CSV/API download "
      "URLs." },

  { .id = "JO29A_FGT_HEALTHCARE_GOV_DKAN", .name = "Centers for Medicare and Medicaid Services (data.healthcare.gov) "
      "— DKAN dataset metastore",
    .name_ja = "米国 HealthCare.gov データセット目録（DKAN）",
    .category = "government", .portal = "https://data.healthcare.gov",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"dkan\",\"catalog\",\"health\",\"forgotten\",\"usa\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://data.healthcare.gov/api/1/metastore/schemas/dataset/items",
    .body_keys = "description",
    .date_keys = "modified,issued",
    .id_keys = "identifier",
    .interval = 86400,
    .title_keys = "title",
    .description = "DKAN 2 metastore of Centers for Medicare and Medicaid Services "
      "(data.healthcare.gov): the dataset items endpoint returns the "
      "complete DCAT-US catalogue in one unauthenticated response — 337 "
      "datasets (337 distinct identifiers) at probe time (2026-09-14). "
      "Each record carries identifier, title, description, keywords, "
      "theme, modified/issued dates, accrual periodicity, publisher, "
      "contact point and the distribution array with CSV/API download "
      "URLs." },

  { .id = "JO29A_FGT_OPENDATA_DK_SEARCH", .name = "OpenData.dk (Danish municipalities) — CKAN dataset search",
    .name_ja = "デンマーク自治体 OpenData.dk オープンデータ検索（CKAN）",
    .category = "government", .portal = "https://admin.opendata.dk",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"denmark\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://admin.opendata.dk/api/3/action/package_search?q={q}&rows=100&start=0&sort=id%20asc",
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 0,
    .page_max = 30,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "Full-text dataset search over the OpenData.dk (Danish "
      "municipalities) CKAN catalogue (631 datasets in total at probe "
      "time 2026-09-14). Measured filter: q=trafik returned "
      "result.count=42 while an impossible entity returned 0, so the "
      "query is honoured. Each hit is a dataset record with id, title, "
      "organisation, notes, tags and its resource download URLs. Paged "
      "by start in steps of 100. Walked with sort=id asc: the default "
      "CKAN order (score desc, metadata_modified desc) was measured on "
      "data.virginia.gov to repeat 2992 datasets and never return 3301 "
      "others across 33 pages of 1000, while the id-sorted walk "
      "returned all 32458 distinct ids." },

  { .id = "JO29A_FGT_GVA_DADESOBERTES_SEARCH", .name = "Generalitat Valenciana — CKAN dataset search",
    .name_ja = "バレンシア州政府 オープンデータ検索（CKAN）",
    .category = "government", .portal = "https://dadesobertes.gva.es",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"spain\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://dadesobertes.gva.es/api/3/action/package_search?q={q}&rows=100&start=0&sort=id%20asc",
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 0,
    .page_max = 30,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "Full-text dataset search over the Generalitat Valenciana CKAN "
      "catalogue (1307 datasets in total at probe time 2026-09-14). "
      "Measured filter: q=agua returned result.count=69 while an "
      "impossible entity returned 0, so the query is honoured. Each hit "
      "is a dataset record with id, title, organisation, notes, tags "
      "and its resource download URLs. Paged by start in steps of 100. "
      "Walked with sort=id asc: the default CKAN order (score desc, "
      "metadata_modified desc) was measured on data.virginia.gov to "
      "repeat 2992 datasets and never return 3301 others across 33 "
      "pages of 1000, while the id-sorted walk returned all 32458 "
      "distinct ids." },

  { .id = "JO29A_FGT_HELSINKI_HRI_SEARCH", .name = "Helsinki Region Infoshare — CKAN dataset search",
    .name_ja = "ヘルシンキ地域 HRI オープンデータ検索（CKAN）",
    .category = "government", .portal = "https://hri.fi",
    .record_type = "dataset-record",
    .tags = "\"opendata\",\"ckan\",\"catalog\",\"forgotten\",\"finland\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://hri.fi/data/api/3/action/package_search?q={q}&rows=100&start=0&sort=id%20asc",
    .array_path = "result.results",
    .body_keys = "notes",
    .date_keys = "metadata_modified,metadata_created",
    .id_keys = "id",
    .interval = 0,
    .page_max = 30,
    .page_param = "start",
    .page_size = 100,
    .page_zero_based = 1,
    .title_keys = "title",
    .description = "Full-text dataset search over the Helsinki Region Infoshare CKAN "
      "catalogue (593 datasets in total at probe time 2026-09-14). "
      "Measured filter: q=liikenne returned result.count=132 while an "
      "impossible entity returned 1, so the query is honoured. Each hit "
      "is a dataset record with id, title, organisation, notes, tags "
      "and its resource download URLs. Paged by start in steps of 100. "
      "Walked with sort=id asc: the default CKAN order (score desc, "
      "metadata_modified desc) was measured on data.virginia.gov to "
      "repeat 2992 datasets and never return 3301 others across 33 "
      "pages of 1000, while the id-sorted walk returned all 32458 "
      "distinct ids." },
};

HP_REGISTER_TABLE(HP3B29A_FORGOTTEN)
