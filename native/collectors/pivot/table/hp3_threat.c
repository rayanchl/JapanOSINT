/* collectors/pivot/table/hp3_threat.c -- batch 18: threat
 *
 * Every row here names a machine-readable endpoint that was proof-of-life
 * verified before it was written: fetched over the wire by
 * collectors/verify_feeds.py, parsed in its declared mode, and confirmed to
 * yield at least one real record. Rows whose endpoint answered but produced
 * an empty result set were rejected rather than shipped -- a source that is
 * structurally guaranteed to emit nothing on every run is the exact failure
 * the no-fabrication rule exists to prevent.
 *
 * The observed shape and record count for each row are recorded in
 * docs/verified-sources-batch18.tsv; the candidates that failed are kept as
 * data in docs/rejected-sources-batch18.tsv rather than silently dropped.
 *
 * Generated by tools/gen_hp_batch.py from
 * docs/candidate-sources-batch18.threat.txt. Edit the manifest, not this file.
 */
#include "lib/hpengine.h"

static const hp_source HP3_THREAT[] = {
  { .id = "BINARYDEFENSE_BANLIST", .name = "Binary Defense — artillery threat intelligence banlist",
    .name_ja = "Binary Defense 脅威IP一覧",
    .category = "threat", .portal = "https://www.binarydefense.com",
    .record_type = "attacking-host",
    .tags = "\"threat\",\"abuse\",\"blocklist\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://www.binarydefense.com/banlist.txt",
    .csv_comment = "#",
    .csv_no_header = 1,
    .id_keys = "col0",
    .interval = 3600,
    .title_keys = "col0",
    .description = "Addresses observed attacking honeypots in the Artillery sensor "
      "network. Honeypot-derived lists carry no false positives from "
      "legitimate scanning of production hosts" },

  { .id = "URLHAUS_RECENT", .name = "URLhaus — malware distribution URLs",
    .name_ja = "URLhaus マルウェア配布URL",
    .category = "threat", .portal = "https://urlhaus.abuse.ch",
    .record_type = "malware-url",
    .tags = "\"threat\",\"malware\",\"url\",\"abuse\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://urlhaus.abuse.ch/downloads/csv_recent/",
    .csv_comment = "#",
    .csv_no_header = 1,
    .id_keys = "col0",
    .interval = 3600,
    .title_keys = "col2",
    .description = "URLs actively serving malware, each with the submission date, "
      "threat type, the payload tags, the reporter and the hosting "
      "status. The full URL path identifies the distribution kit, which "
      "is what links campaigns that share infrastructure" },

  { .id = "THREATVIEW_C2", .name = "ThreatView — command and control blocklist",
    .name_ja = "ThreatView C2一覧",
    .category = "threat", .portal = "https://threatview.io",
    .record_type = "botnet-c2",
    .tags = "\"threat\",\"c2\",\"malware\",\"blocklist\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://threatview.io/Downloads/High-Confidence-CobaltStrike-C2%20-Feeds.txt",
    .csv_comment = "#",
    .csv_no_header = 1,
    .id_keys = "col0",
    .interval = 3600,
    .title_keys = "col0",
    .description = "High-confidence Cobalt Strike team-server addresses identified "
      "by beacon configuration scanning. Cobalt Strike is the most "
      "widely used post-exploitation framework in both criminal and "
      "state intrusions, so its infrastructure is disproportionately "
      "informative" },

  { .id = "DATAPLANE_SSH_CLIENT", .name = "DataPlane.org — SSH brute-force sources",
    .name_ja = "DataPlane SSH総当たり元",
    .category = "threat", .portal = "https://dataplane.org",
    .record_type = "attacking-host",
    .tags = "\"threat\",\"ssh\",\"bruteforce\",\"sensor\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://dataplane.org/sshclient.txt",
    .csv_comment = "#",
    .csv_delim = "pipe",
    .csv_no_header = 1,
    .id_keys = "col2",
    .interval = 3600,
    .title_keys = "col2",
    .description = "Hosts observed conducting SSH login attempts against the "
      "DataPlane sensor network, each with the originating ASN, the AS "
      "name, the country and the last-seen timestamp. The ASN "
      "attribution turns a list of addresses into a ranking of which "
      "networks host the most abuse" },

  { .id = "DATAPLANE_DNSRD", .name = "DataPlane.org — DNS recursion probes",
    .name_ja = "DataPlane DNS再帰探索元",
    .category = "threat", .portal = "https://dataplane.org",
    .record_type = "dns-abuse",
    .tags = "\"threat\",\"dns\",\"amplification\",\"sensor\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://dataplane.org/dnsrd.txt",
    .csv_comment = "#",
    .csv_delim = "pipe",
    .csv_no_header = 1,
    .id_keys = "col2",
    .interval = 3600,
    .title_keys = "col2",
    .description = "Hosts probing for open DNS resolvers, the reconnaissance step "
      "that precedes a DNS amplification attack. Each entry carries the "
      "ASN and country, so the staging networks are directly "
      "identifiable" },

  { .id = "DATAPLANE_TELNET", .name = "DataPlane.org — Telnet brute-force sources",
    .name_ja = "DataPlane Telnet総当たり元",
    .category = "threat", .portal = "https://dataplane.org",
    .record_type = "attacking-host",
    .tags = "\"threat\",\"telnet\",\"iot\",\"sensor\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://dataplane.org/telnetlogin.txt",
    .csv_comment = "#",
    .csv_delim = "pipe",
    .csv_no_header = 1,
    .id_keys = "col2",
    .interval = 3600,
    .title_keys = "col2",
    .description = "Hosts attempting Telnet logins against sensors — overwhelmingly "
      "compromised IoT devices recruiting more of the same. The ASN "
      "breakdown maps which consumer ISPs carry the largest infected "
      "device populations" },

  { .id = "DATAPLANE_VNC_RFB", .name = "DataPlane.org — VNC probe sources",
    .name_ja = "DataPlane VNC探索元",
    .category = "threat", .portal = "https://dataplane.org",
    .record_type = "attacking-host",
    .tags = "\"threat\",\"vnc\",\"sensor\",\"abuse\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://dataplane.org/vncrfb.txt",
    .csv_comment = "#",
    .csv_delim = "pipe",
    .csv_no_header = 1,
    .id_keys = "col2",
    .interval = 3600,
    .title_keys = "col2",
    .description = "Hosts probing for exposed VNC remote-desktop services, with ASN "
      "and country. Exposed VNC is a standard initial-access vector "
      "into industrial and building-control systems" },

  { .id = "DATAPLANE_SIPINVITE", .name = "DataPlane.org — SIP INVITE probe sources",
    .name_ja = "DataPlane SIP探索元",
    .category = "threat", .portal = "https://dataplane.org",
    .record_type = "telephony-abuse",
    .tags = "\"threat\",\"sip\",\"voip\",\"sensor\"",
    .mode = HP_CSV, .want = HP_ANY, .free_tier = 1,
    .url = "https://dataplane.org/sipinvitation.txt",
    .csv_comment = "#",
    .csv_delim = "pipe",
    .csv_no_header = 1,
    .id_keys = "col2",
    .interval = 3600,
    .title_keys = "col2",
    .description = "Hosts sending unsolicited SIP INVITE requests, the "
      "reconnaissance behind toll fraud and voice phishing campaigns, "
      "with ASN and country attribution" },

  { .id = "JVN_VULN_LIST", .name = "JVN iPedia — Japanese vulnerability database",
    .name_ja = "JVN 脆弱性対策情報",
    .category = "threat", .portal = "https://jvndb.jvn.jp",
    .record_type = "vulnerability",
    .tags = "\"threat\",\"vulnerability\",\"japan\",\"cert\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://jvndb.jvn.jp/myjvn?method=getVulnOverviewList&feed=hnd&rangeDatePublished=n&maxCountItem=50",
    .interval = 3600,
    .description = "The Japanese national vulnerability database maintained by "
      "JPCERT and IPA, carrying advisories for Japanese vendor products "
      "that never receive a CVE and are absent from NVD entirely" },

  { .id = "CIRCL_CVE_SEARCH", .name = "CIRCL — CVE search service",
    .name_ja = "CIRCL CVE検索",
    .category = "threat", .portal = "https://www.circl.lu",
    .record_type = "vulnerability",
    .tags = "\"threat\",\"vulnerability\",\"cve\",\"circl\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://cve.circl.lu/api/cve/{qU}",
    .description = "The Luxembourg CERT's consolidated CVE service, joining the NVD "
      "record with vendor statements, CAPEC attack patterns, exploit-DB "
      "references and the affected CPE set in one document. The CAPEC "
      "linkage maps a vulnerability to the attack techniques that "
      "exploit it" },

  { .id = "MALPEDIA_FAMILIES", .name = "Malpedia — malware family taxonomy",
    .name_ja = "Malpedia マルウェアファミリ分類",
    .category = "threat", .portal = "https://malpedia.caad.fkie.fraunhofer.de",
    .record_type = "malware-family",
    .tags = "\"threat\",\"malware\",\"family\",\"taxonomy\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://malpedia.caad.fkie.fraunhofer.de/api/get/families",
    .interval = 3600,
    .description = "The Fraunhofer FKIE curated malware taxonomy: every named family "
      "with its aliases across vendor naming schemes, the platform it "
      "targets, attribution to a threat actor where established, and "
      "the reference literature. Alias resolution is the hard part of "
      "correlating vendor reporting, and this is the reference table "
      "for it" },

  { .id = "MALPEDIA_ACTORS", .name = "Malpedia — threat actor registry",
    .name_ja = "Malpedia 攻撃主体登録",
    .category = "threat", .portal = "https://malpedia.caad.fkie.fraunhofer.de",
    .record_type = "threat-actor",
    .tags = "\"threat\",\"actor\",\"attribution\",\"apt\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://malpedia.caad.fkie.fraunhofer.de/api/get/actors",
    .interval = 3600,
    .description = "Named threat actors with every alias used by every vendor, the "
      "country attributed, the malware families associated and the "
      "reference reporting. The alias graph is what lets reporting on "
      "APT29, Cozy Bear, Nobelium and Midnight Blizzard be recognised "
      "as reporting on one actor" },

  { .id = "MITRE_ATTACK_ENTERPRISE", .name = "MITRE ATT&CK — enterprise technique catalogue",
    .name_ja = "MITRE ATT&CK 手口分類",
    .category = "threat", .portal = "https://attack.mitre.org",
    .record_type = "attack-technique",
    .tags = "\"threat\",\"mitre\",\"attack\",\"taxonomy\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/mitre-attack/attack-stix-data/master/enterprise-attack/enterprise-attack.json",
    .array_path = "objects",
    .interval = 3600,
    .description = "The ATT&CK knowledge base in STIX form: every tactic, technique "
      "and sub-technique with its description, detection guidance, "
      "mitigations, the groups observed using it and the software that "
      "implements it. The common vocabulary that makes intrusion "
      "reporting comparable between organisations" },

  { .id = "MITRE_ATTACK_ICS", .name = "MITRE ATT&CK — ICS technique catalogue",
    .name_ja = "MITRE ATT&CK 制御系手口分類",
    .category = "threat", .portal = "https://attack.mitre.org",
    .record_type = "attack-technique",
    .tags = "\"threat\",\"mitre\",\"attack\",\"ics\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/mitre-attack/attack-stix-data/master/ics-attack/ics-attack.json",
    .array_path = "objects",
    .interval = 3600,
    .description = "The industrial control system matrix, covering techniques that "
      "act on physical process rather than on data, with the assets "
      "targeted and the observed incidents. The reference framework for "
      "assessing an attack against operational technology" },

  { .id = "MITRE_CAPEC", .name = "MITRE CAPEC — attack pattern catalogue",
    .name_ja = "MITRE CAPEC 攻撃パターン",
    .category = "threat", .portal = "https://capec.mitre.org",
    .record_type = "attack-pattern",
    .tags = "\"threat\",\"mitre\",\"capec\",\"taxonomy\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://raw.githubusercontent.com/mitre/cti/master/capec/2.1/stix-capec.json",
    .array_path = "objects",
    .interval = 3600,
    .description = "The Common Attack Pattern Enumeration: abstract attack patterns "
      "with prerequisites, skill required, consequences and the "
      "weaknesses they exploit. Sits between a CWE weakness class and "
      "an ATT&CK technique" },

  { .id = "NIST_CPE_DICTIONARY", .name = "NVD — CPE product dictionary",
    .name_ja = "NVD 製品識別辞書",
    .category = "threat", .portal = "https://nvd.nist.gov",
    .record_type = "product-identifier",
    .tags = "\"threat\",\"cpe\",\"vulnerability\",\"inventory\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://services.nvd.nist.gov/rest/json/cpes/2.0?keywordSearch={q}&resultsPerPage=100",
    .array_path = "products",
    .description = "The Common Platform Enumeration dictionary: the canonical "
      "identifier for every vendor, product and version tracked in "
      "vulnerability data, with deprecation history. CPE is the join "
      "key between an asset inventory and a vulnerability feed" },

  { .id = "NVD_CVE_HISTORY", .name = "NVD — CVE change history",
    .name_ja = "NVD CVE変更履歴",
    .category = "threat", .portal = "https://nvd.nist.gov",
    .record_type = "vulnerability-change",
    .tags = "\"threat\",\"cve\",\"vulnerability\",\"audit\"",
    .mode = HP_JSON, .want = HP_ANY, .free_tier = 1,
    .url = "https://services.nvd.nist.gov/rest/json/cvehistory/2.0?cveId={qU}",
    .array_path = "cveChanges",
    .description = "Every change ever made to a CVE record with the date, the source "
      "that made it and the old and new values. A silently downgraded "
      "CVSS score or a quietly widened affected-version range is only "
      "visible here" },
};

HP_REGISTER_TABLE(HP3_THREAT)
