import SwiftUI

/// Mounted inside Console's NavigationStack. Originally a Map sheet, then a
/// top-level tab, now a Console destination.
struct FollowLogTab: View {
    var body: some View { FollowPanel() }
}
