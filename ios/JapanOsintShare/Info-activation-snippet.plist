<?xml version="1.0" encoding="UTF-8"?>
<!--
  Replace the NSExtensionActivationRule that Xcode's Share Extension template
  generates (it ships TRUEPREDICATE, which makes JapanOSINT appear in the
  share sheet for EVERYTHING — including PDFs, contacts and files it cannot
  do anything with). Paste the NSExtensionAttributes dict below into
  JapanOsintShare/Info.plist, inside the existing NSExtension dict.

  One item of one supported kind. Anything else and we do not appear at all,
  which is the honest behaviour: an app that offers to accept a file and then
  says "unsupported content" has wasted the user's tap.
-->
<plist version="1.0">
<dict>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionAttributes</key>
        <dict>
            <key>NSExtensionActivationRule</key>
            <dict>
                <key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
                <integer>1</integer>
                <key>NSExtensionActivationSupportsImageWithMaxCount</key>
                <integer>1</integer>
                <key>NSExtensionActivationSupportsText</key>
                <true/>
                <!-- Explicitly refused: files, movies, attachments. -->
                <key>NSExtensionActivationSupportsFileWithMaxCount</key>
                <integer>0</integer>
                <key>NSExtensionActivationSupportsMovieWithMaxCount</key>
                <integer>0</integer>
                <key>NSExtensionActivationSupportsAttachmentsWithMaxCount</key>
                <integer>0</integer>
            </dict>
        </dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.share-services</string>
        <key>NSExtensionPrincipalClass</key>
        <string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
    </dict>
</dict>
</plist>
