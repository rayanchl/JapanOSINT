{
  "name": "japan-osint-map",
  "version": "1.0.0",
  "private": true,
  "comment": "Backend is the native C binary — launch with ./launch.sh. These scripts cover the web client + llama only.",
  "scripts": {
    "dev": "npm run dev:client",
    "dev:full": "concurrently \"npm run dev:llama\" \"npm run dev:client\"",
    "dev:client": "cd client && npm run dev",
    "dev:llama": "bash scripts/start-llama.sh",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
